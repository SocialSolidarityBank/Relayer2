DATABASE_URL=postgres://relayer:relayer@localhost:55432/relayer
# 한 Postgres 를 다른 서비스와 나눠 쓸 때 전용 스키마. 비우면 public.
PGSCHEMA=
PII_ENC_KEY=   # openssl rand -base64 32
SESSION_SECRET= # openssl rand -base64 32
PORT=8787
