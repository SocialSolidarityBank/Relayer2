# 릴레이어 디자인 정본

릴레이어의 화면 규칙은 이 문서 하나에서 정한다. 현재 구현과 사용자가 확정한 화면 규칙을 다루되, 규칙을 정한 것과 구현을 검증한 것을 구분한다. CCC에만 있는 화면이나 계획을 구현된 것으로 적지 않는다. 이름은 [GLOSSARY.md](GLOSSARY.md), 동작과 저장 계약은 [SPEC.md](SPEC.md)가 정한다. 디자인을 정리한다는 이유로 두 문서의 의미를 바꾸지 않는다.

이 문서의 기준은 2026-09-16에 확인한 릴레이어의 CSS·마크업과 실제 렌더다. CCC 문서와 다르면 릴레이어의 현재 구현을 기록한다. 차이를 숨기거나 CCC 화면을 다시 만드는 근거로 쓰지 않는다. 아래의 이유는 이식 코드의 주석과 커밋에 남은 선택 근거이며, 새 사용자 실험의 결과가 아니다.

## 1. 정본과 구현의 경계

| 대상 | 역할 |
|---|---|
| 이 문서 | 릴레이어에서 지킬 디자인 규칙과 현재 한계를 정한다. |
| `web/src/styles/tokens.css` | 색·간격·크기 등 값을 CSS 변수로 제공한다. |
| `web/src/styles/wire.css` | 이식한 `wire-*` 부품의 형태와 내부 정렬을 구현한다. |
| `web/src/styles/shell.css` | 페이지 폭과 반응형 배치, 기록 레일 등 화면 규칙을 구현한다. |
| `web/src/styles/app.css` | 릴레이어의 간단한 내비게이션과 이식 후 필요한 국소 보정을 구현한다. |
| `web/src/ui.tsx` | 현재 화면이 재사용하는 React 부품을 제공한다. |

스타일은 `tokens.css → wire.css → shell.css → app.css` 순서로 읽는다. 뒤의 파일이라는 이유만으로 이식 부품을 다시 정의하지 않는다. 기존 부품을 먼저 찾고, 릴레이어에만 필요한 차이만 해당 선택자에서 보정한다.

`GLOSSARY.md` §7-2의 디자인 메모와 `CARRYOVER.md`의 이식 계획은 이 문서가 정리한 규칙의 근거다. 이식 파일에 남은 pen 정본, CCC의 `DESIGN-RULES.md`, ADR 번호와 검사 명령도 **CCC에서의 출처와 이력**이다. 릴레이어의 별도 디자인 정본이나 실행 가능한 검사 목록으로 보지 않는다. 이름의 결정 권한은 계속 `GLOSSARY.md`에 둔다. 릴레이어에는 두 번째 디자인 요약본을 만들지 않는다. 규칙을 바꾸면 이 문서와 해당 CSS를 함께 맞춘다.

`wire-*` 클래스 이름은 보존한다. 화면의 표시명을 바꾸더라도 URL·필드·권한 식별자를 바꾸지 않는다.

이 문서의 “현재 작업”과 “이번 표면 정리”는 2026-09-16 디자인 레인 인계 작업을 가리킨다. 이 작업에서만 허용하는 수정 범위는 이 문서, 스타일 파일, 화면의 클래스와 마크업 정렬이다. 화면 구획의 순서·저장 동작·서버·배포 스크립트는 수정하지 않으며 CCC 파일은 읽기만 한다. 이 작업의 제한을 향후 승인된 기능·배치 변경까지 금지하는 상시 규칙으로 해석하지 않는다.

## 2. 색과 대비

### 기본 색

기본 화면은 라이트다. 아래 값은 `tokens.css`의 기본값이며, CSS에 다크 값이 있다는 사실을 사용자가 테마를 전환할 수 있다는 뜻으로 쓰지 않는다.

| 토큰 | 라이트 값 | 쓰는 이유와 자리 |
|---|---|---|
| `--canvas` | `#FAFAF9` | 페이지 바탕이다. 카드의 흰 면과 배경을 구분한다. |
| `--panel` | `#FFFFFF` | 카드와 입력칸의 읽고 쓰는 면이다. |
| `--muted` | `#F5F5F4` | 보조 표면을 구분한다. |
| `--ink` | `#3D3445` | 본문·입력값·제목의 기본 글자다. 순검정 대신 이식한 웜 잉크를 유지한다. |
| `--sub` | `#534E57` | 라벨·설명·메타다. 크기와 굵기 차이와 함께 제목과 설명을 가른다. |
| `--line` | `#D7D3CE` | 카드·버튼·배지의 기본 1px 경계다. 읽는 카드에 그림자를 겹치지 않는다. |
| `--line-control` | `#E3E3E3` | 입력칸의 1px 경계다. 낮은 대비는 아래 한계에 기록한다. |
| `--risk` | `#B52573` | 위험 신호와 오류다. 일반 장식이나 성공 표시에 쓰지 않는다. |
| `--risk-tint-solid` | `#F7EEF2` | 위험 신호 배너의 배경이다. |

### 계열색과 채운 면

| 글자용 토큰 | 값 | 현재 쓰임 |
|---|---|---|
| `--blue-deep` | `#486997` | 포커스 링과 도움말 안 링크다. |
| `--mint-deep` | `#56CC99` | 폼 묶음 제목과 목표 라벨이다. |
| `--lavender-deep` | `#7D4E9B` | 필수 표식의 글자와 아웃라인이다. |

면과 글자의 토큰을 섞지 않는다. 필수 표식은 배지 채움색 대신 `--lavender-deep` 글자와 아웃라인을 쓰고 배경은 비운다. 별도 소비가 없는 기본 면·tint 토큰은 현재 사용 규칙에서 제외한다. 실제 버튼과 선택 표시에 쓰이는 그라데이션 값은 아래 표에 적는다.

| 토큰 | 값 | 역할과 이유 |
|---|---|---|
| `--badge-blue` | `#67A9F0` | 블루 배지의 면이다. 일반 글자용 deep와 분리되어 있다. |
| `--badge-mint` | `#56CC99` | 민트 배지의 면이다. |
| `--badge-lavender` | `#C890F0` | 라벤더 배지의 면이다. |
| `--on-badge` | `#FFFFFF` | 라이트의 채운 배지 위 글자다. |
| `--on-action` | `#3D3445` | 밝은 그라데이션 행동 면 위 글자다. |
| `--gradient-brand` | `90deg, #8ED9B8 → #CBA8E2` | 세컨더리 버튼의 아웃라인이다. |
| `--gradient-action` | `135deg, #A8E3C6 → #D1B3E3` | 프라이머리 버튼과 선택한 체크·라디오의 면이다. |
| `--gradient-deep` | `180deg, #56CC99 → #C890F0` | 체크·라디오 경계에 쓰는 계열 선이다. |
| `--line-on-action` | `rgba(61,52,69,0.5)` | 프라이머리 버튼의 밝은 행동 면을 구분하는 1px 경계다. |
| `--discrepancy-tint` | `color-mix(in srgb, var(--discrepancy) 15%, transparent)` | 불일치 문장의 배경이다(2026-09-18 Q, v6). `--discrepancy` 를 15% 로 섞은 값이지 새 hue 가 아니다. |

CCC는 블루 채움 배지를 시간 축으로 제한하지만, 릴레이어의 `Badge`는 `blue`를 직접 받으며 일정에서 `물어볼 것` 건수에도 쓴다. 현재 규칙을 시간 축 전용이라고 적지 않는다. CCC의 추가 배지 variation이나 `TimeAxisBadge`를 릴레이어의 부품으로 소개하지 않는다.

**상태 띠(2026-09-18 Q, v6).** 분석이 단 문장의 왼쪽 색 띠다 — 변화는 민트 `--mint`, 확인필요는 코랄 `--badge-coral`, 완료·해결은 블루 `--blue`, 선택한 문장은 `--blue-tint` 면이다. 띠와 불일치 배경(`--discrepancy-tint`)은 한 문장에 동시에 설 수 있다 — 띠는 상태, 배경은 불일치라 축이 다르다. 이모지는 쓰지 않는다.

### 대비의 현재 한계

흰 패널과의 상대 휘도 대비를 계산하면 `--ink`는 11.83:1, `--sub`는 8.09:1이다. 도움말을 흐리게 만들기 위해 opacity를 더 낮추지 않는다. `--blue-deep`는 5.61:1, `--lavender-deep`는 6.11:1, `--risk`는 6.04:1이다.

반면 민트 묶음 제목(`#56CC99` / 흰색)은 2.00:1이고, 흰 글자와 블루·민트·라벤더 배지 면의 대비는 각각 2.47:1·2.00:1·2.41:1이다. 입력칸 경계와 흰 면은 1.28:1이다. 이식 주석의 “deep 3색이 모두 AA를 통과한다”는 문장을 릴레이어의 검증 결과로 쓰지 않는다. CCC에서 색을 승인했다는 사실도 접근성 통과를 뜻하지 않는다.

[WCAG 2.2의 일반 텍스트 대비 기준](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)은 4.5:1이다. 현재 화면 전체가 이 기준을 통과한다고 주장하지 않는다. 기존 팔레트는 이번 표면 정리에서 바꾸지 않으며, 낮은 대비가 있는 자리는 확인된 한계로 남긴다. 비활성 버튼은 일반 텍스트 대비 검사와 구분하지만, 비활성 여부를 색만으로 구현해서는 안 된다.

다크와 고대비는 `html[data-theme="dark"]`, `html[data-contrast="high"]`로 적용되는 이식 CSS다. 다크의 바탕·패널·본문·보조 글자는 각각 `#100E13`·`#18151D`·`#F2EFF4`·`#B5AEBE`다. 릴레이어에는 이 속성을 바꾸는 사용자 조작이 없고 OS 테마 자동 적용도 없다. 따라서 현재 대비 문제의 해결책으로 테마 전환을 안내하지 않는다.

## 3. 글자와 형태

### 글자

서체 스택은 `Pretendard Variable, Pretendard, Apple SD Gothic Neo, Noto Sans KR, system-ui, sans-serif`다. 릴레이어는 서체 이름을 지정하지만 폰트 파일을 번들하거나 `@font-face`로 공급하지 않는다. CCC의 Pretendard self-hosting이 그대로 구현되어 있다고 적지 않는다. 실행 기기에 따라 폴백이 달라지므로 정렬은 실제 브라우저에서 잰다.

| 자리 | 크기 | 굵기 | 색·행간 |
|---|---|---|---|
| 페이지 제목 `.wire-page-title` | 28px (`--text-2xl`), 767px 이하 24px (`--text-xl`) | 600 | `--ink`, 1.25 |
| 카드 제목 `.wire-card-title` | 16px (`--text-md`) | 600 | `--ink`, 1.35 |
| 항목 제목 `.wire-item-title` | 14px (`--text-sm`) | 400 | `--ink` |
| 폼 라벨 `.wire-form-label` | 14px (`--text-sm`) | 600 | `--sub`, normal |
| 폼 묶음 제목 `legend` | 16px | 600 | `--mint-deep`, 1.35 |
| 입력값·선택창·placeholder | 13px (`--text-detail`) | 400 | `--ink`, 단일행 normal·여러 줄 1.6 |
| 필드 도움말 `.wire-form-hint`, 선택 힌트 `.wire-choice-hint` | 12px (`--text-caption`) | 400 | `--sub` |
| 설명 `.panel-meta`, `.wire-item-desc` | 12px (`--text-caption`) | 400 | `--sub` |
| 글자 버튼 | 14px | 600 | 변형별 색, normal |
| 배지 | 12px (`--text-badge`) | 400 | 배지 면에 맞는 전경 |

기본 본문은 16px·400·행간 1.55다. 작은 도움말과 배지는 해당 역할에서만 쓴다. 화면 이름이 길다는 이유로 제목이나 내비게이션의 글자를 줄이지 않는다. 기본 자간은 `--tracking-base: -0.01em`이다. `--nudge-hangul`은 0em이다. 글자를 임의로 위아래 밀지 않고 상자의 정렬과 실제 사용 글꼴을 먼저 확인한다.

현재 상담 기록하기는 일반 `Field`·`Card`를 사용한다. CCC의 `.record-writing-help` 전용 12px 안내 규칙을 이 화면 전체의 현행 규칙으로 옮기지 않는다. 일반 `p`와 `.empty`도 용도별 클래스 없이 자동으로 위계가 맞는다고 보지 않는다.

### 형태와 간격 토큰

| 토큰 | 값 | 쓰는 이유와 자리 |
|---|---|---|
| `--radius-control` | 6px | 입력칸과 선택창의 사각 형태다. |
| `--radius-card` | 12px | 카드와 반복 행, 배너를 묶는 형태다. |
| `--radius-pill` | 9999px | 글자 버튼과 배지를 알약으로 만든다. |
| `--radius-xs` | 4px | 체크박스의 모서리다. |
| `--control-height` | 40px | 단일행 입력칸과 선택지의 최소 높이다. |
| `--pill-height` | 32px | 글자 버튼의 높이다. |
| `--badge-height` | 22px | 배지 높이와 폼 라벨 행의 최소 높이다. 필수 배지 유무로 입력칸 윗선이 달라지지 않게 한다. |
| `--control-pad` | 10px (`--space-2-5`) | 입력칸 좌우와 textarea 사방 여백이다. 카드 여백과 별도로 관리한다. |

현재 화면이 쓰는 기본 간격은 `--space-1/2/3/4/5/6/8 = 4/8/12/16/20/24/32px`다. 사용하는 하프스텝은 `--space-1-5/2-5/3-5 = 6/10/14px`다. 6은 선택 표시와 글자 사이, 10은 입력칸 안쪽, 14는 글자 버튼 좌우 여백에 쓴다. 선언만 남은 2px·40px 간격 토큰을 새 사용 규칙으로 확장하지 않는다.

## 4. 배치와 정렬

### 페이지와 카드 목록

로그인한 화면의 셸은 `routes.tsx`의 `.app-shell`이고 머리줄·사이드바·본문 세 부분이다. 본문 열(`.content-column`) 안의 `.page-content`가 페이지 폭과 여백을 소유한다. 로그인·불러오는 중 화면만 `.wire-shell` 한 장을 쓴다. `.page-content`의 최대 폭은 패딩을 포함해 1440px이다. 좌우 패딩은 32px, 위아래는 48px이며, 뷰포트 767px 이하에서는 각각 16px·32px이다. 페이지의 큰 구획 사이에는 `--section-gap: 32px`을 쓴다.

`.card-grid`는 `repeat(auto-fit, minmax(min(100%, var(--grid-min)), 1fr))`와 간격 20px을 쓴다. 인테이크 작성하기의 `.two-col`은 `--grid-min: 560px`으로 덮는다(15초 다시보기도 쓰던 자리였으나 그 화면은 2026-09-17 폐지). 최소 560px 두 칸과 간격 20px이 들어가는 내부 폭 1140px부터 두 열이 열린다. 페이지 최대 폭 안에서는 세 열이 열리지 않는다. 좁은 화면은 한 열이다. 읽는 흐름과 선택지 줄바꿈을 지키려는 릴레이어의 결정이며, 새 열 수 체계를 만들지 않는다.

같은 행의 `.card-grid > .wire-card`는 높이를 맞춘다. `.briefing-cards-grid`(같은 열 계산 + `align-items:start`)는 15초 다시보기 화면과 함께 쓰임이 없어졌다 — CSS는 이식 상태로 남아 있다(§10).

카드 내부 패딩은 24px이고 기본 본문 간격은 12px이다. 현재 폼 카드도 같은 12px을 사용한다. CCC의 `.wire-form-card`에 있는 20px 간격은 릴레이어의 마크업이 사용하지 않으므로 현행 폼 규칙에 포함하지 않는다.

내비는 왼쪽 사이드바다(2026-09-16 Q — 한 줄에 아홉 개가 늘어선 구 `.app-nav`를 대체했다). 폭 280px, 머리줄 아래 sticky이고 묶음은 `일정`·`당사자`·`설정` 순서다. 항목의 클릭 높이는 최소 32px이다. 머리줄은 기관 이름과 로그인 사용자 이름만 담는다.

**메뉴는 목록·등록만 가리킨다**(2026-09-18 Q A1). 일정 묶음은 `상담 일정 보기` 하나, 당사자 묶음은 `당사자 목록`·`당사자 등록`, 설정 묶음은 볼 수 있는 설정 화면들이다. 구 `상담 일정 등록`·`상담 기록하기` 메뉴와 그 뒤에 서던 고르는 화면(`#/pick/*`)은 걷었다 — **한 사람을 가리키는 행동이라 목록·등록과 층이 다르고**, 당사자 목록 카드가 그 입구다. 기록은 늘 일정 예약 화면을 지나며(Q 결정 D1), 기록이 있는 사례의 다음 일정은 당사자 정보 HERO의 `상담 일정 등록`이 맡는다.

**홈은 `상담 일정 보기`(`#/schedule`)다**(2026-09-17 Q). 일정 묶음의 첫 항목이고, 로그인하면 이 화면에 떨어진다. 자리를 돌리는 일은 **로그아웃**이 한다 — 로그아웃할 때 주소를 홈으로 되돌려 두고, 로그인 뒤에는 주소를 건드리지 않는다. 로그인 응답이 온 뒤에 주소를 바꾸면 그 사이에 옮겨 간 화면을 뒤늦게 낚아챈다(2026-09-17 실측: 로그인 직후 당사자 등록으로 간 동선이 일정으로 튕겼다). 세션이 끊겨 다시 로그인하는 경우에는 보던 주소가 남아 그 자리로 돌아간다.

**메뉴 항목마다 16px 라인 아이콘이 선다**(2026-09-17 Q). 글리프는 CCC `shell-icons.tsx`에서 가져와 `web/src/shell-icons.tsx`에 두고, 현재 화면이 쓰는 것만 옮겼다. 새로 그리지 않는다 — 같은 뜻의 글리프가 제품마다 갈린다. 획은 1.5px, 색은 `currentColor`라 활성 항목에서 글자와 같이 물든다. 배정은 같은 대상의 보기와 등록을 더하기 유무로 가른다: 상담 일정 보기 `upcoming`(시계) · 당사자 목록 `participants` · 당사자 등록 `participant-add` · 내 정보 `participants` · 실무자 관리 `invite` · 기관 정보 관리 `org` · 시스템 `settings`. 일정 묶음이 보기 하나가 된 뒤(A1) `calendar-plus`·`record` 글리프는 사이드바에서 쓰이지 않는다 — 이식 파일에는 남겨 둔다.

**계정 행동은 사이드바 하단 세 버튼이다**(2026-09-17 Q). 순서는 설정 · 테마 · 로그아웃이고, 옷은 이식한 `.header-icon-button` 32px 원형(그라데이션 1px 테두리 + `--panel` 채움)을 그대로 쓴다. 라벨은 `aria-label`과 `title`이 갖는다. CCC는 이 묶음을 데스크톱 머리줄과 드로어 상단에 두지만, 릴레이어는 하단 한 자리로 모은다 — 머리줄에 로그아웃을 같이 두면 어느 것이 정본인지 알 수 없다. 설정 버튼은 `시스템`으로 가고, 시스템을 볼 수 없는 실무자는 자기가 볼 수 있는 첫 묶음으로 간다. 테마 버튼은 §9를 따른다.

**767px 이하에서는 사이드바가 본문 위에 눕는다**(2026-09-16, `app.css` 미디어 블록). 이식 CSS는 이 폭에서 사이드바를 오른쪽 드로어로 바꾸고 머리줄을 숨기지만, 릴레이어에는 여는 손잡이(`.drawer-bar`)·스크림·닫기 버튼이 없어 390px에서 내비가 화면 밖(x=375)에 갇히고 이름·로그아웃까지 사라졌다. 항목과 순서를 그대로 둔 채 흐름으로 되돌리고, 묶음은 폭이 되는 만큼 나란히 세운다. 실측(2026-09-16, 390px): 머리줄 64px · 내비 268px · 본문 시작 y=388, 링크 7개 모두 화면 안. 드로어를 살리려면 여닫는 상태·스크림·초점 처리가 필요하다 — 이 폭이 실사 표면이 되면 그때 붙인다.

### 당사자 이름 중심 원칙

**특정 당사자를 다루는 페이지와 당사자 카드는 이름 중심으로 구성한다.** 목록뿐 아니라 당사자 정보, 인테이크, 상담 기록, 다시보기, 해당 당사자의 일정 화면에도 적용하는 공통 규칙이다.

- **이름을 첫 번째 시각적 기준으로 둔다.** 한 사람을 다루는 페이지는 상단의 주된 제목 영역에, 카드는 첫 줄에 이름을 가장 먼저 알아볼 수 있게 표시한다. 이름을 작은 부제나 접힌 영역에만 두지 않는다.
- **화면 용도와 업무 정보는 이름을 보조한다.** `당사자 정보`·`상담 기록하기` 같은 화면 용도, 사업명·회차·상태·담당자·가명 ID가 이름보다 앞서거나 더 강하게 보이지 않게 한다. 여러 사람을 보여 주는 목록 페이지의 제목은 `당사자 목록`으로 유지하고 각 카드에서 이름을 앞세운다.
- **기본정보를 접어 숨기지 않는다.** 당사자 카드는 아코디언으로 만들지 않으며, 이름과 주요 정보는 별도 조작 없이 보여 준다. 긴 상담 이력이나 상세 내용만 필요에 따라 접을 수 있다.
- **이름이 없거나 열람 권한 때문에 받지 못하면 허용된 가명으로 대체한다.** 이름이 있을 때 가명은 보조 정보다. 화면을 채우려고 권한을 우회하거나 숨겨진 이름을 추정하지 않는다.
- **이름 중심 표시는 데이터 식별 규칙이 아니다.** 동명이인은 이미 제공된 사업·연락처 등 허용된 보조 정보로 구분하며, 이름이 같다는 이유로 합치지 않는다. 내부 ID·URL·API·배정 권한은 바꾸지 않는다.

새 화면과 기존 화면을 수정할 때 이 원칙으로 검수한다. 이 절은 사용자가 확정한 준수 규칙이며, 모든 기존 페이지의 적용·렌더 검증이 끝났다는 뜻은 아니다.

### 당사자 카드(HERO)와 탭

**한 사람을 다루는 화면의 머리는 당사자 카드다**(2026-09-17 Q — CCC `ParticipantHeroCard`, D38). "여기가 이 사람의 카드다"가 첫 문장이 되게, 화면 용도가 아니라 **이름**이 제목(`h1`, 18px·600)이다. 이식 CSS(`wire.css:269-293`)를 그대로 쓰고 새 클래스를 만들지 않는다.

- 구분선 **위에는 이름과 행동만** 둔다. 행동은 오른쪽 끝에서 세로 가운데다(당사자 정보가 셋, 나머지는 하나).
- 구분선 **아래는 라벨/값 정보 격자 4열**이다(2026-09-17 Q — 카드 장폭이 넓어 3열은 칸이 남는다). 칸은 **공용 부품이 고정한다**(2026-09-18 Q C1 — `ui.tsx` 의 `participantHeroDetails()`): `당사자 ID` · `참여 사업`(회차 꼬리표) · `연락처` · `이메일`. 화면마다 칸을 따로 짜서 어떤 화면에는 이메일이 없던 것을 한 자리로 모았다. **없는 값은 칸째로 빠진다.** 값이 칸보다 길면 한 줄로 잘라 말줄임하고 전체는 `title`로 남긴다. 실측 1280: 열 폭 199.75px × 4.
- 4열 규칙은 `app.css` 의 L1 구획이 갖고, **좁은 화면 1열 규칙을 같은 컨테이너 쿼리로 다시 선언한다**(`@container (max-width: 760px)`). `app.css` 는 `wire.css` 보다 뒤라 조건 없는 선언 하나만 두면 760px 이하의 이식 규칙(80px 라벨 + 값 한 열)까지 덮어 390 화면에서 4열이 65.75px 로 쪼개졌다(2026-09-17 실측, PR #22의 결함).
- **배지를 두지 않는다** — 상태도 라벨/값이다(`상태: 진행 중`·`종결`). CCC 2026-09-08 Q와 같다.
- 정보가 없으면 구분선과 아래 영역을 만들지 않는다. 이름이 없으면 가명이 이름 자리를 대신하고, 이름이 있으면 가명은 `당사자 ID` 값으로 내려간다.
- **연락처·이메일을 올린다**(2026-09-17 Q). 동명이인을 구분하는 단서가 그 둘이라 실무자가 머리에서 바로 확인해야 한다. 이 때문에 이름을 받지 않던 화면(기록·인테이크·검토·원본 보기·일정 등록)이 `GET /cases/:id/detail`을 한 번 더 부른다. 그 경로는 PII 열람을 감사에 남기지만 서버가 같은 사람·같은 사례·같은 행위를 **10분 창으로 접으므로**(`api/src/audit.ts` `FOLD_MINUTES`) 화면 수만큼 줄이 쌓이지 않는다. 실측: 다섯 화면을 이어 열어도 `case.detail` 1줄.
- 스크롤을 따라오지 않는다(`sticky`는 셸과 기록 레일의 것이다). 페이지의 첫 블록이라는 뜻의 상단 고정이다.

**탭은 카드 아래에서 이 사람의 화면을 가른다.** 네 개이며 순서를 바꾸지 않는다: `당사자 정보` · `회차별 요약` · `회차별 원본 보기` · `목표`. 기본 탭은 `당사자 정보`다. **원본은 `회차별 원본 보기` 탭과 회차별 요약 머리의 원본 버튼 둘이 입구**이고 열리는 것은 **큰 팝업 두 열**이다(2026-09-18 Q E2·F5 — 드로어는 폐지).

**탭 모양은 토글 상자다**(2026-09-17 Q — CCC 2026-09-09 시안 `artifacts/2026-09-09-workflow-prototypes/prototype.css` 의 `.participant-tabs`를 가져왔다). 상자는 `--panel` 면에 `--gradient-brand` 1px 아웃라인이고 안쪽 여백은 8px, 라운드는 카드와 같은 12px이다. 칸은 같은 폭으로 나누며(1440: 각 260px, 767px 이하 2열) 고른 탭만 `--gradient-action`으로 채우고 글자는 `--on-action`·600이다. 밑줄 탭(`.wire-tab`)은 이 자리에서 쓰지 않는다.

**15초 다시보기는 폐지했다**(2026-09-17 Q). 화면·라우트(`#/cases/:id/briefing`)·버튼·탭을 모두 걷었다. 기록을 저장하면 그 사람의 당사자 정보로 돌아오고, **일정을 저장하면 상담 일정 보기로 돌아온다**(2026-09-17 Q — 잇달아 잡는 일이 많다). 확인할 과제·오늘 물어볼 것은 상담 기록하기의 레일이 계속 보여 준다(같은 `GET /cases/:id/briefing` 자료를 쓴다 — 서버 경로는 그대로다).

**화면 이름은 카드가 있는 화면에서 쓰지 않는다**(2026-09-17 Q ①ⓐ). `상담 기록하기`·`인테이크 작성하기` 같은 용도명은 제목 자리를 사람 이름에 내주고, 지금 어디인지는 사이드바의 켜진 메뉴와 `뒤로` 알약이 말한다. 화면마다 다른 것은 **정보 격자의 회차 칸 꼬리표**뿐이다: `2회차까지 기록`(당사자 정보) · `3회차`(기록) · `1회차 인테이크` · `3회차 확인`/`4회차 잡기`(일정 예약 — 예정 회차를 확인하러 왔는지 새로 잡는지로 갈린다) · `마지막 2회차`(종결) · `2회차`(검토).

당사자 카드를 쓰는 화면은 여섯이다: 당사자 정보 · 상담 기록하기(수정) · 인테이크 작성하기 · 일정 예약 · 상담 종결 · AI 검토. 행동 슬롯은 **당사자 정보에서 셋**(`상담 기록하기`·`상담 일정 등록`·`상담 일정 보기`), 나머지 다섯에서 `당사자 정보` 하나다(2026-09-18 — 일정 예약의 구 `상담 기록하기` 행동은 저장 바가 그 일을 하므로 걷었다). 구 `상담 내용 원본 보기` 화면(`/full`)과 원본 드로어는 팝업으로 대체해 폐지했다.

### 탭별 성격 (2026-09-17 Q — 15초 다시보기 폐지 후 재정의)

| 탭 | 무엇을 보는 자리인가 | 담는 것 |
|---|---|---|
| `당사자 정보` | 이 사람에 대해 **바뀌지 않는 것** | 회차 정보(현황, 풀폭) · 동의 · 동의 링크 · 파일 업로드 · 상담 종결 입구 |
| `회차별 요약` | 이 사례의 **회차 기록과 지금 상태** | 회차 접힘 카드(구역 아코디언 카드 1열 — 위험 신호 · v6 요약 4구역 · 기록 상태, #96) · 머리 행동 버튼 없음 |
| `회차별 원본 보기` | **원본 그대로** | 회차 목록 + `원본 보기`(큰 팝업 두 열) |
| `목표` | 목표가 **어떻게 변해 왔나** | 카드 하나: 전체 상담 목표 · 다음 상담 목표(복수) · 지난 목표 보기 |

**위험 신호는 회차 카드가 스스로 말한다**(2026-09-18). 구 상단 배너(폐지한 15초 다시보기가 갖고 있던 자리)는 걷었다 — 신호가 나온 회차 카드의 머리 배지·`is-crisis` 테두리와 펼친 `위험 신호` 구역이 그 일을 한다. 아래 회차별 요약 절을 따른다.

**폐지한 화면의 나머지 조각이 간 곳**: 전체·오늘 상담 목표 → `목표` 탭 · 직전 회차 요약 → `회차별 요약`의 AI 정리 접힘 카드 · 다음 상담 일시 → 당사자 카드의 `다음 상담` · 확인할 과제·오늘 물어볼 것 → 상담 기록하기의 레일(결과를 고르는 자리). `지난 회차 미확인` 꼬리표는 위험 신호 항목에만 남았다.

### 회차별 요약 (2026-09-17 Q · 2026-09-18 Q F)

- 상단 **`위험 신호` 배너는 폐지**했다. 본문 중심으로 가고, 위험 신호는 그 신호가 나온 **회차 카드**가 스스로 말한다: 머리에 `위험 신호 N` 배지, 카드 테두리·면은 이식 `is-crisis`(risk 선 + 틴트), 펼치면 `위험 신호` 구역에 신호 문장과 AI 판정 상태(`AI 확인 안 함` 등)가 선다.
- **한 회차가 한 접힘 카드**다. 회차 안에 접힘 카드를 또 두지 않는다(구 `AI 요약`·`내용 불일치` 이중 접힘 폐지). 카드들은 같은 `group`이라 한 번에 하나만 펼쳐진다.
- 회차 줄 머리 타이포: **`N회차`(16/600)** + 날짜 + 종류(`인테이크`)를 13/400 `--sub`로, **세로선 없이 여백(8/16)** 으로 가른다. 그 뒤에 배지가 선다. 767px 이하에서는 머리가 두 줄로 풀린다(회차·날짜·배지 / 행동·꺽쇠).
- **정렬은 최신순**이다. 회차 정보 표와 같은 순서다.
- 펼친 본문은 **테두리 없는 텍스트 구역 네 개**다(2026-09-18 Q — 팀 목업 넷의 구성). 1440에서 **2×2**, 767 이하는 한 열이다(`.seq-sections[data-cols="2"]`).

  | 구역 | 제목 배지 면 | 자료 |
  |---|---|---|
  | `이번 상담의 핵심` | 라벤더(AI 산출) | v6 승인 분석의 핵심 항목(`summary_override` 가 있으면 그 텍스트 + `사람이 고침` 읽기 전용 배지). v6 없이 구버전 승인 요약만 있으면 그 텍스트 + `구버전 정리` 배지. 둘 다 없으면 `AI 정리 없음` 한 줄 |
  | `이번 회차에서 확인된 변화` | 민트 `--badge-mint` | v6 승인 분석만. 하위 소제목 셋 `약속 이행 여부` · `상담 중 새로 드러난 것` · `이번 상담 후 새로운 가능성`, ①②③ 은 내용 있는 것만 세어 렌더 시 연속 부여 |
  | `확인필요` | 코랄 `--badge-coral`(주목·경고) | v6 승인 분석만 — 브리핑(`GET /cases/:id/briefing`)의 과제·질문 분할 채움은 폐지했다 |
  | `완료·해결` | 블루 `--badge-blue`(구 중립을 바꾼다 — 상태 띠와 같은 축) | v6 승인 분석만 |
  | `위험 신호` · `기록 상태` | `--risk` · 블루 `--badge-blue` | 둘은 **한 줄을 다 쓴다**(`.is-wide`) — 하나는 경고, 하나는 꼬리라 두 칸으로 가르지 않는다. 운영 정보로 그대로다 |

- **구역 제목은 배지다**(F2). 정해진 면(`--badge-*`) + **어두운 글씨 `--on-action`** 이다 — 배지 면은 두 테마 모두 밝아 항상 어두운 잉크가 읽힌다. **줄글 여러 개는 불렛**(`.seq-list`)으로 푼다 — 문단 세 개가 이어 붙으면 어디가 끊기는지 읽히지 않았다.
- **한글 텍스트 여백은 표준 한 벌**이다(F3): 행간 1.6, 문단 사이 `0.75em`, `word-break: keep-all`.
- **다크모드 색은 토큰이 정한다**(F1). 펼친 회차 머리의 회차 번호·날짜(`.seq-head-no`·`.seq-head-meta`)는 그 파스텔 면 위에서 `--on-action` 으로 뒤집는다 — 다크에서 `--ink`(밝음)·`--sub`(회색)가 파스텔 위에 얹혀 안 보였다. 테마별 리터럴 색은 어디에도 두지 않는다.
- 목업의 이모지 제목(🔵🟠🟢)은 쓰지 않는다. 구역 제목 아래 설명은 **본문을 되풀이하지 않고 짧은 한 줄**만 둔다.
- 이름: `AI 요약` → **`이번 상담의 핵심`**(+ `AI` 배지), `확인된 변화` → **`이번 회차에서 확인된 변화`**(2026-09-18 Q, v6 — 구 `내용 불일치` 대조표와 `FactChanges` 표는 요약에서 뺐다).
- **내용 없는 구역·하위 항목은 그리지 않는다**(2026-09-18 Q, v6 — `달라진 사실 없음`·`확인할 것 없음`·`완료된 것 없음` 문구 폐지). 카드 하단에 **키워드 칩**(`Badge`)이 선다 — 누르면 원본 팝업의 백링크 패널로 이어진다.
- **머리 행동 버튼은 없다**(2026-09-18 Q, 당사자 정보 개편 #96 — 구 `AI 정리 보기`·`수기 원본 보기`·`녹음 전사 기록 보기`·`수정` 넷과 요약문 편집을 걷었다; v6 도 따른다). 원본 팝업의 입구는 `회차별 원본` 탭의 회차 행, 요약 카드의 **키워드 칩**(백링크 패널), 문장의 **근거 링크**다. AI 정리 검토·다시 하기는 `#/cases/:id/sessions/:id/review` 다. 승인 뒤 요약을 고치는 자리는 없다 — `summary_override` 가 있으면 `사람이 고침` 으로 읽기만 한다.
- **근거 링크**(2026-09-18 Q "원래 계획했던 하이라이터", #100 → v6): 변화·확인필요·완료·위험 신호의 문장은 텍스트 링크(민트 밑줄)이고, 누르면 `근거` 모달(머리 + 본문)에 그 회차 수기 원문이 서며 v6 분석이 있으면 **span 좌표 그대로** `<mark>`(민트 tint)를 친다. span 이 없는 항목(구버전·위험 신호)만 낱말 겹침으로 표시한다.
- 설명형 카드 hint(`회차 줄은 기록 상태예요…`, `회차를 고르면…`)는 걷었다(§13).

### 목표 탭 (2026-09-18 Q G)

- **카드 하나**다(G1 — 구 카드 셋과 카드마다 따로 선 `저장` 버튼 셋을 걷었다). 전체 상담 목표와 다음 상담 목표는 **컬러 라벨**로 가른다.
- **전체 상담 목표 입력칸은 2px 그라데이션 아웃라인**이고 세로로 조금 길다(G2, `rows=3`). 달력 카드 외곽선(B2)과 같은 토큰을 쓴다 — 이 화면에서 가장 무거운 값이라 강조가 붙는다.
- **다음 상담 목표는 복수**다(Q 결정 D5·G3). 줄 단위로 `+`/`−`로 늘리고 줄이며, 서버는 기존 컬럼에 `\n`으로 이어 보관한다(`PATCH /cases/:id/next-goals { lines }` — 스키마 불변). **전체 상담 목표는 하나**다.
- `+`/`−` 세트는 입력칸 행의 **위쪽에 맞춘다**(G3, `.next-goal-row .number-stepper` — §3 AC-G3가 top을 실측한다). 어휘는 당사자 등록의 숫자 스테퍼를 다시 쓴다.
- 수정 이력은 기존 `goal_revisions`가 그대로 갖고 `지난 목표 보기` 모달이 읽는다(G4).
- 설명형 hint 둘은 걷었다(§13).

### 회차 원본 팝업 (2026-09-18 Q E2·F5 — 구 드로어 대체)

- 회차는 **아코디언**, 원본은 **큰 팝업 두 열**이다. 입구는 `회차별 원본 보기` 탭의 `원본 보기` 하나와 회차별 요약 머리의 원본 버튼 둘이다(F4 — 누른 쪽 열에 초점이 간다). 전용 화면(`/full`)과 **오른쪽 드로어는 폐지**했다.
- 왜 팝업으로 돌아갔나: 드로어 720px 에는 수기와 전사를 **나란히** 놓을 수 없어 안에서 전환해야 했다(2026-09-18 Q). 두 원본을 대조하는 것이 이 화면의 일이라 폭을 택했다 — 팀 목업 넷도 1,024~1,040px 모달이다.
- 부품은 `dialog.tsx` 의 `Dialog size="wide"`(폭 `min(1200px, 96vw)`)에 제어형 `open` 을 얹은 것이고, 내용은 `web/src/session-original.tsx` 한 벌이다(`SessionOriginalDialog`). 네이티브 `<dialog>` 라 Escape·백드롭으로 닫히고 초점이 안에 갇힌다.
- **두 열은 늘 같은 폭**이다(`.original-dialog .original-col` — §3 AC-E2 가 실측한다). 한쪽이 비어도 칸을 없애지 않고 `없음`으로 말한다 — 없는 발화·시각·화자를 화면이 만들어 채우지 않는다.
- 왼쪽 `수기 기록`은 **기록지 그대로**다(`RecordScreen embedded`, #96·#97 — 첫상담 회차는 잠근 인테이크). 승인된 v6 분석이 있으면 그 위에 **`원문 그대로 | 구조화` 토글**이 선다(기본 원문, 첫상담 회차는 토글 없음). `구조화`는 **접힘 없는 목차형**이다 — 큰 주제 제목 → 세부 단락 제목 → 원문 문장을 전부 펼치고, 문장 왼쪽에 상태 띠, 위에 키워드 칩이 붙는다. 오른쪽 `녹음 전사 기록`은 녹음 항목·오디오·접힌 `전사문(확인됨)`·맥락 덩어리(#96)이고, 승인 전사에 v6 분석이 있으면 덩어리 자리에 **문장 행**(상태 띠 상속·불일치 배경·연결 표시)이 선다. `자동 전사라 틀린 곳 있음` 한 줄, 녹음이 여럿이면 `전사 없는 녹음 N건`.
- **수정은 편집 모드 + 리비전 로그**다(플래너 판단 D3). `수정` 을 누르면 원문 칸이 열리고 저장하면 **새 리비전이 붙는다**(`POST /sessions/:id/revisions`, append-only — 지울 수 없다). `수정 기록 N` 이 팝업 아래에 최신순으로 쌓인다.
- 원본을 고친 회차는 파생물에 **`원본 수정됨, 재정리 필요`** 배지가 붙는다(서버 `stale.ai_summary`·`stale.mismatch`). **자동 재처리는 하지 않는다**(비용·동의 게이트) — `AI 정리 다시 하기`가 사람의 손에 남는다.
- 팝업 안에 접힘 카드를 두지 않는다. 목업의 3단 중첩 아코디언(`01 → 1-1 → 원문`)은 채택하지 않았다 — `구조화` 뷰도 같은 이유로 접힘 없이 전부 펼친다.
- **전사 열은 문장 단위다**(2026-09-18 Q, v6). 상태 띠(01 상속)와 불일치 배경(`--discrepancy-tint`)을 얹고, 문장을 누르면 우측(767px 이하에서는 하단) **대조 패널**이 4항목 `차이점 / 녹음 내용 / 수기 내용 / 연결된 수기 단락` 만 갱신한다. 불일치가 아닌 일반 연결 문장은 차이점 없이 연결된 수기 단락으로 이동·강조한다. **`기록 불일치 모아보기`** 토글은 판정 문장만 남기고 해제 시 전문을 복원한다. 키워드 칩을 누르면 같은 패널 자리에 **백링크 목록**(같은 사례 전 회차의 해당 문장)이 서고, 항목을 누르면 그 회차 팝업의 단락으로 이동한다. 키보드로 문장을 고를 수 있고 Escape 로 닫히며 닫힐 때 초점이 돌아간다. 전사가 없는 녹음은 `전사 없는 녹음 N건` 으로 결손을 말한다.

### 당사자 정보 탭 (2026-09-17 Q · 2026-09-18 Q E)

- 구 `기본 정보` 카드는 걷었다 — 이름·연락처·이메일·가명·사업이 당사자 카드(HERO)와 같은 값이었다. 그 자리에 **회차 정보**가 선다.
- **회차 정보는 가로 풀폭 접힘 카드**다(2026-09-18 Q E1). 구 2열(오른쪽 `첫상담 기록` 카드)은 걷었다 — 인테이크 입구는 `회차별 원본 보기` 탭의 회차 줄이 이미 갖는다. 접힌 머리 한 줄이 현황을 말하고(`인테이크 작성함 · 2 / 6회차 · 수기 2 · 전사 승인 0 · 다음 N회차 …`), 펼치면 **회차 × 상태 표**가 **최신순**으로 선다: 회차 · 일시(시:분) · **소요 분** · 상태 · 수기 · 녹음 · 전사(E4 — 소요 분은 `sessions.duration_min`이다). 전사 6값은 `전사 없음·전사 중·초안·승인·실패·건너뜀`이다. 767px 이하에서는 표가 카드 안에서만 가로로 넘어간다.
- 회차 부제(`6/6회차` 등)는 **얇은 Meta 톤에 숫자만 강조**한다(E3). 안내문 크기를 넘기지 않는다.
- 동의 카드 제목은 **`개인 정보 및 민감 정보 처리 동의`**이고 카드 머리 설명은 **한 줄 명사구**다(E5 — 본문 설명을 걷었다).
- **동의는 항목마다 접힘 카드 하나**다. 이중 접힘(항목 + `자세히 보기`)은 걷었다. 접힌 머리 한 행은 `항목 이름 · 설명 · 날짜 · 동의 여부`이고, 펼치면 동의문 전문이 바로 선다. 문안을 화면에 복사해 두지 않는다.
- **동의문 행 라벨은 PIPC 동의 안내서 서식 열 이름으로 표준화했다**(E6). 공용 `ConsentDetail`(`ui.tsx`) 한 곳이 갖는다: `동의 내용` · `동의 항목` · `동의 목적` · `보유·이용 기간` · `제공받는 자` · `동의 거부권과 불이익`. 구 `동의문`·`무엇을 받나`·`왜 받나`·`얼마나 두나`·`어디로 가나`·`거부할 수 있나`는 되살리지 않는다(GLOSSARY).
- 체크 라벨은 **`동의`**다(E7 — 구 `이 항목에 동의함`). 동의·철회는 펼친 본문 아래 왼쪽 정렬 체크 하나이고, 켜면 `grant`, 끌 때는 이력을 따라 `withdraw`/`decline`으로 갈린다.
- **`문안 판` 행은 당사자 화면에서 숨긴다**(E8·Q 결정 D9). 판·지문은 `설정 › 동의서 관리`에서만 보인다(내규 번호 역할).
- 카드 순서는 **동의 → 동의 링크 → 파일 업로드**다(E9 — 동의 링크 카드를 파일 업로드 위로 올렸다).
- `서면 문서` → **`파일 업로드`**. 한 행에 `[파일 선택 | 문서 이름]` 입력칸과 `업로드` 버튼을 세우고, 안내 `보존기간 30일, 20MB 제한`은 입력칸 바로 아래 8 여백으로 붙인다. 칸 안의 `파일 선택`만 라운드 박스(6px)다.
- `상담 종결` 카드는 이 탭에 남고 **한 행**이다: 제목, 짧은 메시지(`미완료 N건, 상담 종결시 기록 작성 필요`), 버튼. 종결된 사례는 값만 말하고 버튼을 두지 않는다. 버튼을 누르면 **확인 대화상자**(`Confirm`)가 미완료 과제 수·기록 부족·앞으로 잡힌 예정 회차를 한 줄씩 세어 보여 주고, 확인하면 **기록 화면(`#/cases/:id/record?closing=1`)** 으로 간다 — 종결도 상담이라 기록이 먼저다.
- `당사자 열람` → **`개인정보 및 민감정보 처리 동의 링크`**(2026-09-17 Q). 이 카드가 하는 일은 **동의 받기**다. 버튼은 `링크 만들기`(살아 있으면 `새로 만들기`)이고 카드 제목과 같은 행 오른쪽 끝이다. 만든 직후에만 `코드는 지금 한 번만 표시`와 링크·확인 코드가 서며, 살아 있는 동안 `링크 끊기`가 그 아래 남는다. 서버는 폐기 표시(`revoked_at`)만 하므로 `삭제`라고 적지 않는다.
- 이 카드는 **당사자 등록과 같은 부품**이다(`web/src/consent-link.tsx`의 `ConsentLinkCard`).
- 동의문 **문안의 개정은 설정 › 동의서 관리**가 맡는다(Q 결정 D6 — 관리자 수정 허용, 저장하면 판이 오른다). 구 "문안은 해시에 묶여 손대지 않는다"는 이 결정으로 대체됐다.
- **HERO 행동은 셋**이다: `상담 기록하기`(`#/cases/:id/schedule?then=record` — Q 결정 D1) · `상담 일정 등록`(`#/cases/:id/schedule`) · `상담 일정 보기`(`#/schedule?case=<id>`). 가운데가 **기록이 있는 사례의 다음 일정 입구**다 — 사이드바에서 `상담 일정 등록`을 내린 뒤(A1) 순수 일정 등록의 입구가 비어 L3가 여기 세웠다.

### 당사자 목록 (2026-09-18 Q A2·A3·A4)

CCC의 `apps/web/app/components/wire/participant-card.tsx`와 `participants/page.tsx`에서 이름 중심 카드 구조를 가져왔다. 공통 이름 중심 원칙을 따르며 목록의 이름은 16px·600이다.

**목록은 1열이다**(A4 — 구 2열 대체). 카드가 접힘 카드가 되면서 요약이 한 줄에 다 서고, 펼친 본문이 옆 카드와 어긋나지 않는다. 격자는 `.participant-list`(간격 16)다.

**카드는 접힘 카드**다(A2). 골격은 상담 일정 보기의 `Fold`와 같다(`surface-card wire-card wire-card-details` + `<details name>` 배타 묶음) — 목록에서 손보는 것은 머리 한 줄의 자리뿐이다. 카드 이름은 `.participant-card`이고, 이식 `wire.css`의 `.participant-card{display:grid;gap:10}`는 **격자 카드를 상정한 규칙**이라 접힘 카드에서 머리와 본문 사이에 10을 더해 카드 여백 실측을 흔들었다 — `.participant-list > .participant-card`에서만 흐름 배치로 되돌린다.

- **접힌 머리는 한 행**이다: `이름(16/600) · 상태 · 12px --sub 요약(당사자 ID · 사업명 N회차 · 다음 상담)` + 행동 둘 + 꺽쇠. 왼쪽 덩이는 `.fold-head`가 감싸고 오른쪽 행동은 `.wire-item-action`이 감싸 **오른쪽 끝**에 세운다(§3 AC-A2가 두 덩이의 세로 가운데를 실측한다).
- **상태는 배지가 아니라 컬러 텍스트**다(A3). 이름 뒤에 12/600으로 붙는다: `진행 중` 민트(`--mint-deep`) · `종결` `--sub` · `배정 필요` 코랄(`--discrepancy`, §6이 정한 주목색 하나). 배지 면은 목록에서 줄을 밀었다.
- **행동은 둘**이다: `당사자 정보`(`#/cases/:id/info`) · `상담 기록하기`(`#/cases/:id/schedule?then=record` — Q 결정 D1, 기록은 늘 일정 예약을 지난다). 누를 때 카드가 펼쳐지지 않게 `stopPropagation`을 건다. **배정이 없는 사례에는 행동을 그리지 않는다** — 열 수 없는 화면을 가리키는 버튼은 고장으로 읽힌다.
- **펼친 본문은 연락처·이메일·담당 실무자**다(라벨/값 격자 `participant-card-fields`). 머리가 말한 것은 되풀이하지 않는다.
- **연락처·이메일은 카드를 펼칠 때 그 사례만** `GET /cases/:id/detail`로 부른다(A4). 구 규칙은 보이는 열 건을 미리 불러 목록을 여는 것만으로 감사 줄을 열 개씩 남겼다. 한 번 부른 사례는 다시 부르지 않고, 실패도 기억해 여닫기가 요청을 쌓지 않는다.
- 권한이 없어 비워 온 회차·일정은 `기록 없음`·`예정 없음`으로 바꾸지 않고 **말하지 않는다**.

**현황판은 걸개 아래, 목록 위 한 줄**이다(2026-09-17 Q): `보이는 사람` · `전체` · `진행 중` · `배정 필요` · `담당 실무자`. 라벨 12px `--sub` + 값 18/600이며 **종결은 세지 않는다**. `보이는 사람`은 걸개·찾기를 거친 수다.

**한 쪽은 열 장이다**(2026-09-17 Q). 목록 아래 가운데에 꺽쇠 둘과 `N / M 쪽`을 둔다. 찾기·걸개·정렬을 바꾸면 첫 쪽으로 돌아간다.

**업무 바 한 줄**(2026-09-17 Q): 왼쪽에 검색칸(보이는 라벨 없이 `aria-label`만, 340px), 오른쪽에 걸개·정렬 선택창 넷. 걸개는 상태(`상태 전체`·`배정 필요`·`진행 중`·`종결`) · 사업명 · 담당 실무자(`미배정` 포함)이고 정렬은 `가나다순`(기본) · `다음 상담 이른 순` · `다음 상담 늦은 순`이다. 사업·실무자 선택창의 값은 목록이 만들며 없는 값을 고르게 두지 않는다. 다음 상담이 없는 사람은 날짜 정렬에서 뒤로 보낸다. 선택창은 고정 폭(150px, 사업명만 210px)이며 767px 이하에서는 두 칸씩 선다 — 폭을 안 박으면 `wire-input-box`가 남은 자리를 다 먹어 넷이 한 줄씩 쌓였다(실측 587px).

**고르기 화면은 없다**(A1). `#/pick/record`·`#/pick/schedule`와 화면 제목을 메뉴 이름으로 맞추던 규칙은 걷었다 — 사례를 고르는 자리는 당사자 목록 하나이고, 무엇을 하러 왔는지는 카드의 행동이 말한다. 카드 전체를 감싼 링크도 없어졌다(행동 둘이 그 일을 한다).

### 당사자 등록 (2026-09-18 Q)

- 제목 아래 설명 줄과 카드 hint(`이름·연락처·이메일은 금고에…`, `한 상담은 사업 하나만…`, `설정 › 기관 정보 관리에서…`, `선택, 예: 6`)는 걷었다(§13). 사업이 하나도 없을 때의 안내만 상태로 남긴다.
- **동의문 본문은 당사자 정보 탭과 같은 부품**이다(2026-09-18 Q H1 — `ui.tsx` 의 `ConsentDetail`). 라벨 여섯(`동의 내용`·`동의 항목`·`동의 목적`·`보유·이용 기간`·`제공받는 자`·`동의 거부권과 불이익`)을 두 화면이 한 곳에서 받는다. 접힘 카드 **사이 간격은 8**이다(§3 AC-H1 이 실측한다 — 실측 8.0 × 6).
- **동의는 항목마다 상위 접힘 카드 하나**다. 묶음 카드(`동의`)와 이중 접힘(`자세히 보기`)을 걷었다. 접힌 머리는 `체크 + 항목 이름(필수는 별표) + 문안 한 줄`이고, 펼치면 문안 전체가 라벨/값으로 선다. 카드 높이 1,476 → 문서 2,728 → 1,824px(1440 실측).
- **체크는 접힌 머리에 둔다** — 등록에서 할 일이 동의를 받는 것이고 문안은 필요할 때만 펼친다. 이미 받은 동의를 읽는 자리(당사자 정보 탭)는 반대로 체크가 본문 아래다. 체크를 눌러도 카드가 접히거나 펼쳐지지 않는다(`stopPropagation`).
- **필수 표시는 별표**다(2026-09-18 Q — 배지 금지와 같은 결). `Field`의 `필수` 배지는 부품을 건드리지 않고 CSS로 껍데기를 벗겨 글자는 접근성용으로 숨기고 risk 색 `*`만 남긴다(`.wire-required-marker`). 접힘 카드 제목 같은 `Field` 밖 자리는 `.wire-required-mark`를 쓴다.
- 만드는 화면이라 버튼은 **`등록`**이다(`저장`은 이미 있는 것을 고칠 때 쓴다). 화면 아래 버튼은 `등록하고 인테이크 쓰기` 하나다.
- **짧은 값은 한 행에 나란히 둔다**(2026-09-18 Q — "공간 낭비가 심하다"). 당사자 카드는 `이름 · 연락처 · 이메일` 3열, 참여 사업 카드는 `사업명 · 예정 회차 수` 2열이다. 격자는 `.form-row[data-cols]`이고 767px 이하에서 한 열로 접힌다(390 실측: 293px 한 열).
- 라벨은 `사업` 대신 **`사업명`**이고, 선택창의 빈 첫 항목은 `고르기` 대신 **`선택`**이다.
- 예정 회차 수에는 **위아래 꺽쇠 스테퍼**(`.number-stepper`)를 입력칸 안 오른쪽에 둔다. 직접 쳐도 되고 1~99 사이로만 오르내린다. 이식 `wire.css:997`이 `.wire-input-box .wire-chevron`을 절대 배치로 오른쪽에 못박기 때문에(선택창 꺽쇠 하나만 있다고 본 규칙) 스테퍼 안에서만 `position:static`으로 되돌린다 — 안 되돌리면 위·아래 꺽쇠가 같은 좌표에 겹쳐 별표처럼 보인다(실측).
- **동의 요청 링크 카드는 동의 항목 위**다. 당사자 정보 탭과 **같은 부품**(`consent-link.tsx`의 `ConsentLinkCard`)이며 제목은 `개인정보 및 민감정보 처리 동의 링크`, 버튼은 아직 없으면 `동의 요청 링크 만들기`·이미 있으면 `새로 만들기`다. 누르면 이 화면에서는 먼저 등록하고 곧바로 발급한다(사례가 없으면 링크를 만들 수 없다). 발급 뒤 링크·확인 코드·안내가 카드 안에 서고 `인테이크 쓰기`로 이어 간다 — 코드는 발급 응답에만 있어 화면을 떠나면 다시 못 본다.
- 펼친 동의 머리는 파스텔 그라데이션 면이라 **거기 얹히는 글자는 `--on-action`**이다. 이식 CSS는 `.wire-card-title`만 뒤집어서 스스로 색을 선언한 체크 라벨(`--ink`)이 다크에서 흰 글씨로 남아 안 보였다(실측). 이 반전은 **파스텔 면을 쓰는 머리에만** 건다 — 회차 카드 머리는 배경이 투명해서(실측 `background: none`) 같이 뒤집으면 어두운 글자가 어두운 면에 얹힌다.
- 접힌 머리의 체크는 가운데 맞춘다. 이식 `.wire-choice`는 여러 줄 라벨 기준 `align-items: flex-start`라 한 줄 머리에서 3px 올라붙었다(실측).

### 상담 일정 보기 (2026-09-17 Q)

- HERO는 `상담 일정`이며 사이드바의 `상담 일정 보기` 이름과 `#/schedule` 경로는 유지한다. 가장 왼쪽에 월간·주간·일간 선택창을 두며 기본값은 월간이다.
- 선택창 바로 옆의 이전·다음 꺽쇠는 세 보기 공통이다(월간은 월, 주간은 주, 일간은 하루씩). 꺽쇠 사이 기간 이름은 세 보기 모두 글자 버튼이며 누르면 공통 A안 `DatePicker` 달력 모달(`inline` 트리거)이 열리고, 고른 날짜가 든 기간으로 이동한다. `오늘` 이동은 유지한다. 툴바 컨트롤(선택창·꺽쇠·오늘)은 높이 30px이고 서로 20px 간격을 둔다. 기간 이름은 그라데이션 없이 글자만 둔다.
- 월간은 월요일~일요일 순서의 7열 달력이다. 요일 헤더는 `--gradient-action` 배경이고, 요일과 날짜 숫자는 `--text-detail`(13px)로 왼쪽 정렬하며(요일·셀 안쪽 여백 12px) 세로 구분선을 헤더까지 잇는다. **내부 선은 한 톤(`--line`)이다**(2026-09-18 Q B1) — 요일 머리만 진한 `--line-on-action`을 쓰던 자리를 걷었다. 격자가 두 겹으로 읽혔다. 일정 줄 글자는 잉크가 상자 중심보다 위에 서서 `translateY(0.1em)`으로 시각 가운데를 맞춘다(실측 1.38px/13px). 셀의 일정은 상자 없이 `시각 이름` 13px 한 줄로 쌓고(시각은 `AM 07:14` 꼴, `calendar.ts` `calendarTime`)(최대 3건 + `더 보기`), 줄에 마우스를 올리면 `--gradient-hover`다. 좌측 굵은 색 선은 어디에도 두지 않는다(2026-09-17 Q).
- **달력 카드의 외곽선은 2px 그라데이션 아웃라인**이다(2026-09-18 Q B2). 오늘 원과 같은 `--gradient-action` 토큰을 쓰고(`linear-gradient(--panel,--panel) padding-box, var(--gradient-action) border-box`) **카드 자체**에 건다 — 표 바깥에 선을 하나 더 두면 카드 테두리와 두 겹이 된다.
- 주간·일간은 시간대별 표다. 시작 시각만 표시하며 같은 시간대의 일정도 겹쳐 숨기지 않는다. 시간칸의 일정도 상자 없이 13px `시각 이름` 줄이며 칸 안에서 위아래 가운데에 서고, 일정이 있는 칸은 **칸 전체**가 호버 면(`--gradient-hover`)과 클릭 대상이다(빈 자리를 눌러도 첫 일정으로 날짜 상세가 열린다). 조회한 기간에 일정이 있으면 첫 일정의 시간대로 표를 스크롤한다.
- 별도의 `시작 시각 기준 · 좁은 화면에서는 표를 좌우로 넘겨 볼 수 있어요.` 안내와 그 행, 일간 헤더의 `시작시간 기준` 글자는 두지 않는다. 일간 헤더는 시간·날짜를 한 행에 모아 그라데이션으로 채우고, 빈 시간칸은 36px로 줄인다. 주간의 빈 시간칸은 64px를 유지한다. 일간 표는 시간 행 단위로 스크롤이 멈춘다(`scroll-snap`, 헤더 높이만큼 `scroll-padding-top`).
- `오늘` 배지는 월간·주간·일간 어디에도 두지 않고 색으로만 구분한다. 월간의 오늘은 26px 원 안 정중앙의 숫자에 2px `--gradient-action` 테두리 원을 두르고(채움·색 숫자 없음 — 다크에서 색 숫자가 안 보였다, 2026-09-17 Q), 주간·일간의 오늘 열 헤더와 선택한 날짜는 기존 `--gradient-action`과 `--on-action`을 쓴다. 공통 달력 모달의 오늘/선택 상태는 그대로이며, 마우스를 올려도 그라데이션이 유지된다.
- 모바일 월간은 날짜와 건수를 중심으로 보여 준다. 주간 표는 표 안에서만 가로 스크롤하며 페이지 전체가 넓어지지 않는다.
- 주간은 1행(요일 머리)과 1열(시각 라벨)을 `--muted` 면으로 깐다 — 흰 면에 선만으로는 머리와 본문이 갈리지 않았다(2026-09-17 Q). 좌상단 모서리도 같은 면이고 그 칸의 `시간` 글자는 화면에서 걷고 접근 이름으로만 남긴다. 오늘 열 머리는 그대로 `--gradient-action`이다. 월간·일간 머리 면은 바꾸지 않는다.
- 제목 아래에 `표시 범위 N건 · 한국 시간 기준` 같은 설명 줄은 두지 않는다(2026-09-17 Q). 건수는 아래 카드의 hint가, 시간대는 값이 말한다.
- 달력 아래 카드의 기본은 **다가오는 일정**이다. 오늘(1시간 유예) 이후 가까운 순으로 다섯 건을 보여 주고, 넘치면 카드 맨 아래 `날짜 더보기`가 다섯 건씩 더 붙인다. hint는 `가까운 N건 · 모두 M건`이다. 달력에서 오늘이 아닌 날짜(또는 일정)를 고르면 같은 자리가 그 날짜 카드로 바뀌고 고른 일정의 줄이 펼쳐진다. 이 목록은 달력이 보는 기간과 별개로 오늘부터 180일을 따로 조회한다.
- 목록의 줄은 모두 **접힌 카드**(`Fold`)이며 같은 `group`을 써서 한 번에 하나만 펼쳐진다(`<details name>`). 카드 사이 간격은 8이다.
- 접힌 머리는 **한 행**이다(2026-09-17 Q): 이름 16/600 · 12px(`--text-badge`) `--sub` 요약 `당사자 ID · 사업명 N회차 · 일시` · 행동 둘 · 꺽쇠. 요약에는 **볼드를 쓰지 않는다** — 날짜·시간도 값이다. 모자라면 뒤에서 말줄임하고 전체는 `title`로 남긴다. 767px 이하에서는 이름 / 요약(두 줄까지 흐름) / 행동으로 풀어 놓는다 — 한 행에서는 행동 둘이 폭을 다 먹어 이름이 0px이 됐다(390 실측).
- 머리 오른쪽 끝의 행동은 `당사자 정보`(`#/cases/:id/info`)와 `상담 기록하기`(`#/cases/:id/record`) 둘이며 꺽쇠 앞에 선다. 둘 다 세컨더리 30px 알약이다 — 다섯 줄에 채운 버튼이 다섯 개면 강조가 아니라 소음이다. 누를 때 카드가 펼쳐지지 않게 호출부가 `stopPropagation`을 건다.
- 펼친 본문은 **가로선 없는 컬러 라벨 + 값**이다(`.wire-field-row[data-layout="stack"][data-size="sm"]` — 라벨 14/600 `--mint-deep`, 값 14/400). `DataRows`의 가로선 격자는 줄마다 칸을 키워서 걷었다(2026-09-17 Q). 항목은 연락처·이메일·방식·장소·메모·확인할 과제·물어볼 것이며 폭이 되면 세 열까지 앉는다. 머리가 말한 이름·아이디·사업명·회차·일시는 되풀이하지 않고, 없는 값은 칸을 만들지 않으며 배지도 쓰지 않는다 — 건수도 값이다.
- 연락처·이메일은 `GET /schedules`에 없다. **줄을 펼치는 순간** 그 사례만 `GET /cases/:id/detail`로 부른다(`Fold`의 `onOpen`) — 목록을 여는 것만으로 열지도 않은 사례의 금고를 열고 `case.detail` 감사 기록을 남기지 않는다. 불러오는 동안은 `불러오는 중`, 실패하면 연락처 칸에 `불러오지 못했어요`를 둔다.
- 카드 안의 접힘 카드는 `min-width: 0`이어야 한다 — grid 항목의 `min-width: auto`가 제목의 최소폭을 물려받아 390 화면이 473px로 늘어났다(2026-09-17 실측).
- `screens/home.css`가 조회 화면 스타일, `calendar.ts`가 한국 날짜와 기간 계산을 맡는다.

### 공통 날짜·시간 입력 — A안 (2026-09-17 Q 승인)

`prototype/calendar-input`의 세 비교안 중 A를 선택했다. 일정 등록, 상담 기록하기·수정, 인테이크 작성·수정에 모두 적용한다. 비교용 전환바와 저장 체험은 본 화면에 넣지 않는다.

- 날짜와 오전·오후 / 시 / 분을 나란히 두고, 좁은 카드나 767px 이하 화면에서는 날짜 아래에 시간 입력을 둔다. 각 화면의 구획과 저장 버튼은 유지한다.
- 날짜 버튼은 한국어 달력 대화상자를 연다. `선택 완료`는 입력값만 적용하며 `취소`와 Escape는 변경을 버리고 원래 버튼으로 초점을 돌린다. 대화상자 위의 `오늘`·이전·다음은 공용 알약 버튼(`오늘`은 프라이머리, 꺽쇠는 32px 원)이고, 날짜는 44px 원이다. 오늘은 그라데이션 면, 고른 날짜는 2px 그라데이션 테두리 원으로 표시하며 `오늘`·`선택` 배지는 두지 않는다(2026-09-17 Q).
- **시·분 선택창의 값은 숫자만**이다(2026-09-18 Q C2 — 구 `8시`·`34분`). 단위는 라벨이 말하고, 두 자리로 짧아진 선택창 덕에 한 행에 종료 시각까지 든다. 부품 아래의 `한국 시간 기준` 한 줄도 걷었다(§13 — 화면이 저장값을 한국 시간으로 되읽어 준다).
- 일정 예약의 `상담 내용` 카드에는 일시·종료 시각·상담 방식·상담 장소·메모·종결 상담 선택이 함께 든다(아래 일정 예약 절).
- 카드·입력칸·버튼은 기존 `ui.tsx` 부품을 사용한다. 공통 입력은 `date-time-input.tsx`·`date-picker.tsx`, 공통 스타일은 `date-time-input.css`, 한국 시간 변환은 `date-time.ts`가 맡는다. 일정 등록 카드·저장 바에만 필요한 스타일은 `screens/schedule-new.css`에 둔다.

### 일정 예약 (2026-09-18 Q C·D1)

경로 이름(`#/cases/:id/schedule`)과 사이드바에서 내린 메뉴 이름은 바꾸지 않았다. **기록은 늘 이 화면을 지난다**(Q 결정 D1) — 당사자 목록 카드의 `상담 기록하기`가 `?then=record`를 달고 여기로 오고, 일시를 확인한 다음 기록 화면으로 잇는다. 일시를 확인하지 않은 기록을 막는 자리다.

- **카드는 하나**다(C3 — 구 `상담 일시`·`상담 내용` 두 카드를 합쳤다). 제목은 `상담 내용`이고 안은 두 덩이다: 1행 `[날짜 · 오전·오후 · 시 · 분 · 종료 시각] [상담 방식]`, 그 아래 왼쪽 `상담 장소`·`종결 상담` 체크, 오른쪽 `메모`. 1행의 두 칸은 **세로 가운데**로 맞춘다(`.schedule-when-row`, `align-items: center`) — 라벨 있는 입력칸과 라벨 없는 라디오 묶음의 높이가 달라 위쪽 정렬이면 라디오가 날짜 라벨 줄에 붙었다.
- **상담 장소는 상시 노출**이다(C3). 대면이 아니면 서버가 거절하므로 보내지 않는다 — 화면에서 칸이 사라지지는 않는다.
- **종료 시각 칸**을 둔다(Q 결정 D4). 네이티브 `input[type=time]`이고, 시작 일시와 뺀 분을 `duration_min`으로 보낸다. 종료가 시작보다 이르면 **세지 않는다** — 날짜를 안 물었으므로 자정 넘김인지 오타인지 알 수 없고, 틀린 소요시간은 없는 것보다 나쁘다.
- 시·분 선택창은 **숫자만**이다(C2): `8` · `53`. 단위는 라벨(`시`·`분`)이 이미 말한다. `한국 시간 기준` 한 줄은 걷었다 — 저장 바가 고른 일시를 한국 시간으로 되읽어 준다.
- `필수` 배지를 두지 않는다(배지 금지 — 값으로 말한다). 입력칸의 `required`는 남기고 표시만 끈다: 일시가 비면 `일정 저장`이 이미 막혀 있다.
- **저장 바는 작은 안내 + 버튼**이다(C4). 왼쪽은 `Meta` 톤 한 줄(고른 일시, 없으면 `날짜와 시간 선택 필요`, 저장 중·실패만 덧붙인다)이고 버튼과 24 거리를 두며 **세로 가운데**로 선다.
- **예정 회차가 있으면 그 값이 채워진 채 시작한다**(D1). 일시·방식·장소·메모는 `GET /cases/:id`가 한 번에 준다. 이때 버튼은 `상담 기록하기`이고 **회차를 새로 만들지 않는다** — 같은 상담이 두 번 잡히지 않게.
- **예정 회차가 없으면 지금 일시를 기본값으로 두고 작은 안내 팝업**(`일시 확인 필요`)을 한 번 띄운다(C5). 확인하면 `일정 저장`이 그 일시로 회차를 만들고 기록 화면으로 잇는다.
- `?then=record` 없이 들어온 **순수 일정 등록은 프리필하지 않는다**. 빈 칸에서 시작하고 저장하면 `상담 일정 보기`(`#/schedule`)로 돌아온다 — 잇달아 잡는 일이 많다. 채워 두면 예정 회차와 같은 일시로 한 번 더 저장된다.
- HERO 행동은 `당사자 정보` 하나다. 구 `상담 기록하기`는 저장 바가 그 일을 하므로 걷었다.


### 상담 기록하기 (2026-09-18 Q D)

본문은 **5:5 두 열**이다(Q 결정 D2). 구획이나 저장 버튼을 다른 열로 옮기지 않는다.

```text
.wire-container.rail-grid.record-grid   ← 1fr 1fr
├── .record-main   (왼쪽) 오늘 상담 내용 ~ 음성·수기 기록 불일치
└── .record-side   (오른쪽, order 로 뒤로 보낸 붙박이 레일) 종결 카드 · 확인할 과제 · 오늘 물어볼 것
```

- **레일이 오른쪽 열**이다(D2·D3). 공용 `.rail-grid` 의 `300px / 가변 / 200px` 세 트랙은 이 화면에서 걷었다 — 릴레이어에 세 번째 목차 요소가 없어 1150px 이상에서 오른쪽 200px 트랙이 비어 있었다. 두 열일 때만 레일이 `sticky`(`top: calc(var(--header-height) + var(--space-6))`)이고, 컨테이너 880px 미만에서는 한 열로 풀려 레일이 본문 위에 선다. 열 사이 32px, 열 안 카드 24px.
- **종결 카드는 제목 없는 2행**이다(D2): `종결 상담` 체크 한 줄 + `체크 시 상담 기록 저장 후 상담 종결지 작성 화면으로 이동` 한 줄. 가로선을 두지 않는다. 체크 행의 위아래 패딩만 걷어 카드 사방 24 한 값을 지킨다.
- **확인할 과제·오늘 물어볼 것은 카드 안 카드를 두지 않는다**(D5). 항목은 `.record-open-item` 으로 쌓고 사이는 `border-top` 한 줄이다(레시피는 인테이크 조회 표 항목과 같다: 본문 gap 12 + 위 16). 항목이 **넷 이상**이면 묶음을 `Fold`(기본 펼침)로 접을 수 있게 하고 제목에 개수를 붙인다.
- 과제 사이 `결과 ----` 범례는 없다(D6). 라디오 셋만 두고 묶음 이름은 `role="radiogroup"` 의 접근 이름이 갖는다.
- **일시와 소요 시간이 한 행**이다(D7·Q 결정 D4): `.record-when-row` = `[날짜·오전오후·시·분] [소요 시간(분)]`. 값은 `sessions.duration_min` 이고 767px 이하에서는 한 열이다.
- **녹음 카드 머리는 한 행**이다(D1): 왼쪽 안내 세 줄(`파일 형식` · `파일 크기` · `주의`)이 라벨/값으로 서고 오른쪽에 `녹음 시작`·`파일 업로드` 가 같은 행 세로 가운데다(`.recording-head`·`.recording-note`). 라벨은 민트 deep, 값은 `--sub`. `주의` 문구는 **확정 근거가 있는 만큼만** 적는다 — iOS 는 화면이 잠기면 녹음이 멈추고, 다른 기기는 `멈출 수 있음`이다(2026-09-18 L2 실측·문헌).

### 설정 네 화면 (2026-09-18 Q I·J·K·L)

설정은 묶음 하나가 화면 하나다: `내 정보` · `실무자 관리` · `기관 정보 관리` · `시스템`. 묶음 안 항목을 또 고르게 하지 않는다(시스템만 예외 — 아래).

- **카드 머리 오른쪽 끝이 그 카드의 행동 자리**다(I2·K3·L4). `Card` 의 `action` 슬롯을 `.wire-card-action` 으로 감싸고, 이식 `.wire-card-head` 가 둘째 자식부터 `margin-left:auto` 로 오른쪽 끝에 세운다. 본문 맨 아래까지 내려가 `저장`을 찾을 일이 없다.
- **내 정보는 두 행**이다(I1). 읽는 값(아이디·역할·기관)이 한 행, 고치는 칸(이름·연락처·이메일)이 한 행이다. 읽는 값 행은 `.wire-data-row` 를 세로 쌓기로 바꿔 3열에 앉히고 줄 사이 선을 두지 않는다(`.me-facts`).
- `화면 테마` → **`테마`**다(I3). 제목 줄과 민트 라벨을 걷고 선택창 하나만 둔다(`밝게`·`어둡게`·`기기 설정`).
- **계정 삭제는 접힘 카드 한 행**이다(I4). 머리에 제목과 `삭제` 버튼이 서고, `나가기`를 적으라는 안내는 입력칸 placeholder 가 갖는다. 확인 칸을 따로 두지 않는다.
- **실무자 목록은 한 사람이 접힘 카드 하나**다(J1 — 가로선 표 폐지). 접힌 머리에 이름·역할·아이디·맡은 수가 한 줄로 서고, 펼치면 역할 바꾸기와 `담당 중인 당사자`가 있다. 뒤는 **팝업 표**(`.assignee-dialog` + `.data-table`: 이름·사업·회차·다음 상담)다.
- **배정 요청은 2열 카드**다(J2): `승인할 배정 요청` · `내 배정 요청`. 이식 `.card-grid` 의 `auto-fit` 은 1031px 에서 3열까지 열려 셋째 칸이 비므로 두 열로 못 박는다.
- **담당 실무자 배정은 한 행 목록**이다(J3): 이름 · 아이디·사업·회차·연락처·이메일 · 담당 · `실무자 배정`. 행 사이는 가로선이고(카드 안 카드 없음 — D5 와 같은 결) 찾기·사업 걸개·한 쪽 10건 쪽 넘기기가 붙는다. 버튼은 **드로어**를 열어 실무자를 `+`/`−` 로 여러 명 고르게 하고, 현재 담당에는 `담당 실무자` 배지를 둔다.
- **사업 목록은 접힘 머리 한 행**이다(K1): 이름 · 기간·당사자 수·설명 · `사업 종료`. `내리기`는 무엇이 내려가는지 읽히지 않아 걷었다. 사업 추가는 입력칸(360px)과 버튼이 한 행이다(K2, `.inline-action-row`).
- **시스템은 항목마다 한 행 카드**다(L1): 제목 · 설명(남는 폭, 말줄임) · `열기`. 묶음 카드 안에 상자 넷을 넣지 않는다. 항목은 자기 주소를 갖는다(`#/settings/connections`·`audit`·`consent`·`download`).
- 열람 기록의 검색·걸개는 다른 화면과 같은 업무 바(`.work-toolbar`)다(L2 — 구 `.log-filters` 폐지).
- **동의서 관리는 당사자 정보 동의 카드와 같은 구성**이다(L3·Q 결정 D6). 읽기는 공용 `ConsentDetail`, 관리자는 드로어에서 문안을 고친다. 저장 전에 경고 `Confirm` 이 서고(저장하면 모든 당사자의 동의가 `확인 필요`로 떨어진다), 저장하면 판이 자동으로 오른다. **판·지문은 여기에서만 보인다**(Q 결정 D9 — 당사자 화면에서는 숨긴다).
- **자료 다운로드의 경고는 카드 밖 아래 코랄 한 줄**이다(L4, `.wire-note[data-tone=warn]`). 버튼(`CSV 내려받기`)은 카드 머리 오른쪽이다.
- 전역으로 `저장하기` → **`저장`**이다(§6 명사구 — 설정 5곳).

### 필드와 반복 입력

- `.wire-form-field`는 `align-content:start`를 사용한다. 옆 칸의 도움말이 길어져도 입력 상자가 늘어나지 않게 한다.
- 라벨과 입력칸, 입력칸과 도움말의 격자 간격은 12px이다. 라벨 안 글자와 필수 배지는 8px이다. 문단 자체 마진이 더해지는 자리는 실제 렌더로 확인한다.
- `.wire-field-with-action[data-no-label="true"]`는 입력칸과 `추가` 버튼의 세로 중심을 맞춘다. 입력은 남는 폭을 쓰고 버튼은 내용 폭을 유지한다.
- 반복 입력은 카드 제목이 이름을 설명하므로 입력의 접근성 이름을 유지하고 별도 라벨 행을 덧붙이지 않는다. `추가`·`삭제`에 아이콘 전용 `.wire-repeat-actions`를 씌우지 않는다.
- `.wire-choice`는 label 전체가 클릭 영역이다. 높이는 최소 40px, 표시와 글자 사이는 6px, 보기 사이 열 간격은 24px이다. 체크·라디오 표시는 18px이며 선택지 행 자체를 알약으로 만들지 않는다.
- `.wire-data-rows`는 행 사이에만 선을 둔다. 767px 이하에서는 라벨과 값을 한 열로 쌓는다.

제목·본문의 왼쪽 시작선, 입력칸·버튼의 중심, 레일·본문의 윗선은 DOM 좌표로 확인한다. 맞는 요소에 보정값을 추가하지 않는다. 상자 크기와 패딩이 원인인지 먼저 확인한다.

## 5. 현재 재사용하는 부품

`web/src/ui.tsx`의 부품을 먼저 사용한다. CCC에 같은 이름의 더 큰 부품이 있어도 릴레이어의 공개 계약을 대신하지 않는다.

| 부품 | 클래스와 구조 | 언제 쓰는가 |
|---|---|---|
| `PageHeader` | `.page-header`, `h1.wire-page-title`, 선택적인 `.panel-meta`와 `.page-actions` | 화면 제목과 설명을 표시한다. 화면 하나짜리 행동(되돌이·이동 버튼)은 `actions`로 넘겨 **제목과 같은 행에서 세로 가운데·오른쪽 끝**에 세운다(2026-09-17 Q). 767px 이하에서는 이식 규칙이 제목 아래 오른쪽으로 내린다. |
| `ParticipantHero` | `header.page-header.surface-card.participant-hero-card`, `h1.participant-hero-title`, `.page-actions`, `.participant-hero-divider`, `.participant-hero-details` | 한 사람을 다루는 화면의 머리다. 이름이 제목이고 정보는 라벨/값이다 — 규칙은 §4 당사자 카드(HERO)와 탭. |
| `Card` | `section.surface-card.wire-card`, 선택적인 `h2.wire-card-title`와 `.wire-card-divider`, `.wire-card-body` | 정보나 폼을 하나로 묶는다. 제목 없는 카드에는 제목 구분선을 만들지 않는다. |
| `Button` | `.wire-button > .wire-button-text` | 현재 화면은 `primary`와 기본 `secondary`를 쓴다. 최소 높이는 32px이고 글자 폭에 좌우 14px 패딩과 테두리를 더한다. |
| `Field` | `.wire-form-field` 안 라벨·`.wire-input-box`·선택적 도움말 | 입력·선택·여러 줄 입력을 같은 위계로 표시한다. `htmlFor` 연결을 유지한다. |
| `Chevron` | 12px `.wire-chevron` SVG, 획 1.5px | 방향만 회전한다. `Field control="select"`가 자동으로 붙이며 네이티브 select의 의미는 유지한다. |
| `ChoiceGroup`, `Choice` | `fieldset.wire-fieldset`, `.wire-choice-group`, `label.wire-choice`, 네이티브 input | 단일·복수 선택을 표시한다. radio의 `name`과 label 연결을 유지한다. |
| `LineList` | 입력칸과 버튼 한 행, 추가한 값의 `.wire-repeat-card`와 `Item` | 수행할 과제·다음에 물어볼 것 등 반복 입력을 조립한다. 추가·저장 의미는 기존 동작을 바꾸지 않는다. |
| `Fold` | `details.surface-card.wire-card.wire-card-details`, `summary.wire-card-summary`, `.wire-card-body` | 접었다 펴는 카드다. 접힌 줄에 제목과 한 줄 설명(말줄임)이 서고, **펼친 제목 줄 아래에는 가로선을 두지 않는다**(2026-09-17 Q). 제목과 본문은 24px 여백으로 갈린다. **한 화면에서는 하나만 펼친다** — 아래 펼친 제목 줄 규칙을 본다. |
| `Item` | `.wire-item` 안 `.wire-row-text`(제목·설명)와 선택적 `.wire-item-action` | 한 항목의 제목·설명·행동을 표시한다. **행동이 있으면 글 왼쪽·행동 오른쪽으로 서고 세로 가운데를 맞춘다**(2026-09-17 Q). 글과 행동 사이는 12px, 제목과 설명 사이는 4px, 행동 안 버튼 사이는 8px이다. 767px 이하에서는 행동이 글 아래로 내려가 12px을 띄운다. |
| `Badge` | `.wire-badge > .wire-badge-label` | 상태·건수를 표시한다. 현재 React 계약은 기본 중립과 `mint`, `lavender`, `blue`다. |
| `DataRows` | `dl.wire-data-rows`, 행별 `dt`·`dd` | 이름과 값의 표를 표시한다. |
| `Empty` | `p.empty` | 불러오기나 빈 상태를 기존 문구와 함께 표시한다. |
| `FormActions` | `.wire-form-actions` | 폼의 저장·이동 버튼을 오른쪽에 모으고 사이를 12px 띄운다. |
| `ErrorText` | `p.wire-error[role="alert"]` | 오류를 글로 알린다. 색만으로 오류를 전달하지 않는다. |

기본 카드는 `--line` 1px 아웃라인과 `--panel` 면을 쓴다. 제목 아래 구분선은 1px이며 카드 패딩을 가로질러 양쪽 테두리에 닿고, 위아래 여백은 24px이다. 이 선은 **제목과 본문을 가진 카드**의 것이고, 접힘 카드(`Fold`)와 제목 없는 카드에는 두지 않는다. 반복 행은 `.wire-repeat-card`의 상하 16px·좌우 24px을 쓴다. 카드 안에 임의의 새 상자나 그림자를 추가하지 않는다. **고를 대상이 여럿이면 카드를 한 장 더 씌우지 않고 각자 카드로 세운다**(2026-09-17 Q — 설정 › 시스템의 구 `무엇을 볼까요` 묶음 카드 안 상자 넷이 그 예다. 상자가 두 겹이면 정작 누를 것이 안쪽 상자다).

**펼친 제목 줄의 면(2026-09-17 Q).** 펼친 `Fold` 의 제목 줄은 이식 CSS 의 활성 면(`--gradient-action`)을 받는다. 이 색은 프라이머리 버튼·켠 체크·고른 행과 같은 *행동·선택* 어휘이므로, **가리킬 대조군이 있을 때만** 뜻이 있다. 그래서 한 화면의 `Fold` 는 하나만 펼치고 나머지는 접어 둔다. 어느 것을 펼치는지는 화면이 정한다. 회차 원본은 2026-09-18 부터 접힘 카드가 아니라 **드로어**라 이 규칙 밖이다 — 무엇을 볼지는 머리의 버튼 둘이 정한다. 펼친 제목 줄 안의 조각은 모두 채운 면 전용 색을 받는다: 제목·설명 `--on-action`, 배지는 테두리를 벗고 `--panel` 면, 세로선 `--line-on-action`. 설명(`.fold-title-desc`)은 이식 규칙에서 빠져 있어 `app.css`가 메운다 — 다크에서 `--sub`가 파스텔 면 위에 올라가 대비 1.15:1이었다(2026-09-17 실측, 라이트도 4.34:1). `.is-crisis`는 이 규칙 밖이다(`--risk` 틴트 독점).

**둘 이상을 상시 펼쳐야 하는 화면이 생기면** 그 화면의 펼친 제목 줄은 활성 면 대신 `--muted` 회색 면 + `--ink` 글자를 쓴다(대비 라이트 10.84:1 · 다크 14.4:1). 지금 그런 화면은 없으므로 CSS도 만들지 않는다 — §10의 "선언만 있고 안 쓰는 CSS"를 다시 만들지 않는다.

**안 접히는 카드 제목에는 면을 쓰지 않는다**(2026-09-17 Q 검토). `Card` 의 제목은 글자와 1px 선만으로 본문과 갈린다. 면은 *누를 수 있는 것*(접힘 카드 머리·버튼·선택 행)의 어휘이고 선은 구조의 어휘다. 카드는 접히지 않으므로 알릴 상태가 없고, 한 화면에 카드가 3~8장 서는 이 앱에서 머리 띠가 반복되면 강조가 아니라 배경이 된다. 이는 CCC 2026-08-05·06의 "면 배경 폐지, 모두 라인으로 영역 구분" 결정과도 같은 방향이다.

**제목 위계 네 단(2026-09-17 Q).** 같은 자리에서 글자 크기가 갈리지 않게 역할별로 클래스를 고정한다.

| 역할 | 클래스 | 값 |
|---|---|---|
| 화면 이름 | `h1.wire-page-title` | 28px(767 이하 24) · 600 · `--ink` |
| 카드 이름 | `h2.wire-card-title` (`Card title`) | 16px · 600 · `--ink` |
| 카드 안 묶음 이름 | `h3.wire-subhead` | 14px · 600 · `--sub` |
| 값·상태·안내 | `.panel-meta`, `.wire-item-desc` | 14px · 400 · `--sub` |

입력의 이름은 `Field` 의 라벨(14px · 600 · `--sub`)이 갖는다. **라벨을 카드 이름 자리로 쓰지 않는다** — 당사자 목록의 `찾기`가 제목 없는 카드 안 라벨이어서 옆 카드들과 제목 크기가 달랐다. 지금은 `Card title="찾기"`이고 입력의 접근성 이름은 `aria-label`이 갖는다. 같은 이유로 `.panel-meta`를 구획 머리로 쓰지 않는다(원본 드로어의 `전사문`, 불일치 카드의 `숫자 항목 비교`·`회차간 기록 불일치`를 `wire-subhead`로 올렸다).

버튼의 프라이머리는 밝은 행동 그라데이션, 세컨더리는 기존 아웃라인을 사용한다. 타입에만 남은 `neutral`·`ghost`·`danger`를 현재 화면에서 사용하는 변형으로 설명하지 않는다. 선택창의 꺽쇠를 텍스트 기호로 다시 그리지 않는다. 버튼의 native `disabled`와 포커스 처리를 유지한다. 입력칸의 포커스 링은 내부 input과 바깥 상자에 중복하지 않고 `.wire-input-box:focus-within`에서 표시한다.

호버를 지원하는 기기에서는 활성 버튼의 그라데이션이 흐른다. 현재 주기는 `calc(var(--motion-flow-period) * 2)`이며, 기본 토큰 1600ms의 두 배인 3200ms다. 이징은 `--ease-standard: cubic-bezier(0.2,0,0,1)`이고, 누를 때는 버튼을 1px 내린다. 로딩·스크롤 진입 효과를 더하지 않는다. `prefers-reduced-motion:reduce`에서는 이식된 전역 규칙이 전환·애니메이션을 1ms로 줄이고 반복을 한 번으로 제한한다.

## 6. 화면의 말 (2026-09-18 Q 개정)

문서의 설명은 이유를 밝히는 평서문으로 쓴다. **화면의 문구는 문장이 아니라 단어·명사구다.** 라벨·제목·도움말·상태·오류 모두 서술어 어미(`…해요`, `…예요`, `…주세요`, `…합니다`)로 끝내지 않고 마침표를 찍지 않는다(구 `…해요` 체는 2026-09-18 폐지). 입력값·원문·동의 문안(`api/src/consent.ts`, 해시에 묶임)은 바꾸지 않는다.

- 화면 이름은 `인테이크 작성하기`, `상담 기록하기`, `당사자 정보`, `상담 일정 등록`, `상담 종결` 등 용어 정본을 그대로 쓴다(`상담 내용 원본 보기`는 2026-09-18, `15초 다시보기`는 2026-09-17 폐지). 기록·작성 화면의 동사형 이름을 명사형으로 통일하지 않는다.
- 행동 버튼은 실제 결과를 말한다. 현재의 `추가`, `삭제`, `저장`, `저장하고 상담 일정 잡기`, `등록하고 인테이크 쓰기`처럼 동작과 이동을 구분한다. 의미가 다른 버튼을 모두 `확인`으로 줄이지 않는다.
- 문구 패턴: 불러오는 중 `불러오는 중` · 없음 `[대상] 없음`(`예정된 상담 없음`) · 실패 `[동작] 실패`(`저장 실패`; 다음 행동은 쉼표로 `서버 연결 실패, 잠시 뒤 다시 시도`) · 입력 요구 `[대상] 필요`/`[대상] 선택` · 상태 `저장됨`, `녹음 중`, `이번 확인 안 함` · 결과 설명 `저장 시 상담 종결 화면으로 이동`. 두 절은 쉼표 하나로 잇고 세 절은 줄인다. 감정어·느낌표·물음표는 쓰지 않는다.
- 도움말은 §13대로 **예시**와 **지금 화면 상태**, 그리고 행동 전에 알아야 할 **저장 결과**만 남긴다. 규율·이유·설계 근거(`…때문`, `회차가 아니라 번호를 받지 않음`)는 화면에 올리지 않고 `SPEC.md`·`docs/` 에 적는다.
- 같은 항목도 기록할 때는 `수행할 과제`, 다시 볼 때는 `확인할 과제`, 종결할 때는 `미완료 과제`다. 디자인 통일을 이유로 하나의 이름으로 바꾸지 않는다.
- `당사자`, `실무자`, `전체 상담 목표`, `오늘 상담 목표`, `다음 상담 목표`를 그대로 쓴다. 다른 이름이 필요하면 먼저 용어 정본의 결정을 받는다.
- 서버가 돌려주는 오류 메시지(`{ error }`)도 같은 규칙이다 — 화면이 그대로 보여 주기 때문이다.

긴 문구는 글자 크기를 낮추거나 이름을 줄이는 대신 허용된 폭 안에서 읽히게 한다. 테스트가 기존 버튼 이름으로 찾는 동선을 문안 교정에 섞어 바꾸지 않는다.

### 라벨 색 (2026-09-18 Q — 전역)

- 라벨(폼 라벨 `.wire-form-label`, 표 라벨 `.wire-data-row>dt`, HERO `.wire-field-label`, 묶음 `legend`)은 **민트 deep** 이 기본이다. 카드 제목·항목 제목은 `--ink` 그대로다.
- **주목·경고**가 필요한 라벨·카드 제목은 **코랄**(`--discrepancy`)이다 — `Field tone="warn"`, `Card tone="warn"`. `--risk`(핑크)는 확인된 리스크 배너·위험 버튼 전용이라 라벨에 쓰지 않는다(CCC D9).
- **AI 산출**을 담는 라벨·카드 제목은 **라벤더**(`--lavender-deep`)다 — `tone="ai"`. 배지 계열 의미(민트=사람·진행, 라벤더=AI·승인 대기, 블루=시간 축)는 CCC §4 그대로다.

### 구분 기호 (2026-09-18 Q — 전역, 2026-09-17 규칙 대체)

- 성격이 다른 정보 조각 사이에 **가운데 점(`·`)·세로선(`|`)·긴 대시(`—`)를 쓰지 않는다.** 조각을 `Meta`(`ui.tsx`) 의 독립 노드로 두고 **간격 16**(`.wire-meta-row`, `app.css`)으로 가른다: `가명  사업명 N회차  일시`, `2회차  2026. 9. 10.  인테이크`.
- 같은 종류의 목록은 **쉼표**다: `이름, 연락처, 이메일`, 담당자 이름들. 값 안에서 두 가지를 이어 말할 때도 쉼표다: `미완료 1건, 상담 종결시 기록 작성 필요`.
- **용어 안의 가운데 점은 예외**다: `개인정보 수집·이용`, `녹음·전사`, `수기·음성`, `법률·행정`, `오전·오후`. 한 낱말로 읽히는 병렬이라 구분자가 아니다.

### 한 줄과 말줄임 (2026-09-18 Q — 전역)

- 버튼·항목 제목·항목 설명·카드 도움말은 **한 줄**이다. 버튼은 이식 계약대로 줄바꿈하지 않고(`white-space:nowrap`), 넘치면 이름을 줄인다(말줄임하지 않는다 — 한 덩어리로 읽혀야 한다).
- 항목 제목·설명(`.wire-item-title`, `.wire-item-desc`)과 카드 도움말(`.wire-card-body>.panel-meta`)은 남는 폭이 없으면 **말줄임(…)** 하고 전체 문구를 `title` 로 남긴다(`Item` 이 문자열이면 자동). 본문 단(원문·입력값·`.info-original`)은 이 규칙 밖이라 줄바꿈한다.

### 카드 여백 (2026-09-18 Q — 전역)

- 카드(`.wire-card`)는 사방 **24**(`--card-pad`) 한 값이고, 내용 묶음(`.wire-card-title`·`.wire-card-body`)은 카드 안쪽 폭을 다 쓴다(왼쪽 정렬, 양끝 폭 맞춤). 카드 자체는 열(`.wire-container`) 폭을 다 써 열 가운데 선다. 제목 구분선은 좌우 -24 풀블리드라 여백 계산에서 뺀다.
- 검증은 눈이 아니라 실측이다: 카드 안 첫 내용의 왼쪽·오른쪽·위 거리와 마지막 내용의 아래 거리가 모두 24(±1)인지 `align-check`/헤드리스로 잰다(`docs/qa-steps.js` 옆 `scripts/measure-cards.mjs`).

### 파일 고르기 (2026-09-17 Q — 전역)

파일을 고른 뒤 이름을 적거나 확인해서 올리는 자리에서는 **`파일 선택` 버튼을 입력칸 안**에 둔다(`.doc-upload-box`). 버튼이 칸 밖에 따로 서면 어디까지가 한 입력인지 읽히지 않는다. 확정 버튼(`업로드`)만 칸 밖 오른쪽에 선다. 고르는 즉시 올라가는 자리(상담 녹음의 `파일 업로드`)는 입력칸이 없어 이 규칙 밖이다.

## 7. 가져온 것과 릴레이어에서 정한 것

| 대상 | CCC에서 가져온 것 | 릴레이어의 현재 선택 |
|---|---|---|
| 색·형태·타이포 | 2026-09-14 이식한 `design/tokens.css` | 값은 유지하고 실제 사용·대비 한계를 이 문서에 기록한다. 폰트 공급은 이식되지 않았다. |
| 카드·필드·버튼 | `apps/web/app/components/wire/wire-styles.ts`의 CSS | `ui.tsx`의 최소 React 부품으로 조립한다. `WireCardSection` 등 CCC 전용 React API가 있다고 가정하지 않는다. |
| 페이지와 기록 레일 | `apps/web/app/layout.tsx`의 CSS | 머리줄·사이드바·본문의 `.app-shell`을 쓰고(2026-09-16), 767 이하에서는 드로어 대신 흐름으로 눕힌다. 사업 전환기와 목차는 아직 없다. |
| 선택창 | 공용 12px SVG 꺽쇠 | `Field control="select"`가 붙인다. `f4d3029`에서 입력칸처럼 보이던 문제를 고쳤다. |
| 반복 입력 | 입력칸과 조작 버튼의 한 행 계약 | `d47ebb5`·`f4d3029`에서 라벨 없는 중앙 정렬과 글자 버튼의 내용 폭을 되살렸다. |
| 카드 목록 | auto-fit과 기본 최소 폭 420px | `c96c60f`에서 `.two-col`만 최소 560px으로 올려 넓은 화면도 두 칸까지만 사용한다. |
| 종결 확인 | 기록 레일의 기존 카드 구조 | `d3bfb25`에서 종결 체크를 레일로 옮겼다. 이번 표면 작업에서 다시 이동하지 않는다. |
| 배지 | 모양과 22px·12px 계약 | `Badge`의 현재 톤과 건수 표현을 유지한다. 블루 시간 축 독점은 현행 구현과 다르다. |
| 당사자 정보 | 기존 카드와 단계 표시의 CSS | `.info-tabs`가 `.wire-step`을 탭으로 재사용한다. 새 탭 부품을 만들지 않는다. |
| 이름과 문장 | 이전 제품의 자료를 참고했다. | `GLOSSARY.md`의 릴레이어 용어가 우선한다. CCC의 화면명·세부 목표 체계·사이드바 규칙을 되살리지 않는다. |

CCC 문서는 출처 확인용이며 릴레이어 규칙을 읽기 위한 필수 선행 문서가 아니다. 주요 근거는 현재 스타일·화면 파일, 위 커밋, `GLOSSARY.md` §7-2다. CCC 원격 프리뷰는 접속 코드 입력 화면까지 확인했으며 내부 화면은 이번 검증 근거에 포함하지 않는다.

---

## 9. 테마 (2026-09-16 신설)

다크 토큰은 처음부터 이식돼 있었다(`tokens.css:294-357`). **없던 것은 켜는 장치뿐**이라 그것만 만들었다.
값을 새로 정하지 않았다 — CCC `DESIGN.md` §11 의 다크 값을 그대로 쓴다.

```
web/src/theme.ts              고르기·저장·기기 설정 따라가기
설정 › 내 정보 › 화면 테마    선택창 하나 — `밝게`·`어둡게`·`기기 설정 따라`
사이드바 하단 테마 버튼        라이트↔다크 한 번 누르기
```

- 고른 적이 없으면 **기기 설정을 따른다**. 밤에 쓰는 사람에게 흰 화면을 들이밀지 않는다.
- 한 번 고르면 그 선택이 기기 설정을 이긴다. 사람이 고른 것이 더 구체적인 뜻이다.
- 직접 고른 사람의 화면을 OS 가 밤이 됐다고 바꾸지 않는다.
- **설정의 테마는 선택창이다**(2026-09-17 Q). 설명은 보기 글자 안에서 끝낸다(`밝게 — 흰 바탕`) —
  세 줄 카드 셋으로 늘어놓으면 고르는 일보다 읽는 일이 커진다(§13). 고르는 즉시 칠하고 저장 버튼을 두지 않는다.
- **사이드바 하단 버튼은 두 값만 오간다**(2026-09-17 Q). 아이콘과 라벨은 **가는 곳**을 말하고
  (라이트면 달 + `어둡게`), 현재 상태는 `aria-pressed` 가 알린다. `기기 설정 따라` 는 선택창에만 있다 —
  버튼 한 번에 세 값을 돌리면 지금이 어느 값인지 알 수 없다.

실측(2026-09-16): `밝게`→`어둡게`→`밝게` 세 번 왕복, `--canvas` `#FAFAF9` ↔ `#100E13`,
`--ink` `#3D3445` ↔ `#F2EFF4`. 배지·카드도 이식 토큰을 그대로 받는다.
실측(2026-09-17): 하단 버튼 두 번 누르기 — `data-theme` `dark`→`light`→`dark`, 저장값도 같이 바뀌고
라벨이 `밝게`↔`어둡게` 로 뒤집힌다. 선택창에서 `기기 설정 따라` 를 고르면 저장값이 `system` 이 된다.

고대비(`[data-contrast="high"]`) 토큰도 이식돼 있으나 **켜는 장치는 아직 없다**(`tokens.css:359-399`).

## 10. 이식은 됐지만 아직 안 쓰는 것

2026-09-16 감사 결과, 이식 CSS 3,091줄 중 화면이 쓰는 클래스는 **55개**다. 나머지 약 2,100줄은
**CSS 만 있고 마크업이 없다.** 없는 것이 아니라 **잠들어 있는 것**이라 여기 적는다 —
다음에 그 화면을 만들 때 CSS 를 새로 쓰지 마라.

| 묶음 | 줄 수 | 대표 클래스 | 위치 |
|---|---|---|---|
| 사업 전환기 | ~60 | `.program-switcher` | `shell.css:58-108` |
| 드로어·모바일 바 | ~110 | `.drawer-bar` · `.drawer-scrim` · `.header-icon-button` | `shell.css:180-253, 365-428` |
| 아코디언 | ~90 | `.wire-card-details` · `.wire-card-summary` · `.wire-disclosure-chevron` | `wire.css:97-156, 558-590` |
| 일정 목록·후보 카드 | — | `.schedule-day-list` · `.schedule-candidate-*` | `shell.css:815-908`의 미사용 부분. 기간 탐색 부품은 상담 일정 보기에서 재사용한다 |
| 날짜·시각 피커 | ~135 | `.wire-date-popover` · `.wire-time-slot` | `wire.css:1277-1410` |
| 고대비 토큰 | ~40 | `[data-contrast="high"]` | `tokens.css:359-399` |

상담 일정 입력의 월간 7열 격자는 A안 승인 후 `.schedule-date-grid`로 구현했다.
위 표의 미사용 날짜 피커 스타일(`.wire-date-popover`·`.wire-time-slot`)을 활성화한 것은 아니다.

### 왜 아직 안 붙였나

사이드바와 머리줄은 2026-09-16 에 붙였다(Q "사람들이 헷갈려 한다" — 가로 한 줄 내비는 무엇이 무엇의 갈래인지 못 보여 준다).
남은 것은 드로어다. 손잡이 하나를 붙이는 일이 아니라 여닫는 상태·스크림·초점 처리를 새로 만드는 일이고,
767 이하는 지금 실사 표면이 아니다(§4 의 눕히는 규칙으로 막아 두었다).
아코디언도 마찬가지다 — 인테이크 구획을 접으면 E2E 가 `section.wire-card` 로 찾던 카드가 `details` 가 되고,
접힌 칸은 자동 시험이 채우지 못한다.

**관문 2 측정이 끝난 뒤에 붙인다.** 그때는 CSS 를 쓰는 일이 아니라 마크업을 맞추는 일이다.

## 12. 정본에 없어서 새로 만든 것 — 표 (2026-09-16 Q)

CCC 정본에는 표(table) 부품이 없다. 목록은 전부 `.wire-row`·`.wire-item` 같은 행 부품이고,
그것들은 한 줄에 제목과 설명 두 층을 쌓는다. 수백 건을 훑는 화면에서는 세 줄씩 흘러 못 읽는다.

그래서 `app.css` 에 `.data-table` 을 만들었다. 새 어휘를 만들지 않고 기존 토큰(`--line`,
`--sub`, `--panel`, `--gradient-hover`, `--space-*`, `--text-sm`)만 쓴다. 머리줄은 sticky 이고,
가로로 넘치면 표만 스크롤해 본문 열이 밀리지 않는다.

쓰는 곳: 설정 › 시스템 › 열람 기록 관리, 설정 › 실무자 관리 › 실무자 목록.

## 13. 설명형 글은 기본으로 두지 않는다 (2026-09-16 Q)

카드와 입력칸의 `hint` 로 규율을 설명하지 않는다 — *"기록을 펼쳐 두지 않는 건 그 자체가
개인정보를 화면에 늘어놓는 일이기 때문이에요"* 같은 문장은 **만든 사람의 변명이지 쓰는 사람의
정보가 아니다.** 화면은 대시보드다.

남기는 것은 둘뿐이다.

- **예시** — `예: 010-1234-5678`, 검색칸 `placeholder`. 무엇을 넣으라는 말이다
- **지금 화면 상태** — `200건까지 보여요`. 보이는 것이 전부가 아님을 알린다

설계 근거는 `SPEC.md` 와 `docs/` 에 적고 화면에는 올리지 않는다. 필요하면 그때 넣는다.
