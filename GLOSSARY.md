# 릴레이어 용어·분류 정본 (Glossary & Taxonomy)

- 작성일: 2026-09-14 · 상태: 정본 v1
- 근거: CCC `.worktrees/PLANNER/docs/superpowers/plans/2026-09-13-planner-handoff.md`(표시명 확정 9건, 후속 요구 36건, 다섯 결정, 인테이크 I-01~I-11), `inbox/2026-09-12/handoff-decisions-2026-09-12.md`(01~54), `inbox/2026-09-12/relayer-definitions-v2-2026-09-12.md`, `CONTEXT.md`
- **이 문서가 이름의 유일한 정본이다.** 화면·API·문서·티켓에서 이름이 충돌하면 여기를 따른다. 잦은 수정의 가장 큰 원인이 용어·분류 미정리였으므로, 이름을 바꾸려면 코드가 아니라 이 파일을 먼저 고친다.

---

## 0. 세 가지 층을 절대 섞지 않는다

| 층 | 무엇 | 바꿀 수 있나 |
|---|---|---|
| **표시명** | 사람이 화면에서 읽는 말 | Q 확정으로 바뀐다. 이 문서 §2~§4 |
| **개체명** | 도메인에서 그것이 무엇인지 | 거의 안 바뀐다. 이 문서 §5 |
| **식별자** | URL, DB 컬럼, API 경로, `module` 값, 역할 코드 | **표시명이 바뀌어도 바꾸지 않는다** |

2026-09-13 확정: "이 표의 이름 변경만으로 URL, destination ID, `module` 값, API, 저장 식별자, 권한이나 기능을 바꾸지 않는다."

**한 개체가 표면마다 다른 라벨을 갖는 것은 정상이다.** 예: 같은 과제 하나가 쓰는 자리에서는 `수행할 과제`, 다시보기에서는 `확인할 과제`, 종결에서는 `미완료 과제`다. 세 라벨을 하나로 통일하려 들면 안 된다(§5-3).

---

## 1. 제품 이름

| 층 | 값 |
|---|---|
| 제품 표시명 | **릴레이어** |
| 이전 이름 | CCC (Case Context Companion) — 문서 안 역사 기록에만 남는다 |
| 코드·리소스 식별자 | 기존 `ccc` 계열을 그대로 둔다. 새 레포의 패키지명만 `relayer`를 쓴다 |

---

## 2. 화면 이름 (전체 목록)

**이 표가 화면 이름의 전부다.** 여기 없는 화면 이름을 지어내지 않는다. 표에 없는 화면이 필요하면 정본(`apps/client/src/business/navigation.ts`와 2026-09-12 재구성 문서 §사이드바)을 먼저 읽고 이 표에 추가한다.

> 2026-09-14 사고 기록: 이 표가 "바뀐 이름 9건"만 담고 있어서 안 바뀐 이름의 목록이 없었고, 빈자리에 `사례 등록`이라는 없는 말이 들어갔다. 그래서 전체 목록으로 바꿨다.

### 2-1. 사례 안 화면

| 표시명 | 경로 | 구 표시명 |
|---|---|---|
| **15초 다시보기** | `<case>/briefing` | 상담 전 톺아보기 |
| **인테이크 작성하기** | `<case>/records/intake` | 첫 상담 기록 |
| **상담 기록하기** | `<case>/records/new` | 오늘 상담 기록 |
| ↳ 예정 회차가 없으면 `상담 일시와 상담 방식` 구획이 먼저 뜬다(2026-09-15 Q). 일정 등록은 기록의 전제가 아니다 | | |
| **AI가 정리한 내용 검토하기** | `<case>/records/:id/review` | AI 정리 검토 |
| **상담 종결** | `<case>/close` | 사례 종결 |
| **당사자 정보** (탭 4: 회차별 요약 · 15초 다시보기 · 목표 · 정보) | `/participants/:id` | — |

### 2-2. 사이드바 (정본 5묶음)

| 묶음 | 표시명 | 경로 | 구 표시명 |
|---|---|---|---|
| 오늘 | 일정 | `/schedule` | — |
| | 상담 일정 등록 | `/schedules/new` | — (결정 42의 `상담 등록`은 2026-09-14 Q가 철회) |
| 당사자 | 당사자 목록 | `/participants` | — |
| | **당사자 등록** | `/participants/new` | — (바뀌지 않는다) |
| | 당사자 초대 | `/participants/invite` | — |
| 내 것 | **내 정보** · **설정** (둘로 분리) | `/settings?module=account` | 내 정보 / 기관 모듈 |
| 기관 | 기관 준비 · 기관 정보 · 사용자와 역할 · 담당 배정 요청 · 실무자 초대 | `/onboarding` 외 | — |
| | **AI 연결 설정** | `/settings?module=system` | 연결 상태 |
| 기록과 개인정보 | **상담 기록 처리 현황** | `/settings?module=memory` | 기관 상담 기억 |
| | **열람 기록** | `/settings?module=audit` | 감사 기록 |
| | 개인정보 보유기간 · 개인정보 보존 검토 | `?module=retention-policy`, `?module=retention` | — |

굵은 이름이 2026-09-13 확정으로 바뀐 것이다. 나머지는 그대로 유지한다.

**사례를 여는 자리**: 별도 화면이 아니다. `당사자 등록` 화면 안의 **`참여 사업`** 칸에서 함께 열린다. 사례만 따로 만드는 화면은 없다.

> 라벨 근거: `CONTEXT.md` 용어집(2026-07-26 Q 확정)이 **`참여 사업`**이다. 프로토타입 문구 사전의 `참여할 사업`(`copy.js:260`)은 같은 파일 안에서도 `참여 사업`(`copy.js:516`)과 엇갈리는 국소 표기이므로 용어집을 따른다. 이미 참여 중인 관계를 `참여할`로 부르지 않는다.

**이름 규칙 개정**: 결정 44의 "화면 이름은 명사, 동사형은 버튼에만"은 2026-09-13 확정이 **부분 대체**한다. 기록·작성 행동이 곧 목적지인 화면은 동사형을 쓴다(`상담 기록하기`, `인테이크 작성하기`, `AI가 정리한 내용 검토하기`). 읽기 화면은 명사형을 유지한다(`15초 다시보기`, `회차별 요약`, `목표`, `정보`).

**긴 이름 제약**: `AI가 정리한 내용 검토하기`, `상담 기록 처리 현황`은 1280/767/390 폭에서 잘림·겹침 없이 읽혀야 하며, 이를 위해 글자 크기나 CSS 기준을 낮추지 않는다.

---

## 3. 사람 (역할 분류)

| 표시명 | 뜻 | 금지 표기 | 식별자 |
|---|---|---|---|
| **당사자** | 지원 사업의 지원 대상이자 상담의 주체 | 참여자, 수혜자, 클라이언트, 대상자, 이용자 | `beneficiaryId`, `participants/` |
| **실무자** | 상담을 수행하고 기록하는 사람. **화면의 모든 `담당자` 표기를 실무자로 통일한다**(요구 17) | 담당자, 상담사, 카운슬러 | `users.role='worker'`, 배정은 `assignee` |
| **관리자** | 기관 운영 전부(기관 정보, 사용자·역할, 실무자 초대, 배정 확정, 열람 기록, 개인정보 관리, AI 연결 설정, 설치·서버) | 어드민, 기관 관리자/기술 관리자로 쪼개 부르기 | `role='admin'` |
| **기관** | 이 서비스를 쓰는 조직 단위. 배포기관과 구별할 때만 `사용기관` | 조직, 회사, 센터 | `organizationId` |
| **배포기관** | 릴레이어를 만들고 배포하는 곳(사회연대은행) | 우리 기관, 본사, 플랫폼 | — |

**역할은 셋뿐이다 (2026-09-14 Q 확정).** 구 `기관 관리자` / `기술 관리자` 2단 구분을 **관리자 하나로 합친다.** 설치·서버 작업은 별도 역할이 아니라 관리자의 운영 절차다. 역할을 늘리려면 이 문서를 먼저 고친다.

| 역할 | 볼 수 있는 것 |
|---|---|
| 당사자 | **로그인하지 않는다.** 실무자가 전달한 링크 + 코드로 여는 열람 페이지에서 **기본정보(이름·연락처·이메일)와 자기 일정까지**만 본다. 베타에서는 빼고 P2에 붙인다 |
| 실무자 | 배정된 사례의 상담 자료 |
| 관리자 | 기관 운영 기능 + 배정. 상담 내용 열람은 배정과 감사 규칙을 따른다 |

**배정 규칙**: 배정은 **관리자가 확정하면 즉시 효력**이 생긴다. 실무자의 별도 수락 단계는 없다. 기존 대기 요청을 일괄 활성화하지 않는다. `내 정보`에 배정 수락 기능을 두지 않는다(요구 35).

---

## 4. 목표 세 층 (가장 자주 틀리던 곳)

| 표시명 | 어디서 쓰나 | 어디서 보이나 | 수명 |
|---|---|---|---|
| **전체 상담 목표** | 인테이크 작성하기. **비워둘 수 있다.** 이후 상담 중에도 고칠 수 있다 | 15초 다시보기 `목표`, 목표 탭 | 사례 전체. 구 `장기 목표` |
| **오늘 상담 목표** | 직접 쓰지 않는다 — 직전 회차의 `다음 상담 목표`를 이어받는다 | **15초 다시보기 `목표`** | 그 회차 1회. 구 `단기 목표` + 구 `이번 상담 목표` 통합 |
| **다음 상담 목표** | 상담 기록하기 (요구 19) | 다음 실제 상담의 `오늘 상담 목표` | 다음 회차로 한 번 이어짐 |

**이어받기 규칙**: `다음 상담 목표`는 **같은 사례의 다음 실제 상담**으로 한 번만 이어진다. 조회·예약 변경·예약 취소로 소비되지 않는다. 출처 회차와 수신 회차를 둘 다 보존한다. **`상담 종결`은 실제 상담이 아니므로 소비하지 않는다**(§5-4).

**수정 규칙 (2026-09-14 Q 확정)**: **모든 목표는 고칠 수 있다.**

| 대상 | 언제 | 어디서 | 남기는 것 |
|---|---|---|---|
| 전체 상담 목표 | 인테이크 이후 언제든. 상담 중 방향이 바뀌면 그 자리에서 | 인테이크 작성하기, 상담 기록하기의 목표 칸, (P2) 목표 탭 | 이전 문구를 `goal_revisions`에 남긴다. 덮어쓰기만 하지 않는다 |
| 오늘 상담 목표 | 그 회차를 기록하는 동안 | 상담 기록하기 | 이어받은 원문과 출처 회차를 보존하고, 고친 값은 수신 회차의 값으로 저장 |
| 다음 상담 목표 | 그 회차 기록이 열려 있는 동안 | 상담 기록하기 | 아직 소비되지 않았을 때만 고친다 |

- **목표가 없는 상태는 정상이다.** 인테이크에서 목표를 안 세워도 저장된다. 15초 다시보기는 **목표 없음**으로 표시하고 빈 문구를 지어내지 않는다.
- **이미 지나간 회차의 목표는 소급 변경하지 않는다.** 오늘 고친 전체 목표가 지난 회차 기록의 당시 목표를 바꾸지 않는다.
- 15초 다시보기 자체는 **읽기 전용**이다(결정 25). 수정 진입만 제공한다.

**삭제된 것**: 15초 다시보기의 구 `이번 상담 목표` 구획(요구 7). `단기목표`라는 말 자체를 쓰지 않는다.

---

## 5. 기록 개체와 표면별 라벨

### 5-1. 카드 4종 (내부 분류, 화면에 노출하지 않음)

`fact` 사실 · `question` 확인할 것 · `promise` 약속한 일 · `judgment` 판단.
실무자는 분류를 고르지 않는다. **기록지의 구획이 곧 분류다.**

### 5-2. 질문 (`question`)

| 표면 | 라벨 |
|---|---|
| 상담 기록하기에서 쓸 때 | **다음에 물어볼 것** |
| 인테이크 작성하기에서 쓸 때 | **다음에 물어볼 것** (같은 입력·같은 이어받기, I-08) |
| 15초 다시보기에서 읽을 때 | **오늘 물어볼 것** |

미확인 질문은 누적된다. 새 입력이 과거 질문을 조용히 덮어쓰지 않는다.

### 5-3. 과제 (`promise`)

| 표면 | 라벨 |
|---|---|
| 상담 기록하기에서 쓸 때 | **수행할 과제** (구 `다음까지 하기로 한 것`) |
| 15초 다시보기에서 읽을 때 | **확인할 과제** (구 `남은 할 일`) |
| 상담 종결에서 읽을 때 | **미완료 과제** (구 `남은 할 일 처리`) |
| 일정 화면 | **할 일** (구 `오늘 할 일`) |

**입력에서 뺀 것**: 기한(요구 15), 수행 주체 `누가`의 본인/담당자/기관 선택(요구 16). 숨긴 채 임의 값을 채우지 않고, AI가 수행자를 추정하지 않는다.

### 5-4. 회차와 상담 종결

| 표시명 | 뜻 |
|---|---|
| **회차** | 한 번의 상담. 예정 상태와 기록 완료 상태를 갖는다 |
| **회차별 요약** | 당사자 정보의 기록 탭(구 `상담 기록`). 원문 보기 포함 |
| **상담 종결** | 사례를 닫는 **화면이자 그때 남는 기록**. 마지막 상담에 붙지만 **상담 회차가 아니다**. `종결일지`라는 별도 이름을 쓰지 않는다 |

**상담 종결 규칙 (2026-09-14 Q 확정)**

- **회차가 아니다.** 회차 번호(`seq`)를 받지 않고, `N회차/총 M회차` 계산과 회차 수에 들어가지 않으며, `다음 상담 목표`를 소비하지 않는다.
- **마지막 상담에 붙는다.** 어느 회차 뒤의 종결인지를 가리키는 연결(`last_session_id`)을 갖는다.
- 사례 하나에 **상담 종결 기록 하나**. 종결 확정과 같은 트랜잭션에서 만들고, 같은 종결을 다시 시도해도 중복 생성하지 않는다.
- 일반 상담일지를 덮지 않는다. `회차별 요약`에는 마지막 상담과 `상담 종결`이 **두 항목**으로 보인다.
- 내용은 종결 사유, **미완료 과제** 처리, 확인 항목이다. 실제 상담을 한 것처럼 기록하지 않는다.

---

## 6. 화면 구성 (요구 36건 반영 후)

### 6-1. 15초 다시보기 — 6구획

1. 위험 신호 (상시 표시, 접히지 않음)
2. 당사자 카드 + `오늘 만나기 전 꼭 기억할 것` (유지, 요구 6)
3. 목표 — **전체 상담 목표** + **오늘 상담 목표**(직전 회차에서 쓴 `다음 상담 목표`가 여기 뜬다). 둘 다 없으면 이 구획을 뺀다. 읽기 전용이며 수정 진입만 제공한다
4. 직전 회차 요약
5. 오늘 물어볼 것
6. 확인할 과제

**제거**: `이번 상담 목표`(요구 7), `일치하지 않는 기록`(요구 11). 톺아보기 시절의 7항목이 아니다.

### 6-2. 상담 기록하기 — 6구획

1. 수기 메모 (**유일한 필수**)
2. 수행할 과제
3. 다음에 물어볼 것
4. 달라진 것 (선택. 비우면 `이번 회차 미확인`)
5. 실무자 의견 (구 `담당자 판단`)
6. 목표 — **다음 상담 목표**(새로 씀) + **전체 상담 목표**(필요하면 여기서 고침, 이전 문구는 이력에 남음)

메타: 상담 장소는 **대면일 때만** 입력한다(요구 14).
**제거**: 상담 유형(13), 위기도(18), 추가 기록(20), 과제 기한(15), 수행 주체 선택(16).

### 6-3. 상담 일정 등록 — 3구획

| # | 구획 | 내용 |
|---|---|---|
| 1 | **당사자** | 당사자 검색 + 사례 고르기 + 동의 상태 표시(결정 47). 사례 화면에서 이어 들어오면 이미 정해져 있다 |
| 2 | **일시와 상담 방식** | 일시, 상담 방식 4종(대면·방문·전화·화상). 상담 장소는 대면일 때만 |
| 3 | **메모** | `이번 상담 목표` + `다음에 물어볼 것`을 합친 자유 입력(요구 4). 카드가 되지 않는다 |

**구획 이름은 명사다**(결정 44). 정본 프로토타입의 `누구와`·`언제, 어떻게`는 의문형이라 쓰지 않는다 — 2026-09-14 Q 확정으로 각각 **`당사자`**, **`일시와 상담 방식`**으로 고친다.

**`상담 방식` vs `상담 장소` vs `선호 상담 방식`** — 세 층을 섞지 않는다.

| 말 | 담는 것 | 어디 |
|---|---|---|
| **상담 방식** | 대면 · 방문 · 전화 · 화상 | 상담 일정 등록, 상담 기록하기, 인테이크 운영정보 |
| **상담 장소** | 어디서 만났는지 (자유 입력) | 대면일 때만 |
| **선호 상담 방식** | 당사자가 선호하는 형식. 수식어 `선호`로 구분한다 | 인테이크 3-4 (베타 밖) |

근거: 2026-09-14 Q 확정. 프로토타입 문구 사전(`copy.js:283,397`)과 일치한다. `PRD/intake-questionnaire-v1.md:64`와 2026-09-12 재구성 문서의 `상담 방법`은 구 표기이며 화면에 쓰지 않는다. 당사자 선호 축은 `선호`를 붙여 구분한다.

**종결 상담** 선택을 둔다. 고르면 일반 상담 기록 흐름을 그대로 따르고, **저장 성공 뒤** `상담 종결` 화면으로 이동한다. 저장 실패 시 이동하지 않고 사례를 닫지 않는다(요구 5).

> 구현 2026-09-15: `sessions.is_closing`. 일정 등록에서 미리 고르거나 기록 화면에서 그 자리에서 고른다(기록 화면이 일정의 표시를 이어받고, 바꿀 수도 있다). 회차 종류(`kind`)를 늘리지 않았다 — 종결 상담도 평범한 상담이고, 사례를 닫는 일은 `case_closures` 가 맡는다. 저장 단추 문구가 `저장하고 종결로` 로 바뀌어 다음에 무엇이 오는지 미리 알린다.

### 6-4. 당사자 정보 — 탭 4

| 탭 | 내용 |
|---|---|
| 회차별 요약 | 회차 목록 + 원문 보기 |
| 15초 다시보기 | 가장 최근 것을 여기서 본다 (구 `경과 리포트` 자리) |
| 목표 | 전체 상담 목표 + 날짜별 오늘 상담 목표 |
| 정보 | 기본 정보 + **상담 종결 버튼**(누르면 `상담 종결` 화면) |

**제거**: 당사자 카드(요구 25 — 15초 다시보기의 카드는 유지), 할 일 전체(28).

### 6-5. AI가 정리한 내용 검토하기

- 버튼은 **승인 / 수정** 두 개. 반려·재생성 삭제(요구 21). 수정은 사람이 직접 문구를 고치는 것이며, 수정본도 **별도 승인 전에는 초안**이다
- 수정 가능: 요약·핵심 한 줄·과제 후보·다음 질문 문구
- 수정 불가(잠금): 원문·근거 인용·출처 hash·불일치 판정
- `후속 제안` → **수행할 과제** + **다음에 물어볼 것** 둘로 분리(요구 22)
- `대조` → **음성·수기 기록 불일치** + **회차간 기록 불일치** 둘로 분리(요구 23)

### 6-6. 일정

`오늘 할 일` → **할 일**(1), `검토할 AI 초안` → **초안 검토**(2), `맞음·틀림 미정 위험 신호` 표시 **제거**(3 — 다른 화면의 확정 위험 신호나 저장 이력은 그대로).

### 6-7. 내 정보 / 설정

| 묶음 | 내용 | 권한 |
|---|---|---|
| 내 정보 | 표시 이름·로그인 이메일·비밀번호 수정만. 배정 수락 제거 | 본인 |
| 설정 › 개인 보안 | 추가 인증(MFA) | 본인 |
| 설정 › 기관 운영 | 기관 정보, 사용자·역할, 실무자 초대, 기관 준비, 담당 배정 확정 | 관리자 |
| 설정 › AI 연결 설정 | 설치·AI·STT·장비 상태 조회와 재확인 | 관리자 |
| 설정 › 상담 기록 처리 현황 | 기억 설정과 처리 건수 | 관리자 |
| 설정 › 열람 기록 | 감사 사건 조회 | 관리자 |
| 설정 › 개인정보 관리 | 보유기간, 보존·파기 검토 | 관리자 |

기관·역할은 본인이 바꾸지 않는다.

---

## 7. 인테이크 (I-01~I-11 중 확정분)

| 질문 | 선택지 | 키 |
|---|---|---|
| 기초생활보장 수급 여부 | 수급 중 / 과거 수급 / 비수급 / 신청·심사 중 / 무응답 (단일) | `basic_livelihood_status` |
| 수급 유형 | 생계·의료·주거·교육 (복수) / 해당 없음 / 무응답 (배타) | `basic_livelihood_benefit_types` |
| 차상위계층 여부 | 해당 / 비해당 / 신청·확인 중 / 무응답 (단일) | `near_poverty_status` |
| 기타 공적급여 | 짧은 자유 입력 | `other_public_benefits` |

- 구 8개 체크(한부모·장애인연금·기초연금 등)로 되돌리지 않는다. 과거 응답은 소급 변환하지 않고 원문을 보존한다.
- `도움을 요청할 수 있는 사람` + `본인의 강점` → **강점과 비공식 자원** 한 칸으로 합침(I-07). 기존 `strength_detail` 재사용.
- 고른 어려움 영역은 **실제 세부 질문을 펼친다**. 제목·배지만 보여주면 미완이다(I-05).
- 전체 상담 목표는 사례의 `overall_goal`을 쓰고 질문지 JSON에 별도 정본을 만들지 않는다(I-09).

---

## 8. 삭제된 말 (되살리지 않는다)

`톺아보기` · `15초 페이지` · `케이스` · `사례 등록`(정본에 없던 말) · `참여할 사업` · `상담 방법`(→ 상담 방식) · `사례 종결` · `종결일지` · `장기 목표` · `단기 목표` · `이번 상담 목표` · `남은 할 일` · `다음까지 하기로 한 것` · `담당자` · `위기도` · `상담 유형` · `추가 기록` · `일치하지 않는 기록`(15초 다시보기에서만) · `경과 리포트`(탭 이름으로서) · `감사 기록` · `기관 상담 기억` · `연결 상태` · `환경설정`

**단, 자료를 가리키는 일반 문장은 일괄 치환하지 않는다.** "첫 상담 기록이 있는 사업", "저장된 첫 상담 기록"처럼 자료 자체를 부르는 말, 상담 유형·기록 형식 같은 일반 명사, 과거 결정 문서의 역사 기록은 그대로 둔다. 바꾸는 것은 **페이지 제목·메뉴·진입 버튼·그 제목을 가리키는 안내문**이다.

---

## 9. 화면에서 지운다고 자료를 지우는 것이 아니다

위기도·추가 기록·상담 유형·기한·수행 주체·일치하지 않는 기록은 **입력과 표시에서만** 사라진다. 이미 저장된 값, 원문, 감사 이력은 보존하고, 빠진 값을 `안정`이나 `변화 없음`으로 추정해 채우지 않는다. 구 버전 기록은 그 당시 의미로 읽는다.
