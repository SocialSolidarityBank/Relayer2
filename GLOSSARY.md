# 릴레이어 용어·분류 정본 (Glossary & Taxonomy)

- 작성일: 2026-09-14 · 상태: 정본 v1
- 근거: CCC `.worktrees/PLANNER/docs/superpowers/plans/2026-09-13-planner-handoff.md`(표시명 확정 9건, 후속 요구 36건, 다섯 결정, 인테이크 I-01~I-11), `inbox/2026-09-12/handoff-decisions-2026-09-12.md`(01~54), `inbox/2026-09-12/relayer-definitions-v2-2026-09-12.md`, `CONTEXT.md`
- **이 문서가 이름의 유일한 정본이다.** 화면·API·문서·티켓에서 이름이 충돌하면 여기를 따른다. 잦은 수정의 가장 큰 원인이 용어·분류 미정리였으므로, 이름을 바꾸려면 코드가 아니라 이 파일을 먼저 고친다.

---

## 0. 세 가지 층을 절대 섞지 않는다

| 층 | 무엇 | 바꿀 수 있나 |
|---|---|---|
| **표시명** | 사람이 화면에서 읽는 말 | Q 확정으로 바뀐다. 이 문서 §2~§4 |
| **개체명** | 도메인에서 그것이 무엇인지 | 거의 안 바뀐다. 이 문서 §5 |
| **식별자** | URL, DB 컬럼, API 경로, `module` 값, 역할 코드 | **표시명이 바뀌어도 바꾸지 않는다** |

2026-09-13 확정: "이 표의 이름 변경만으로 URL, destination ID, `module` 값, API, 저장 식별자, 권한이나 기능을 바꾸지 않는다."

**한 개체가 표면마다 다른 라벨을 갖는 것은 정상이다.** 예: 같은 과제 하나가 쓰는 자리에서는 `수행할 과제`, 다시보기에서는 `확인할 과제`, 종결에서는 `미완료 과제`다. 세 라벨을 하나로 통일하려 들면 안 된다(§5-3).

---

## 1. 제품 이름

| 층 | 값 |
|---|---|
| 제품 표시명 | **릴레이어** |
| 이전 이름 | CCC (Case Context Companion) — 문서 안 역사 기록에만 남는다 |
| 코드·리소스 식별자 | 기존 `ccc` 계열을 그대로 둔다. 새 레포의 패키지명만 `relayer`를 쓴다 |

---

## 2. 화면 이름 (전체 목록)

**이 표가 화면 이름의 전부다.** 여기 없는 화면 이름을 지어내지 않는다. 표에 없는 화면이 필요하면 정본(`apps/client/src/business/navigation.ts`와 2026-09-12 재구성 문서 §사이드바)을 먼저 읽고 이 표에 추가한다.

> 2026-09-14 사고 기록: 이 표가 "바뀐 이름 9건"만 담고 있어서 안 바뀐 이름의 목록이 없었고, 빈자리에 `사례 등록`이라는 없는 말이 들어갔다. 그래서 전체 목록으로 바꿨다.

### 2-1. 사례 안 화면

| 표시명 | 경로 | 구 표시명 |
|---|---|---|
| **15초 다시보기** | `<case>/briefing` | 상담 전 톺아보기 |
| **인테이크 작성하기** | `<case>/records/intake` | 첫 상담 기록 |
| **상담 기록하기** | `<case>/records/new` | 오늘 상담 기록 |
| ↳ 예정 회차가 없으면 `상담 일시와 상담 방식` 구획이 먼저 뜬다(2026-09-15 Q). 일정 등록은 기록의 전제가 아니다 | | |
| **AI가 정리한 내용 검토하기** | `<case>/records/:id/review` | AI 정리 검토 |
| **상담 종결** | `<case>/close` | 사례 종결 |
| **당사자 정보** (탭 4: 회차별 요약 · 15초 다시보기 · 목표 · 정보) | `/participants/:id` | — |

### 2-2. 사이드바 (정본 5묶음)

| 묶음 | 표시명 | 경로 | 구 표시명 |
|---|---|---|---|
| 일정 | **상담 일정 보기** (페이지 HERO: **상담 일정**) | `/schedule` | 일정 |
| | 상담 일정 등록 | `/schedules/new` | — (결정 42의 `상담 등록`은 2026-09-14 Q가 철회) |
| 당사자 | 당사자 목록 | `/participants` | — |
| | **당사자 등록** | `/participants/new` | — (바뀌지 않는다) |
| | 당사자 초대 | `/participants/invite` | — |
| 내 것 | **내 정보** · **설정** (둘로 분리) | `/settings?module=account` | 내 정보 / 기관 모듈 |
| 기관 | 기관 준비 · 기관 정보 · 사용자와 역할 · 담당 배정 요청 · 실무자 초대 | `/onboarding` 외 | — |
| | **AI 연결 설정** | `/settings?module=system` | 연결 상태 |
| 기록과 개인정보 | **상담 기록 처리 현황** | `/settings?module=memory` | 기관 상담 기억 |
| | **열람 기록** | `/settings?module=audit` | 감사 기록 |
| | 개인정보 보유기간 · 개인정보 보존 검토 | `?module=retention-policy`, `?module=retention` | — |

별도 변경일이 없는 굵은 이름은 2026-09-13 확정 용어다. 상담 일정 보기의 메뉴·HERO 구분은 아래 2026-09-17 결정을 따른다.

`상담 일정`의 보기 선택 이름은 **월간 · 주간 · 일간**이며 월간이 기본이다(2026-09-17 Q). 메뉴 이름과 페이지 제목을 구분하며 경로는 바꾸지 않는다.

**사례를 여는 자리**: 별도 화면이 아니다. `당사자 등록` 화면 안의 **`참여 사업`** 칸에서 함께 열린다. 사례만 따로 만드는 화면은 없다.

> 라벨 근거: `CONTEXT.md` 용어집(2026-07-26 Q 확정)이 **`참여 사업`**이다. 프로토타입 문구 사전의 `참여할 사업`(`copy.js:260`)은 같은 파일 안에서도 `참여 사업`(`copy.js:516`)과 엇갈리는 국소 표기이므로 용어집을 따른다. 이미 참여 중인 관계를 `참여할`로 부르지 않는다.

**이름 규칙 개정**: 결정 44의 "화면 이름은 명사, 동사형은 버튼에만"은 2026-09-13 확정이 **부분 대체**한다. 기록·작성 행동이 곧 목적지인 화면은 동사형을 쓴다(`상담 기록하기`, `인테이크 작성하기`, `AI가 정리한 내용 검토하기`). 읽기 화면은 명사형을 유지한다(`15초 다시보기`, `회차별 요약`, `목표`, `정보`).

**긴 이름 제약**: `AI가 정리한 내용 검토하기`, `상담 기록 처리 현황`은 1280/767/390 폭에서 잘림·겹침 없이 읽혀야 하며, 이를 위해 글자 크기나 CSS 기준을 낮추지 않는다.

---

## 3. 사람 (역할 분류)

| 표시명 | 뜻 | 금지 표기 | 식별자 |
|---|---|---|---|
| **당사자** | 지원 사업의 지원 대상이자 상담의 주체 | 참여자, 수혜자, 클라이언트, 대상자, 이용자 | `beneficiaryId`, `participants/` |
| **실무자** | 상담을 수행하고 기록하는 사람. **화면의 모든 `담당자` 표기를 실무자로 통일한다**(요구 17) | 담당자, 상담사, 카운슬러 | `users.role='worker'`, 배정은 `assignee` |
| **관리자** | 기관 운영 전부(기관 정보, 사용자·역할, 실무자 초대, 배정 확정, 열람 기록, 개인정보 관리, AI 연결 설정, 설치·서버) | 어드민, 기관 관리자/기술 관리자로 쪼개 부르기 | `role='admin'` |
| **기관** | 이 서비스를 쓰는 조직 단위. 배포기관과 구별할 때만 `사용기관` | 조직, 회사, 센터 | `organizationId` |
| **배포기관** | 릴레이어를 만들고 배포하는 곳(사회연대은행) | 우리 기관, 본사, 플랫폼 | — |

**역할은 셋뿐이다 (2026-09-14 Q 확정).** 구 `기관 관리자` / `기술 관리자` 2단 구분을 **관리자 하나로 합친다.** 설치·서버 작업은 별도 역할이 아니라 관리자의 운영 절차다. 역할을 늘리려면 이 문서를 먼저 고친다.

| 역할 | 볼 수 있는 것 |
|---|---|
| 당사자 | **로그인하지 않는다.** 실무자가 전달한 링크 + 코드로 여는 열람 페이지에서 **기본정보(이름·연락처·이메일)와 자기 일정까지**만 본다. 베타에서는 빼고 P2에 붙인다 |
| 실무자 | 자신이 단독 또는 공동으로 배정된 사례의 상담 자료 |
| 관리자 | 기관 운영·배정 기능. 상담 자료는 관리자도 해당 사례에 배정되어야 열람 가능 |

**배정 규칙**: 팀 합의에 따른 배정을 관리자가 확정하면 효력이 생긴다. 한 사례에 **공동 실무자를 여러 명** 배정할 수 있고, 기존 실무자를 **개별 제외**할 수 있다(2026-09-16 Q). 교체만 가능한 구조가 아니다. 제외된 사람은 다음 자료 요청부터 접근할 수 없다. 요청 승인은 기존 담당을 유지하며 요청자를 추가한다. 실무자의 별도 수락 단계는 없고, 과거 대기 요청을 일괄 활성화하지 않는다.

미배정 사례를 임의로 누구에게나 열어주지 않는다. 배정 요청·관리용 목록에는 가명·사업·현재 배정만 보이고 상담 내용·실명·회차·일정은 노출하지 않는다. 동의 사건과 당사자 열람 링크도 사례별로 관리한다.

---

## 4. 목표 세 층 (가장 자주 틀리던 곳)

| 표시명 | 어디서 쓰나 | 어디서 보이나 | 수명 |
|---|---|---|---|
| **전체 상담 목표** | 인테이크 작성하기. **비워둘 수 있다.** 이후 상담 중에도 고칠 수 있다 | 15초 다시보기 `목표`, 목표 탭 | 사례 전체. 구 `장기 목표` |
| **오늘 상담 목표** | 직접 쓰지 않는다 — 직전 회차의 `다음 상담 목표`를 이어받는다 | **15초 다시보기 `목표`** | 그 회차 1회. 구 `단기 목표` + 구 `이번 상담 목표` 통합 |
| **다음 상담 목표** | 상담 기록하기 (요구 19) | 다음 실제 상담의 `오늘 상담 목표` | 다음 회차로 한 번 이어짐 |

**이어받기 규칙**: `다음 상담 목표`는 **같은 사례의 다음 실제 상담**으로 한 번만 이어진다. 조회·예약 변경·예약 취소로 소비되지 않는다. 출처 회차와 수신 회차를 둘 다 보존한다. **`상담 종결`은 실제 상담이 아니므로 소비하지 않는다**(§5-4).

**수정 규칙 (2026-09-14 Q 확정)**: **모든 목표는 고칠 수 있다.**

| 대상 | 언제 | 어디서 | 남기는 것 |
|---|---|---|---|
| 전체 상담 목표 | 인테이크를 작성하거나 다시 고칠 때 | 인테이크 작성하기 | 이전 문구를 `goal_revisions`에 남긴다. 상담 기록 화면에서 빠져도 저장된 목표는 유지한다 |
| 오늘 상담 목표 | 그 회차를 기록하는 동안 | 상담 기록하기 | 이어받은 원문과 출처 회차를 보존하고, 고친 값은 수신 회차의 값으로 저장 |
| 다음 상담 목표 | 그 회차 기록이 열려 있는 동안 | 상담 기록하기 | 아직 소비되지 않았을 때만 고친다 |

- **목표가 없는 상태는 정상이다.** 인테이크에서 목표를 안 세워도 저장된다. 15초 다시보기는 **목표 없음**으로 표시하고 빈 문구를 지어내지 않는다.
- **이미 지나간 회차의 목표는 소급 변경하지 않는다.** 오늘 고친 전체 목표가 지난 회차 기록의 당시 목표를 바꾸지 않는다.
- 15초 다시보기 자체는 **읽기 전용**이다(결정 25). 수정 진입만 제공한다.

**삭제된 것**: 15초 다시보기의 구 `이번 상담 목표` 구획(요구 7). `단기목표`라는 말 자체를 쓰지 않는다.

---

## 5. 기록 개체와 표면별 라벨

### 5-1. 카드 4종 (내부 분류, 화면에 노출하지 않음)

`fact` 사실 · `question` 확인할 것 · `promise` 약속한 일 · `judgment` 판단.
실무자는 분류를 고르지 않는다. **기록지의 구획이 곧 분류다.**

### 5-2. 질문 (`question`)

| 표면 | 라벨 |
|---|---|
| 상담 기록하기에서 쓸 때 | **다음에 물어볼 것** |
| 인테이크 작성하기에서 쓸 때 | **다음에 물어볼 것** (같은 입력·같은 이어받기, I-08) |
| 15초 다시보기에서 읽을 때 | **오늘 물어볼 것** |

미확인 질문은 누적된다. 새 입력이 과거 질문을 조용히 덮어쓰지 않는다.

### 5-3. 과제 (`promise`)

| 표면 | 라벨 |
|---|---|
| 상담 기록하기에서 쓸 때 | **수행할 과제** (구 `다음까지 하기로 한 것`) |
| 15초 다시보기에서 읽을 때 | **확인할 과제** (구 `남은 할 일`) |
| 상담 종결에서 읽을 때 | **미완료 과제** (구 `남은 할 일 처리`) |
| 일정 화면 | **할 일** (구 `오늘 할 일`) |

**입력에서 뺀 것**: 기한(요구 15), 수행 주체 `누가`의 본인/담당자/기관 선택(요구 16). 숨긴 채 임의 값을 채우지 않고, AI가 수행자를 추정하지 않는다.

### 5-4. 회차와 상담 종결

| 표시명 | 뜻 |
|---|---|
| **회차** | 한 번의 상담. 예정 상태와 기록 완료 상태를 갖는다 |
| **회차별 요약** | 당사자 정보의 기록 탭(구 `상담 기록`). 원본 보기 포함 |
| **상담 종결** | 사례를 닫는 **화면이자 그때 남는 기록**. 마지막 상담에 붙지만 **상담 회차가 아니다**. `종결일지`라는 별도 이름을 쓰지 않는다 |

**상담 종결 규칙 (2026-09-14 Q 확정)**

- **회차가 아니다.** 회차 번호(`seq`)를 받지 않고, `N회차/총 M회차` 계산과 회차 수에 들어가지 않으며, `다음 상담 목표`를 소비하지 않는다.
- **마지막 상담에 붙는다.** 어느 회차 뒤의 종결인지를 가리키는 연결(`last_session_id`)을 갖는다.
- 사례 하나에 **상담 종결 기록 하나**. 종결 확정과 같은 트랜잭션에서 만들고, 같은 종결을 다시 시도해도 중복 생성하지 않는다.
- 일반 상담일지를 덮지 않는다. `회차별 요약`에는 마지막 상담과 `상담 종결`이 **두 항목**으로 보인다.
- 내용은 종결 사유, **미완료 과제** 처리, 확인 항목이다. 실제 상담을 한 것처럼 기록하지 않는다.

---

## 6. 화면 구성 (요구 36건 반영 후)

### 6-1. 15초 다시보기 — 6구획

1. 위험 신호 (상시 표시, 접히지 않음)
2. 당사자 카드 + `오늘 만나기 전 꼭 기억할 것` (유지, 요구 6)
3. 목표 — **전체 상담 목표** + **오늘 상담 목표**(직전 회차에서 쓴 `다음 상담 목표`가 여기 뜬다). 둘 다 없으면 이 구획을 뺀다. 읽기 전용이며 수정 진입만 제공한다
4. 직전 회차 요약
5. 오늘 물어볼 것
6. 확인할 과제

**제거**: `이번 상담 목표`(요구 7), `일치하지 않는 기록`(요구 11). 톺아보기 시절의 7항목이 아니다.

### 6-2. 상담 기록하기 — 5구획

2026-09-16 사용자 피드백 `릴레이어_상담기록하기_수정요청.md` 기준이다.

1. 오늘 상담 내용 (**유일한 필수**; 상담 일시·방식·장소 포함)
2. 수행할 과제
3. 다음에 물어볼 것
4. 실무자 의견
5. 다음 상담 목표

`달라진 것`과 그 영역 선택은 제거한다. 전체 상담 목표는 이 화면에서 수정하지 않는다.
이미 저장된 변화 카드와 전체 목표는 남기며, 인테이크에서는 전체 상담 목표를 계속 편집할 수 있다.
상담 장소는 대면일 때만 표시한다. 음성 비교는 다섯 작성 구획과 별도의 접이식 기능으로 유지한다.

**기존 제거 항목**: 상담 유형(13), 위기도(18), 추가 기록(20), 과제 기한(15), 수행 주체 선택(16).

### 6-3. 상담 일정 등록 — A안 (2026-09-17 Q 승인)

| # | 구획 | 내용 |
|---|---|---|
| 진입 | **당사자 선택** | 메뉴에서는 당사자 목록에서 카드를 고른다. 인테이크에서 이어 들어오면 해당 사례를 쓴다 |
| 1 | **언제 상담하나요?** | 날짜 선택 대화상자 + 오전·오후 / 시 / 분. `선택 완료`는 날짜만 적용하며 아직 저장하지 않는다 |
| 2 | **상담 내용** | 상담 방식, 대면 상담 장소, 메모, 종결 상담 선택. 메모는 카드가 되지 않는다 |
| 하단 | **일정 저장** | 선택한 일시를 확인하고 예정 회차를 저장한다 |

이 화면은 Q가 선택한 A안의 구획명을 따른다. 이전의 명사형 구획(`일시와 상담 방식`·`메모`) 규칙을 이 화면에 한해 대체한다.

**`상담 방식` vs `상담 장소` vs `선호 상담 방식`** — 세 층을 섞지 않는다.

| 말 | 담는 것 | 어디 |
|---|---|---|
| **상담 방식** | 대면 · 방문 · 전화 · 화상 | 상담 일정 등록, 상담 기록하기, 인테이크 운영정보 |
| **상담 장소** | 어디서 만났는지 (자유 입력) | 대면일 때만 |
| **선호 상담 방식** | 당사자가 선호하는 형식. 수식어 `선호`로 구분한다 | 인테이크 3-4 (베타 밖) |

근거: 2026-09-14 Q 확정. 프로토타입 문구 사전(`copy.js:283,397`)과 일치한다. `PRD/intake-questionnaire-v1.md:64`와 2026-09-12 재구성 문서의 `상담 방법`은 구 표기이며 화면에 쓰지 않는다. 당사자 선호 축은 `선호`를 붙여 구분한다.

**종결 상담** 선택을 둔다. 고르면 일반 상담 기록 흐름을 그대로 따르고, **저장 성공 뒤** `상담 종결` 화면으로 이동한다. 저장 실패 시 이동하지 않고 사례를 닫지 않는다(요구 5).

> 구현 2026-09-15: `sessions.is_closing`. 일정 등록에서 미리 고르거나 기록 화면에서 그 자리에서 고른다(기록 화면이 일정의 표시를 이어받고, 바꿀 수도 있다). 회차 종류(`kind`)를 늘리지 않았다 — 종결 상담도 평범한 상담이고, 사례를 닫는 일은 `case_closures` 가 맡는다. 저장 단추 문구가 `저장하고 종결로` 로 바뀌어 다음에 무엇이 오는지 미리 알린다.

### 6-4. 당사자 정보 — 탭 4

| 탭 | 내용 |
|---|---|
| 회차별 요약 | 회차 목록 + 원본 보기 |
| 15초 다시보기 | 가장 최근 것을 여기서 본다 (구 `경과 리포트` 자리) |
| 목표 | 전체 상담 목표 + 날짜별 오늘 상담 목표 |
| 정보 | 기본 정보 + **상담 종결 버튼**(누르면 `상담 종결` 화면) |

**제거**: 당사자 카드(요구 25 — 15초 다시보기의 카드는 유지), 할 일 전체(28).

### 6-5. AI가 정리한 내용 검토하기

- 버튼은 **승인 / 수정** 두 개. 반려·재생성 삭제(요구 21). 수정은 사람이 직접 문구를 고치는 것이며, 수정본도 **별도 승인 전에는 초안**이다
- 수정 가능: 요약·핵심 한 줄·과제 후보·다음 질문 문구
- 화면 문구는 **마스킹**이다(2026-09-16 Q). `가림`·`가림 처리`는 쓰지 않는다. 단, 동의 문안 본문은 해시에 묶여 있어 판을 올릴 때 함께 고친다
- **내용 불일치**: 지난 회차와 이번 회차가 서로 어긋나는 사실. 양쪽 원문을 그대로 보여 주고 판정하지 않는다. 검토 화면과 회차별 요약에 접힌 카드로 선다
- 수정 불가(잠금): 원문·근거 인용·출처 hash·불일치 판정
- `후속 제안` → **수행할 과제** + **다음에 물어볼 것** 둘로 분리(요구 22)
- `대조` → **음성·수기 기록 불일치** + **회차간 기록 불일치** 둘로 분리(요구 23)

### 6-6. 일정

`오늘 할 일` → **할 일**(1), `검토할 AI 초안` → **초안 검토**(2), `맞음·틀림 미정 위험 신호` 표시 **제거**(3 — 다른 화면의 확정 위험 신호나 저장 이력은 그대로).

### 6-7. 내 정보 / 설정

| 묶음 | 내용 | 권한 |
|---|---|---|
| 내 정보 | 표시 이름·로그인 이메일·비밀번호 수정만. 배정 수락 제거 | 본인 |
| 설정 › 개인 보안 | 추가 인증(MFA) | 본인 |
| 설정 › 기관 운영 | 기관 정보, 사용자·역할, 실무자 초대, 기관 준비, 담당 배정 확정 | 관리자 |
| 설정 › AI 연결 설정 | 설치·AI·STT·장비 상태 조회와 재확인 | 관리자 |
| 설정 › 상담 기록 처리 현황 | 기억 설정과 처리 건수 | 관리자 |
| 설정 › 열람 기록 | 감사 사건 조회 | 관리자 |
| 설정 › 개인정보 관리 | 보유기간, 보존·파기 검토 | 관리자 |

기관·역할은 본인이 바꾸지 않는다.

---

## 7. 인테이크 — 2026-09-16 사용자 피드백

현행 화면 순서는 SPEC §9를 따른다. 실제 상담 방식과 선호 상담 방식은 별개다.

| 질문 | 선택지/입력 | 저장 키 |
|---|---|---|
| 수급자 여부 | 기초생활보장수급 / 차상위계층 / 해당 없음 (단일) | `welfare_status` |
| 수급 유형 | 기초생활보장수급일 때만 생계·의료·주거·교육급여 (복수) | `welfare_benefit_type` |
| 기타 공적급여 | 기존 자유 입력 | `welfare_other` |
| 선호 상담 방식 | 대면 / 전화 / 화상 / 기타(이메일, SNS 등) (단일) | `preferred_counsel_method` |
| 그 밖의 상황과 연계가 필요한 내용 | 자유 입력 | `resource_link_detail` |
| 이전에 받은 지원 | 자동 높이 글 칸 | `previous_support_detail` |
| 강점과 도와줄 사람 | 자동 높이 글 칸 | `strength_detail` |

구 수급 여부 두 칸, 어려움 영역과 세부 질문, 도움 우선순위, 참여 여건, 실무자 판단과 다음 단계는 화면에서 제거한다.
과거 응답은 지우지 않으며 모호한 옛 답을 새 선택지로 추정하지 않는다. 전체 상담 목표는 사례의 `overall_goal`이 정본이다.

아래 §7-1·§7-2는 이전 피드백의 이력이다. 현행 입력 규칙과 충돌하면 이 절을 우선한다.

### 7-1. 2026-09-15 Q 지적으로 고친 말과 입력 (근거 있음)

| 전 | 후 | 왜 |
|---|---|---|
| `이전 지원 경험` | **`이전에 받은 지원`** / 칸 이름 `다른 기관에서 받았거나 신청한 지원` | 정본 예시("2025년 주민센터 긴급복지 상담, 소득기준 초과로 미지원")가 가리키는 것은 **다른 기관의 서비스 이용·신청 이력**이다. 우리 기관 상담 참여 이력이 아니다. 표준 서식 용어는 `기존 서비스 이용 및 지원 이력`이며, 그 뜻을 실무자 말로 옮겼다 |
| `강점과 비공식 자원` | **`강점과 도와줄 사람`** | `비공식자원`은 **실제 표준 용어가 맞다** — 통합사례관리는 자원을 공식(기관·제도)과 비공식(가족·이웃·지인)으로 나눈다. 다만 이 칸이 실제로 받는 것은 `도움을 요청할 사람` + `본인의 강점`(I-07 병합)이므로 화면에서는 읽히는 말로 부른다. 저장 키(`strength_detail`)와 표준어는 코드 주석에 남겼다 |
| `1순위/2순위 지원욕구` 라디오 24개 | **드롭다운 2개**, 1순위로 고른 값은 2순위 목록에서 빠짐 | 이름은 표준대로 둔다(업무안내가 우선순위를 별도 항목으로 두고 대상자와 합의해 정하게 한다). 같은 욕구를 1·2순위에 겹쳐 적을 수 없어야 한다. 1순위를 나중에 바꿔 겹치면 2순위를 비운다 |
| `주요 연락 가능 시간` 라디오 | **체크박스** | 평일 오전과 저녁이 둘 다 가능한 사람이 있다 |
| `참여 방해요인` 라디오 + `복수 요인` 선택지 | **`참여 제약사항`** 체크박스, `복수 요인` 삭제 | `복수 요인`은 한 가지만 고르게 해 놓고 둘 이상일 때 쓰라던 우회다. 여러 개를 고를 수 있으면 필요 없다. **이름은 2026-09-16 Q 확정으로 정본(CCC PRD 4-1)에서 벗어난다** — `방해`는 당사자를 탓하는 말로 읽히고 `제약`은 상황을 가리킨다. 저장 키 `participation_barrier` 는 그대로다 |
| `상담 신청·유입 경로` 7지 | **5지**(`가족·지인 소개`·`기존 이용자 재상담` 삭제) | 재상담은 어디로 들어왔는지가 아니라 **관계 상태**다. 이 질문의 답이 아니다 |
| `주요 연락 가능 시간` 한 줄 6지 | **`연락 가능 요일` + `연락 가능 시간대` 두 축** | 정본은 `평일 저녁`은 있는데 `주말 오전`은 못 적는 비대칭이다. 요일과 시간대를 갈라 그 구멍을 메운다 |
| (없음) | **`상담 장소`를 1-3 에 추가** | 상담 방식 바로 아래다. 어디서 만나는지는 어떻게 만나는지와 한 덩어리인데 신청 단계에 적을 자리가 없었다(2026-09-16 Q) |
| `상담을 신청한 사유` 9지 | **5지**(경제·재무 · 부채 · 일자리·소득 · 주거 · 기타) | 사유를 돈과 집으로 좁힌다 |
| (없음) | **`필요 자원 연계`**(건강·의료 · 심리·정서 · 법률·행정 · 가족 · 안전 · 기타) | 사유에서 갈라져 나왔다. 사유가 "무엇 때문에 왔나"라면 이것은 "무엇으로 이어 줄까"다. 둘은 세트로 선다(2026-09-16 Q 확정). **`현재 어려움 관련 영역`(국가 표준 11영역)과는 다른 축이다** — 11영역은 세부 질문 모듈의 분기이자 희망이음·행복e음 호환 분류이고, 이 둘은 접수 단계의 사유와 연계처다 |
| `선호 상담 방식` 라디오 + `혼합` 선택지 | **체크박스**, `혼합` 삭제 | 같은 우회다 |
| `상담을 신청한 주된 사유는 무엇인가요?` 라디오 + `복합적인 어려움` | **`상담을 신청한 사유`** 체크박스, `복합적인 어려움` 삭제 | 같은 우회다(2026-09-15 Q 확정). 여러 개를 고를 수 있으면 그것이 곧 복합이고, 무엇이 복합인지까지 남는다. 질문형 라벨도 명사로 줄였다 |

**단일 선택으로 남긴 것**: 이번 회차의 `상담 방법`(한 번의 상담은 한 방식), `상담 신청·유입 경로`(주된 경로 하나), 수급 여부·차상위(배타), 긴급도·다음 단계.

### 7-2. 2026-09-15 Q 지적 4건 (2차)

| 전 | 후 | 왜 |
|---|---|---|
| 과제 결과 4종 `했음 / 진행 중 / 못 함·계속 / 못 함·그만둠` | **`진행 전 / 진행 중 / 완료`** 3종 + `이 과제 그만두기` 체크 | 네 개 중 둘이 `못 함`으로 시작해 읽는 데 시간이 걸렸다. 그만두는 것은 **상태가 아니라 과제를 접는 일**이라 라디오에서 뺀다. 저장값은 그대로다(`진행 전`=not_done·continue, `완료`=done) |
| `추가`를 눌러야 저장 | **적어만 둬도 저장** | `추가`는 항목을 하나 더 만드는 버튼이지 저장 버튼이 아니다. 한 줄만 쓰는 사람은 누를 이유가 없다 |
| 기록 화면에 `종결 상담` 구획 | **왼쪽 레일의 체크 하나** | 여섯 구획과 나란한 한 챕터를 차지할 무게가 아니다 |
| 저장한 회차는 못 고침 | **회차별 요약 › `수정`** | 결과는 지우지 않고 같은 (카드, 회차) 에 새 행을 쌓아 **마지막 행이 유효**하다(`migrations/0005`). 잘못 누른 것도 기록에 남는다 |

**넓은 화면은 두 칸까지다.** 인테이크와 15초 다시보기는 이식 CSS 의 auto-fit 그리드(`.card-grid`·`.briefing-cards-grid`)를 쓰되 `--grid-min` 을 560 으로 올려 3열을 막는다(2026-09-15 Q). 읽는 순서가 정해진 화면에서 3열은 눈이 Z 자로 튄다. 좁은 화면은 그대로 한 칸이다.

**상담 장소는 상담 방식 바로 아래다.** 일시·방식·장소가 한 묶음이고 장소는 대면일 때만 나온다(요구 14). 상담 내용 밑에 두면 방식과 떨어져 왜 나타났다 사라지는지 읽히지 않는다(2026-09-15 Q). 기록 화면은 예정 회차가 있어도 이 카드를 보여 준다 — 실제로 만난 시각·방식이 잡아 둔 것과 다를 수 있다.

**선택창에는 꺽쇠가 있어야 한다.** 이식 CSS 가 `appearance:none` 으로 네이티브 화살표를 끄므로(`wire.css` "select 는 네이티브 화살표를 끄고 꺽쇠를 직접 그린다") SVG 꺽쇠를 같이 그리지 않으면 **입력칸으로 보인다**(2026-09-15 Q). 꺽쇠는 CCC `apps/web/app/components/wire/chevron.tsx` 를 그대로 이식한 `Chevron` 하나뿐이며, `Field control="select"` 가 자동으로 붙인다.

**글자 버튼은 알약이다.** 이식 CSS 의 `.wire-repeat-actions > .wire-button` 은 CCC 의 `+`·`-` **아이콘 버튼**용이라 너비를 알약 높이로 고정한다 — 글자 버튼에 씌우면 정원이 된다(2026-09-15 Q). 반복 입력의 `추가`·`삭제` 는 그 래퍼를 쓰지 않는다.

**반복 입력은 입력칸과 버튼이 한 행이다**(`DESIGN-RULES.md` §3 "번호 없이 입력칸·버튼 순서로 가로 배치", 2026-09-15 Q 재지적). 라벨 행을 위에 두면 버튼이 라벨 옆으로 올라붙는다. 카드 제목이 이미 이름을 말하므로 행 라벨은 접근성 이름(`aria-label`)으로만 남긴다.

**근거**: 2025·2026 희망복지지원단 업무안내(통합사례관리 공식·비공식자원 구분, 서비스 제공계획의 우선순위 항목), CCC `PRD/intake-questionnaire-v1.md` §3-1·3-2·3-4·4-1.
- 인테이크에 **`수행할 과제`**를 둔다(2026-09-15 예행연습에서 발견). 이름·동작은 상담 기록하기의 것과 같고, 저장하면 다음 회차의 `확인할 과제`로 올라간다.
- 화면에 쓰는 소절 이름에서 정본 문서의 절 번호(`1-2.`)를 뗀다. 구 모듈 `2-1~2-5`가 표준 영역 모듈로 대체되고 `3-3`·`4-2`가 빠지면서 번호가 띄엄띄엄해져 화면이 고장 난 것처럼 읽힌다. 번호는 `intake-questions.ts` 데이터에 정본 대조용으로 남는다.

### 7-3. 2026-09-18 Q 실무자 검측 피드백 (버그리포트·UI 수정요청 시트)

| 전 | 후 | 왜 |
|---|---|---|
| `고쳐 쓰기` | **`수정`** | 짧고 흔한 말. 경로 `/edit`·변수 `editing` 은 그대로 |
| `지우기` | **`삭제`** | 같은 이유 |
| `파일 올리기` | **`파일 업로드`** | 같은 이유 |
| 탭 `회차별 전문 보기` · 버튼 `전문 보기` · 화면 `상담 내용 원문 보기` | **`회차별 원본 보기` · `원본 보기` · `상담 내용 원본 보기`** | `전문`·`원문`이 섞여 있었다. 동의서 문안의 `전문 보기`(문장 전체 펼침)는 뜻이 달라 그대로 |
| 과제 상태 `그만둠` | **`중단`** | 상태 어휘는 명사형: `진행 전`·`진행 중`·`완료`·`중단`. 행동 체크박스 `이 과제 그만두기`만 동사형 |
| 닫힌 카드는 안 보임 | **레일 아래 `완료한 과제 N`·`확인한 질문 N` 접힘 카드** | 완료한 걸 돌아볼 자리가 없었다. 기본 접힘, 읽기만 |

---

## 8. 삭제된 말 (되살리지 않는다)

`톺아보기` · `15초 페이지` · `케이스` · `사례 등록`(정본에 없던 말) · `참여할 사업` · `상담 방법`(→ 상담 방식) · `사례 종결` · `종결일지` · `장기 목표` · `단기 목표` · `이번 상담 목표` · `남은 할 일` · `다음까지 하기로 한 것` · `담당자` · `위기도` · `상담 유형` · `추가 기록` · `일치하지 않는 기록`(15초 다시보기에서만) · `경과 리포트`(탭 이름으로서) · `감사 기록` · `기관 상담 기억` · `연결 상태` · `환경설정`

**단, 자료를 가리키는 일반 문장은 일괄 치환하지 않는다.** "첫 상담 기록이 있는 사업", "저장된 첫 상담 기록"처럼 자료 자체를 부르는 말, 상담 유형·기록 형식 같은 일반 명사, 과거 결정 문서의 역사 기록은 그대로 둔다. 바꾸는 것은 **페이지 제목·메뉴·진입 버튼·그 제목을 가리키는 안내문**이다.

---

## 9. 화면에서 지운다고 자료를 지우는 것이 아니다

위기도·추가 기록·상담 유형·기한·수행 주체·일치하지 않는 기록은 **입력과 표시에서만** 사라진다. 이미 저장된 값, 원문, 감사 이력은 보존하고, 빠진 값을 `안정`이나 `변화 없음`으로 추정해 채우지 않는다. 구 버전 기록은 그 당시 의미로 읽는다.

---

## 15. 설정하기 이름 (2026-09-16 Q)

| 쓰는 말 | 안 쓰는 말 | 왜 |
|---|---|---|
| **설정하기** | `워크스페이스 설정하기` | `워크스페이스`는 대시보드를 뜻하는 말이라 화면에서 쓰지 않는다(2026-09-16 Q) |
| **실무자 관리** › **담당 배정하기** · **실무자 초대하기** · **실무자 목록** · **담당 배정 요청하기** | `담당자 …` | **역할은 `실무자`, 관계는 `담당`**(2026-09-16 Q 3차 검토). 아래 §15-1 참조 |
| **화면 테마** | `테마 설정`, `환경설정` | `환경설정`은 폐어다(§14). `설정 › 화면 테마`로 자리와 이름이 함께 선다 |
| **계정 삭제하기** | `탈퇴` | Q 지시. 실제 동작은 로그인 차단이라 화면이 그 사실을 그대로 말한다 |
| **API 연결 관리** | `AI 연결 설정`(정본 §6-7) | AI·STT·데이터베이스 셋을 한자리에서 보므로 `AI` 하나로 부르면 좁다(2026-09-16 Q) |

### 15-1. 왜 `담당자`가 아니라 `실무자` + `담당`인가

정본 D40·ADR-0017(2026-07-28 그릴링)이 `상담사 → 실무자`, **`담당자 → 담당 실무자`** 로 정했다.
`담당`을 없앤 것이 아니라 **수식으로 내린 것**이다. 홀로 선 `담당자`가 폐어인 이유는
그 말이 서로 다른 둘을 뭉뚱그리기 때문이다.

| 뜻 | 쓰는 말 | 어디에 |
|---|---|---|
| 직업·역할 — 상담을 수행하고 기록하는 사람 | **실무자** | `실무자 초대하기` · `실무자 목록` · `users.role='worker'` |
| 관계 — 이 사례를 맡은 사람 | **담당**(`담당 실무자`) | `담당 배정하기` · `담당 배정 요청하기` · `담당 없음` · `assigned_user_id` |

**초대·목록에서 `담당자`는 사실과 다르다.** 방금 초대한 사람은 아직 아무도 맡지 않았고,
목록에는 `맡은 당사자 0명`인 사람이 선다. 그를 `담당자`라 부르면 화면이 거짓말을 한다.

가르면 화면이 한 가지를 저절로 말한다 — **기관에 있는 것과 이 사람을 맡은 것은 다르다.**
`계정 삭제하기`가 그 구분 위에 서 있다: "맡고 있는 당사자가 N명 있어요. 다른 실무자에게
넘긴 뒤에 나갈 수 있어요." 넘기는 것은 **담당**이고, 넘긴 뒤에도 남는 것은 **실무자** 자격이다.

정본에 이미 `담당 배정 요청`(§6-1)·`담당 배정 확정`(§6-7)이 있다. 새로 지은 말이 아니다.
상담 기록의 `실무자 의견`(§5)도 그대로다.
