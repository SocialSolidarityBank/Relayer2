# 릴레이어 v2 구현 명세 (베타)

- 작성일: 2026-09-14 · 개정 2: 2026-09-13 PLANNER 확정(표시명 9건 · 후속 요구 36건 · 다섯 결정 · 인테이크) + 2026-09-14 Q 확정 5건(목표 표시·목표 수정·역할 3종·비교 기준·상담 종결) 반영
- **이름은 `GLOSSARY.md`가 정본이다.** 이 문서는 동작 계약만 정한다. 일정·범위는 `PLAN.md`, 실행 계약은 `.ouroboros/seed-beta.yaml`.
- 근거: CCC `.worktrees/PLANNER/docs/superpowers/plans/2026-09-13-planner-handoff.md`(이하 **요구 NN** · **I-NN**), `inbox/2026-09-12/relayer-definitions-v2-2026-09-12.md`(**정의서**), `handoff-decisions-2026-09-12.md`(**결정 NN**)

---

## 1. 베타 화면 7개

> 2026-09-16 검수: 이 절은 **베타 출발선**을 적은 것이고 지금은 화면이 더 있다.
> 설정하기(`#/settings/*` 네 묶음), 초대 수락(`#/invite/:token`)이 뒤에 붙었다.
> 현재 구조는 §18·§20·§21 이 정본이다.

| 화면 | 경로 | 하는 일 |
|---|---|---|
| 상담 일정 | `/schedule` | 홈. 월간이 기본이며 주간·일간으로 전환한다. 표시 기간의 예정 회차를 조회하고 날짜별 상세에서 다시보기로 이동한다. 열린 과제·질문 수를 함께 보여 준다 |
| 당사자 목록 | `/participants` | 이름순 카드에 사업·담당자·기록 회차·다음 일정을 펼쳐 표시한다. 이름·가명·사업 이름으로 거르고, 카드를 누르면 당사자 정보로 이동한다. 기록·일정 선택 화면에서는 해당 작성 화면으로 간다 |
| 당사자 등록 | `/participants/new` | 당사자 + PII 금고 + 참여 사업. 사례를 여는 유일한 자리다 |
| 인테이크 작성하기 | `<case>/records/intake` | 사용자 피드백의 9개 구획(§9). 실제 상담 일시·방식과 선호 방식을 분리한다. 다시 열어 수정하며 첫 회차를 중복 생성하지 않는다 |
| 상담 일정 등록 | `<case>/schedule` | A안: 선택 완료 → 오전·오후/시/분 입력 → 일정 저장. 예정 회차 1건 + 메모 |
| 상담 기록하기 | `<case>/records/new` · 고쳐 쓰기 `<case>/sessions/:id/edit` | 오늘 상담 내용·수행할 과제·다음에 물어볼 것·실무자 의견·다음 상담 목표의 5구획. 일시·방식·장소는 첫 구획에 둔다. 전체 상담 목표와 과거 변화 기록은 이 화면에서 편집하지 않고 보존한다 |
| 15초 다시보기 | `<case>/briefing` | 읽기 전용 6구획 |
| AI가 정리한 내용 검토하기 | `<case>/sessions/:id/review` | 초안을 사람이 보고 **승인 / 수정**. 승인 전에는 기록이 아니다(P3) |
| 당사자 열람 | `/access/<링크>` | **로그인하지 않는다.** 링크와 여섯 자리 코드를 지나면 기본정보와 앞으로의 일정만 본다(P2) |
| 당사자 정보 | `<case>/info` | 탭 4: 회차별 요약(원문 보기) · 15초 다시보기 · 목표(전체 목표와 이력) · 정보(기본 정보 + 종결 버튼) |
| 상담 종결 | `<case>/close` | 종결 사유 + 남길 말. 미완료 과제는 보여 주기만 하고 자동 처리하지 않는다 |

**일정 저장 경계 (2026-09-17 Q, A안).** 날짜 대화상자의 `선택 완료`는 폼 값만 바꾸고 서버에 쓰지 않는다.
`취소`·Escape는 선택 전 날짜를 보존한다. `일정 저장`에서만 기존 `POST /cases/:id/sessions`를 호출한다.
시간은 화면에 명시한 한국 시간(UTC+09:00)으로 해석하여 ISO 시각으로 보낸다. 저장 중에는 입력·중복 제출을 막고,
실패하면 입력값과 오류를 보존한다. 성공하면 기존과 같이 해당 사례의 `15초 다시보기`로 이동한다.

**전체 일시 입력 공통화.** 일정 등록, 상담 기록하기와 고쳐 쓰기, 인테이크 작성과 고쳐 쓰기는 같은 A안 입력을 쓴다.
저장된 ISO 시각을 한국 시간의 날짜·오전/오후·시·분으로 불러오며, 날짜 선택·취소만으로 회차를 시작하거나 저장하지 않는다.
표시는 분 단위지만 일시를 바꾸지 않고 다른 내용만 저장하면 원래 시각의 초·밀리초도 보존한다.
날짜·시간 일부만 입력한 값은 저장 전에 차단한다. 인테이크의 일시 전체가 비어 있는 경우에만 기존 API의 생략 규칙을 유지한다.

**상담 일정 보기 (2026-09-17 Q).** 메뉴 이름은 `상담 일정 보기`, 페이지 HERO는 `상담 일정`이다.
처음 열면 한국 날짜의 현재 월을 보여 준다. 월간은 월요일부터 일요일까지의 달력이고, 주간·일간은 시작 시각 기준 시간표다. 주간의 일요일~토요일 범위는 유지한다.
종료 시각이 없는 데이터에 임의의 상담 길이를 붙이지 않는다. 맨 왼쪽 선택창으로 보기를 바꾸며 월간·주간은 이전·다음, 일간은 공통 날짜 선택창으로 이동한다. `오늘` 이동은 세 보기에서 제공한다.
조회에는 기존 `GET /schedules?from&to`를 쓰며 월간의 인접 달 표시 칸까지 포함한다. 담당 사례 필터는 서버의 기존 규칙을 유지한다.
월간 셀에서 생략된 일정을 포함해 선택한 날짜의 전체 일정을 아래 목록에서 볼 수 있다. 다른 기간으로 이동한 뒤 도착한 옛 응답은 버린다.

**목록 검색은 화면이 한다.** 이름은 금고 암호문이라 서버가 `where name like` 를 걸 수 없다. 목록을 받아 브라우저에서 거른다 — 기관 하나 규모(수백 건)라 이 대가를 받아들인다. 수천 건이 되면 검색용 색인(예: 이름 해시)이 필요하다.

베타 밖: 당사자 초대, AI가 정리한 내용 검토하기, 내 정보·설정 7묶음. 이름과 구성은 `GLOSSARY.md` §6에 이미 고정해 둔다.

---

## 2. 카드

### 2-1. 생성

| 카드 | 어느 구획에서 | `source_section` |
|---|---|---|
| `fact` | 과거 인테이크·상담 변화 기록을 보존. 현재 두 작성 화면에는 신규 변화 입력 칸이 없음 | `intake` / `change` |
| `question` | 인테이크 `다음에 물어볼 것`, 상담 기록하기 [3] `다음에 물어볼 것` | `intake` / `question` |
| `promise` | 인테이크 `수행할 과제`, 상담 기록하기 [2] `수행할 과제` | `promise` |
| `judgment` | 상담 기록하기 [4] `실무자 의견`, 위험 신호 표시 | `judgment` |

- 모든 카드는 **출처 회차**(`source_session_id`)와 출처 구획을 갖는다. 상담 일정 등록의 `메모`는 카드가 아니다 — 목표·질문 전용 입력과 구별해 회차 메모로 보존한다(요구 4).
- 과제 입력에 **기한과 수행 주체가 없다**(요구 15·16). 숨긴 채 기본값을 채우지 않는다.
- 베타의 `source_type`은 `manual`만. `ai_approved`는 P3.
- 실무자는 분류를 고르지 않는다. 구획이 곧 분류다.
- **인테이크를 고쳐 써도 결과가 찍힌 카드는 지우지 않는다**(2026-09-15). 다른 회차에서 `확인함`·`못 함`이 붙은 카드를 지우면 그 이력까지 사라진다. 고쳐 쓰기는 결과 없는 카드만 갈아 끼운다.
- `card_outcomes`·`goal_revisions` 의 append-only 트리거는 **행 단위**다(`migrations/0004`). 문장 단위였을 때는 지울 행이 하나도 없어도 발동해, 결과 없는 카드를 지우는 cascade 까지 막았다.
- **인테이크에도 `수행할 과제`가 있다**(2026-09-15 예행연습). 첫 상담에서도 "다음까지 서류 떼어 오기" 같은 약속은 나오는데 적을 자리가 없었고, 그러면 2회차 레일이 비어 `못 함 · 계속`을 누를 일이 생기지 않는다. 약속 카드가 제품의 핵심인데 1회차만 예외일 이유가 없다.

### 2-2. 결과와 개폐

`card_outcomes(card_id, session_id, result, follow, reason)` — append-only. **`UNIQUE(card_id, session_id)` 는 2026-09-15 에 뺐다**(`migrations/0005`): 저장한 회차를 고쳐 쓸 수 있어야 하는데, 지우거나 덮어쓰면 append-only 가 깨진다. 같은 (카드, 회차) 에 새 행을 쌓고 **마지막 행이 유효**하다. 화면 어휘는 `진행 전`·`진행 중`·`완료` 셋이며 `그만두기`는 따로 둔다(`GLOSSARY.md` §7-2).

| result | 화면 라벨 | 카드 상태 |
|---|---|---|
| `done` | 했음 | 닫힘 |
| `in_progress` | 진행 중 | 열림 |
| `not_done` + `continue` | 못 함 → 계속한다 | 열림 |
| `not_done` + `stop` + `reason` | 못 함 → 그만둔다(사유 필수) | 닫힘 |
| `confirmed` + `note` | 확인함 (질문 카드) | 닫힘 |
| `unchecked` | 이번에 확인 안 함 | 열림 |

- 회차 저장(`PATCH /sessions/:id`)은 **한 트랜잭션**에서 제출된 결과를 쓰고, 제출되지 않은 열린 카드 전부에 `unchecked`를 넣는다. 결과 전용 API는 없다.
- `cards.closed_at`을 두지 않는다. 열림·닫힘은 최신 결과에서 파생한다.
- 파생 사실 카드는 저장하지 않고 읽기 시점에 조립한다.
- **묵시적 완료를 만들지 않는다.** 기한이 없으므로 시간 경과로 과제를 닫지 않는다.

---

## 3. 15초 다시보기 계약

저장물이 아니라 조립 결과다. `GET /cases/:id/briefing`은 고정 개수 쿼리로 만든다(카드마다 조회 금지).

```ts
type CheckStatus = {
  state: 'checked' | 'not_checked' | 'checking';
  reason: 'ai_disabled' | 'no_recording' | 'pending' | null;
  through_session_seq: number | null;
};
```

| # | 구획 | 비면 | 베타(AI 꺼짐) |
|---|---|---|---|
| 1 | 위험 신호 | **빼지 않음**, 접히지 않음 | `not_checked / ai_disabled / null` + 실무자가 표시한 것만 |
| 2 | 당사자 카드 + `오늘 만나기 전 꼭 기억할 것` | 항상 | 이름·사업·`지금 회차/총 회차`·오늘 일정과 방법 |
| 3 | 목표 | 둘 다 없으면 **뺀다** | `전체 상담 목표`(있으면) + `오늘 상담 목표`(직전 회차의 `다음 상담 목표`를 이어받은 값). 하나만 있으면 그것만. 없는 쪽은 **목표 없음** |
| 4 | 직전 회차 요약 | **빼지 않음** | `summary_state='none'` + 원문 보기 |
| 5 | 오늘 물어볼 것 | **뺀다** | 열린 `question` 카드 전부, 오래된 순, 출처 회차 표시 |
| 6 | 확인할 과제 | **뺀다** | 열린 `promise` 카드. 위험 관련 상단 고정, 나머지 오래된 순. 지난 회차 `unchecked` 건수 별도 표시 |

- 구 7항목 중 **`이번 상담 목표`와 `일치하지 않는 기록`은 없다**(요구 7·11). 불일치는 AI 검토 화면 안에서 두 종류로 나뉘며(요구 23) 여기 오지 않는다.
- "없음"과 "안 봤음"을 같은 모양으로 보이면 안 된다. 사람이 적는 항목(3·5·6)은 빼고, AI가 관여하는 항목(1·4)은 남기고 상태를 쓴다.
- 모든 표시값에 회차 번호를 붙인다. "마지막 확인 N회차"를 쓰고 "그대로"라고 추정하지 않는다.
- **여기서 고치지 않는다**(결정 25). 목표는 고칠 수 있지만(§4-2) 수정 자리는 상담 기록하기와 (P2) 목표 탭이며, 이 화면은 그 자리로 보내는 진입만 제공한다.
- 개수가 많으면 상단 압축 + 건수 + 전체 목록. 안전 관련은 압축 예외.

---

## 4. 목표

### 4-1. 이어받기

```
인테이크 [전체 상담 목표]      ──> 사례 내내 15초 다시보기 목표 구획 위칸
상담 기록하기 [다음 상담 목표] ──> 같은 사례의 다음 실제 상담의 [오늘 상담 목표]
                                   (= 다음 15초 다시보기 목표 구획 아랫칸)
```

- **한 번만** 이어진다. 조회·예약 변경·예약 취소로 소비되지 않는다.
- 출처 회차(`today_goal_from_session_id`)와 수신 회차(`next_goal_consumed_by_session_id`)를 둘 다 저장한다.
- 이전 회차가 없거나 다음 목표를 안 썼으면 **목표 없음**. 옛 목표를 자동 부활시키지 않는다.
- **`상담 종결`은 실제 상담이 아니므로 소비하지 않는다**(§4-3).

### 4-2. 수정 (2026-09-14 Q 확정)

**모든 목표는 고칠 수 있고, 목표가 없는 상태도 정상이다.**

| 대상 | 수정 자리 | 규칙 |
|---|---|---|
| 전체 상담 목표 | 인테이크 작성하기 | 비워둘 수 있다. 고치면 이전 문구를 `goal_revisions`에 남긴다(append-only) |
| 오늘 상담 목표 | 상담 기록하기 (그 회차 기록 중) | 이어받은 원문과 출처 회차는 보존하고, 고친 값을 수신 회차의 값으로 저장한다 |
| 다음 상담 목표 | 상담 기록하기 (아직 소비되지 않은 동안) | 이미 다음 회차가 이어받았으면 그 회차의 오늘 목표를 고치는 것이지 여기를 고치는 게 아니다 |

- **소급 변경 금지.** 오늘 전체 목표를 고쳐도 지난 회차 기록에 찍힌 당시 목표는 그대로다. 15초 다시보기는 항상 **현재 값**을 보여 준다.
- 빈 목표를 문구로 채우거나, 지난 목표를 자동 부활시키지 않는다.

### 4-3. 상담 종결 (회차가 아니다)

```
case_closures(id, case_id UNIQUE, last_session_id, closed_at, close_reason,
              unfinished_note, created_by)
```

- **회차 번호를 받지 않는다.** `N회차/총 M회차` 계산, 회차 수, 회차 한 줄 조립에 들어가지 않는다.
- 마지막 상담(`last_session_id`)에 붙는다. 사례 하나에 **하나**(`UNIQUE(case_id)`)이며 종결 확정과 같은 트랜잭션에서 만든다. 재시도는 같은 결과를 돌려준다.
- 내용은 종결 사유 + **미완료 과제** 처리 + 확인 항목. 상담을 한 것처럼 기록하지 않는다.
- `회차별 요약`에는 마지막 상담과 `상담 종결`이 **두 항목**으로 보인다.
- 미완료 과제와 목표를 자동으로 완료·중단 처리하지 않는다.
- **구현 완료 2026-09-15.** 처음에는 P2로 미뤘으나 표가 이미 있어 화면만 붙이면 됐다. 종결 사유 6종(목표 달성 · 타 기관 의뢰 · 당사자 거부·중단 · 연락 두절 · 이사·전출 · 기타), 종결하면 `support_cases.status = 'closed'`. 같은 사례를 두 번 닫아도 처음 기록을 그대로 돌려준다(`on conflict`).

---

## 5. 회차 한 줄 (AI 없는 회차)

`buildSessionLine(session, cards)` — 저장하지 않고 조립한다.

1. 그 회차에 생긴 `promise` 카드 수 ≥ 1 → `수행할 과제 N`
2. 그 회차에 생긴 `question` 카드 수 ≥ 1 → `물어볼 것 N`
3. `source_section='change'` 카드의 `area`를 9영역 고정 순서로 → `달라진 것: 주거`
4. `실무자 의견`이 있으면 → `의견 있음`

`, `로 잇고 빈 조각은 뺀다. 전부 비면 `수기 기록`. **요약이 아니라 기록 상태 표시**이며 새 문장을 만들지 않는다(결정 22).

초안(2026-09-12)의 규칙에 있던 `위기도`는 입력 자체가 삭제됐으므로(요구 18) 한 줄에서도 뺀다. 200자 요약 층은 AI 승인 요약이라 베타에서는 항상 "요약 없음".

---

## 6. 도메인 값

| 축 | 값 |
|---|---|
| 욕구영역 `area` | **국가 표준 10종 + 기타**: `safety` 안전 · `health` 건강 · `daily_living` 일상생활유지 · `family` 가족관계 · `social` 사회적관계 · `economy` 경제 · `education` 교육 · `employment` 고용 · `living_env` 생활환경 · `legal` 법률·권익보장 · `other` 기타 |

**욕구영역의 근거와 쓰임**(2026-09-14 조사). 릴레이어는 **범용 사례관리 도구**이므로 영역 분류를 발명하지 않고 국가 표준을 따른다.

- 정본: **2024 희망복지지원단 업무안내**의 통합사례관리 욕구사정 10영역(안전·건강·일상생활유지·가족관계·사회적관계·경제·교육·고용·생활환경·법률및권익보장). 각 영역에 하위 항목이 정의돼 있다.
- 민간도 같은 분류를 쓴다. 사회복지관은 **희망이음**에서 상담·욕구조사·사례회의·종결을 수행하고 공공 **행복e음**과 의뢰·연계가 오간다. 같은 영역을 쓰면 의뢰서·사례회의 자료·통계가 구조적으로 호환된다(원문 기록의 자동 연계는 별개이며 법적 근거·동의·권한이 필요하다).
- `기타`는 실무 매뉴얼이 허용하는 catch-all로 유지한다.
- 구 6영역·9영역에서 바뀐 것: `심리·정서`는 표준의 `건강`에 포함(질문지 모듈도 `2-4 건강·심리정서` 하나), `주거`→`생활환경`, `돌봄`→`가족관계`. **안전·일상생활유지·사회적관계·교육**이 새로 들어왔다. 이행은 `migrations/0002_life_areas.sql`.
- 과거 영역 모듈은 표준 하위 구조를 사용했다. 2026-09-16 피드백으로 모듈 입력 화면과 사용되지 않는 `need-areas.ts`를 제거했다.
- 과거 `need_<area>_<sub>`·`need_<area>_detail`, 고용형태·소득 안정성 등의 응답은 저장된 detail 안에 보존한다. 새 인테이크를 저장할 때 숨겨진 답을 지우지 않는다.

영역은 `cards.area`와 기존 기록의 분류를 보존하는 데 쓴다. 2026-09-16 사용자 피드백으로 인테이크의 영역 선택·세부 질문·지원욕구 우선순위 입력은 제거했다. 화면 제거가 과거 분류나 저장 자료의 삭제를 뜻하지 않는다.

**영역 목록은 `api/src/domain/types.ts` 의 `LIFE_AREAS` 하나뿐이다.** 2026-09-15 에 라우트 검증이 옛 9영역 목록을 따로 들고 있어 `생활환경`·`안전` 같은 표준 영역을 저장하면 500 이 났다. 목록을 두 벌 두지 않는다.

**삭제된 입력**: 위기도, 상담 유형, 추가 기록, 과제 기한, 과제 수행 주체, 영역별 상태값(괜찮음·긴장·위기·해당없음·답변거부).
삭제는 **입력과 표시에서만**이다. 기존 저장값·원문·감사 이력은 보존하고 빈 값을 `안정`이나 `변화 없음`으로 추정하지 않는다.

---

## 7. UX 라이팅

해요체(법적 문서는 합니다체) · 버튼은 짧은 동사 · 막힌 기능은 이유와 계속 가능한 행동을 같은 자리에 · 부정은 부정문으로 · 감정어와 느낌표 금지 · 인격은 "차분하고 정확한 동료". 기호 문법(`*`, `?`, `(본인)`) 금지(결정 12). 시간 약속 문구 대신 "상담 내용만 적어도 저장할 수 있어요"(결정 13).

---

## 8. 결정 추적

### 8-1. 2026-09-13 표시명 확정 9건 → 전부 반영 (`GLOSSARY.md` §2)

### 8-2. 2026-09-13 후속 요구 36건

| 요구 | 처리 |
|---|---|
| 1·2·3 일정 화면 | (b) P2 — 이름은 확정해 둠 |
| 4 등록의 메모 통합 | **(a) 베타** §2-1 |
| 5 종결 상담 선택 → 저장 성공 후 종결 화면 | 2026-09-15 구현 (`sessions.is_closing`) |
| 6 15초 다시보기 당사자 카드 유지 | **(a)** §3-2 |
| 7 `이번 상담 목표` 구획 제거 | **(a)** §3 |
| 8 목표 세 층 정리 | **(a)** §4 |
| 9 `오늘 물어볼 것` 유지 + 연결 | **(a)** §3-5 |
| 10 `남은 할 일` → `확인할 과제` | **(a)** §3-6 |
| 11 `일치하지 않는 기록` 제거 | **(a)** §3 |
| 12 `다음에 물어볼 것` 연결 | **(a)** §2-1·§3-5 |
| 13 상담 유형 제거 | **(a)** §6 |
| 14 장소는 대면만 | **(a)** §6 |
| 15 `수행할 과제`, 기한 제거 | **(a)** §2-1 |
| 16 `누가` 선택 제거 | **(a)** §2-1 |
| 17 `담당자 판단` → `실무자 의견`, 담당자→실무자 통일 | **(a)** `GLOSSARY.md` §3 |
| 18 위기도 제거 | **(a)** §5·§6 |
| 19 `다음 상담 목표` 칸 추가 | **(a)** §4 |
| 20 추가 기록 제거 | **(a)** §6 |
| 21·22·23 AI 검토 화면 | (b) P3 — 계약은 `GLOSSARY.md` §6-5에 고정 |
| 24 공적급여 4문항 | **(a)** §9 |
| 25~30 당사자 정보 탭 | (b) P2 — 구성 확정됨 |
| 31·32·33 상담 종결 | (b) P2 |
| 34·35·36 내 정보·설정 | (b) P1(개인 보안·기관 운영) / P2 |

### 8-3. 다섯 결정

배정 효력 = 관리자 확정 즉시 · 종결 기록 = `상담 종결` 기록을 항상 별도로 · 목표 이어받기 = 다음 실제 상담 · AI 직접 수정 = 문구 전반(원문·인용·hash·불일치 판정은 잠금) · 내 정보 수정 = 로그인 정보 포함. 베타에 걸리는 것은 **목표 이어받기 하나**(§4). 나머지는 P1·P2·P3 계약으로 그대로 옮긴다.

### 8-4. 2026-09-12 결정 01~54

`GLOSSARY.md`와 위 표가 덮어쓴 항목(21~22 목표, 30 남은 할 일, 43 화면 이름, 44 이름 규칙) 외에는 유효. 전수 대응은 이 파일 이전 판의 표를 계승하되, **충돌 시 2026-09-13 확정이 우선한다**.

---

## 9. 인테이크 — 사용자 피드백 반영

근거: `릴레이어_인테이크_수정요청.md`, `릴레이어_상담기록하기_수정요청.md`(2026-09-16 제공).
이 절과 GLOSSARY §6-2·§7이 이전 질문지 화면 규칙보다 우선한다.

화면 순서:
1. 상담 일시와 상담 방식
2. 공적급여, 수급자 여부
3. 상담 운영정보
4. 상담 신청 사유 및 필요 자원 연계
5. 이전에 받은 지원
6. 강점과 도와줄 사람
7. 전체 상담 목표
8. 수행할 과제
9. 다음에 물어볼 것

| 항목 | 입력과 저장 |
|---|---|
| 상담 일시 | 달력·시각 입력. `sessions.held_at`; 생성·수정 모두 반영 |
| 실제 상담 방식 | 대면·전화·화상·기타(이메일, SNS 등) 중 단일 선택. `sessions.method` |
| 상담 장소 | 실제 방식이 대면일 때만 표시. 다른 방식으로 변경하면 `sessions.place`는 null |
| 수급자 여부 | 기초생활보장수급·차상위계층·해당 없음 중 단일 선택. `detail.welfare_status` |
| 수급 유형 | 기초생활보장수급을 고르면 생계·의료·주거·교육급여 복수 선택. `welfare_benefit_type` |
| 기타 공적급여 | 기존 자유 입력 유지. `welfare_other` |
| 선호 상담 방식 | 실제 방식과 분리한 단일 선택. `preferred_counsel_method` |
| 유입 경로·연락 가능 요일/시간대 | 무응답 선택지는 제거하고 해당 없음 유지. 기존 키 사용 |
| 상담신청 사유 | 경제·재무·부채·일자리·소득·주거·기타 중 복수 선택. `application_reason`(2026-09-17 최종 요청으로 09-16 다섯 선택지 교체. `주거`는 요청서 표기이며 욕구영역 `생활환경`과 다른 축) |
| 필요 자원 연계 | 건강·의료·심리·정서·법률·행정·가족·안전·기타 중 복수 선택. `resource_link`(09-16에 글 칸으로 대체했다가 09-17 최종 요청으로 선택 문항 복원. 옛 저장값은 새 선택지와 안 맞아도 보존) |
| 신청 배경 | 기존 입력 유지. `application_reason_detail`, 회차 memo에도 반영 |
| ~~그 밖의 상황과 연계가 필요한 내용~~ | 09-17 최종 요청에서 제외. `resource_link_detail` 저장값은 은퇴 문항으로 보존 |
| 이전에 받은 지원·강점과 도와줄 사람 | 글 길이에 따라 늘어나는 textarea. 기존 키 유지 |
| 전체 상담 목표 | `support_cases.overall_goal`; 질문지 JSON에 복제하지 않음 |
| 수행할 과제·다음에 물어볼 것 | 각각 promise·question 카드 |

현재 어려움 관련 영역과 세부 질문, 우선적으로 필요한 도움, 상담 참여 여건,
담당 실무자 판단 및 다음 단계는 작성 화면에서 제거한다.
새 양식으로 고쳐 저장해도 해당 과거 응답과 숨겨진 fact/judgment 카드·결과 연결은 보존한다.
과거 수급을 현재 수급으로, 실제 상담 방식을 선호 방식으로 추정하지 않는다.

`0021_intake_method.sql`은 method에 `other`만 추가한다. 기존 `visit` 행이나 과거 자료를 일괄 변환·삭제하지 않는다.

## 10. 검증

| 대상 | 방법 |
|---|---|
| 카드 생성·분류 없음 | E2E: 구획 입력 → 카드 종류·출처 회차 확인, 분류 선택 UI 부재 |
| `unchecked` 재노출 | 단위: 결과 미제출 회차 저장 → `unchecked` 1행 + 다음 다시보기에 열린 상태 |
| 목표 이어받기 | 단위: `다음 상담 목표` 저장 → 다음 실제 회차에서만 `오늘 상담 목표`로 표시, 예약 취소로 소비 안 됨 |
| 목표 수정 | 단위: 인테이크에서 목표 없이 저장 가능, 상담 중 전체 목표 변경 시 이전 문구가 `goal_revisions`에 남고 지난 회차 기록의 당시 목표는 안 바뀜 |
| 빈 항목 규칙 | 단위: 목표·물어볼 것·확인할 과제가 비면 제외, 위험 신호·직전 요약은 상태와 함께 잔존 |
| PII 경계 | 단위: 금고 암호문만, API 응답·로그·자유 글에 평문 없음 |
| 범위 | 단위: AI·STT 자격증명과 네트워크 없이 전체 흐름 통과, 관련 UI 미노출 |
| 표시명 | 단위: 화면 제목·메뉴·진입 버튼이 `GLOSSARY.md` §2 표와 일치, 삭제 목록(§8) 문자열 부재 |

---

## 10. 동의 (P1, 2026-09-15)

정본은 CCC `docs/specs/S7-consent-six-domains.md`(2026-09-03 확정)다.

> 2026-09-16 검수: 이 절은 **P1 시점**을 적은 것이다. 지금은 일곱 영역을 전부 쓴다 —
> 정본 여섯에 `document_attachment`(서면 문서 보관)가 더 붙었고(§17-2), 문안은
> 표준 양식 v2 다(§21). 해시 원문도 늘었다. 아래 표와 해시 설명은 그 시점 기록이다.

| 영역 | 화면 이름 |
|---|---|
| `personal_data_collection_use` | 개인정보 수집·이용 |
| `sensitive_information_processing` | 민감정보 처리 |

```
consent_events(id, participant_id, case_id, domain, decision, purpose,
               copy_version, copy_hash, effective_at, recorded_by, recorded_at)
```

- **현재 상태를 저장하지 않는다.** 사건을 접어 계산한다 — 마지막 사건이 `grant` 이고 **지금 문안 해시와 같을 때만** `동의함`이다. 문안이 바뀌면 지난 동의는 자동 승격되지 않고 `확인 필요`로 떨어진다.
- 사건은 append-only(행 단위 트리거). **철회도 새 사건**이며 지난 사건을 지우지 않는다.
- `copy_hash` 는 원문을 NFC 정규화해 SHA-256 한 소문자 hex 64자다. 원문은 정본 §2.1 의
  여덟 줄(domain·label·copy·provider 3칸·purpose·retentionDuration)에 **표준 양식 네 줄**
  (items·purposeText·retentionText·refusalText)을 더한 것이다(§21-2, 2026-09-16).
  provider 는 녹음·외부 LLM·음성 보유기간·서면 문서 넷이 채운다.
- 사건 종류는 `grant`·`withdraw`·`decline` 셋이다. 정본의 `correct`(감사용 정정)는 쓰지 않는다 — 고칠 일은 올바른 사건을 새로 쌓는다.
- **빈 값을 동의로 읽지 않는다.** 등록 화면에서 고르지 않은 영역은 `decline` 사건으로 남는다.

### 10-1. 게이트

| 막는 것 | 필요한 동의 | 응답 |
|---|---|---|
| 당사자 등록(사례 열기) | 개인정보 수집·이용 | `400` |
| 인테이크·회차 기록 저장 | 민감정보 처리 | `409` |
| 녹음 저장 | 상담 녹음 + 음성 원본 보유기간 | `409` |
| 전사 | 외부 STT 처리 | `409` |
| AI 초안 | 외부 LLM·국외 처리 | `409` |
| 서면 문서 올리기 | 서면 문서 보관 | `409` |

내려받기·읽기에는 게이트가 없다 — 이미 받아 둔 것을 보는 일이라 동의가 다시 필요하지 않다.
다만 **문서 내려받기는 감사에 남는다**(§17-2).

상담 자유 글에는 건강·채무·주거가 섞인다. 동의 없이 저장하지 않는다. 철회하면 **그때부터** 저장이 막히고 **이미 저장한 기록은 그대로 남는다** — 철회는 지움이 아니다(파기는 별건이며 보유기간 정책의 몫이다).

---

## 11. 열람 기록 (P1, 2026-09-15)

```
audit_log(id, actor_id, action, participant_id, case_id, fields[], at)
```

- **PII 를 실은 화면 조회 1건 = 감사 1행**(CCC D24·D14). 실은 항목이 여럿이어도 행을 쪼개지 않고 `fields` 에 더한다 — "누가 누구 것을 언제 봤나"를 한 줄로 읽기 위해서다.
- **값은 남기지 않는다.** `['name','phone','email']` 처럼 항목 이름만 남는다. 감사 기록이 또 하나의 개인정보 창고가 되면 안 된다.
- 남기는 조회: 당사자 정보(`name`·`phone`·`email` 중 실은 것), 15초 다시보기(`name`),
  당사자 본인 열람, 서면 문서 내려받기, 열람 기록 조회. **목록 조회는 남기지 않는다**
  (2026-09-16, §19-1) — 목록에는 가명과 회차만 실려 남길 값이 없다.
- 동의·철회도 남긴다(`consent.record`) — 누가 언제 받았는지가 곧 증거다.
- append-only(행 단위 트리거). 감사 쓰기가 실패해도 화면 응답은 막지 않되 **서버 로그에 남긴다** — 조용히 사라지면 감사가 아니다.
- 조회는 **관리자만**(`403`). 실무자 화면에는 메뉴가 없다.

---

## 12. 자유 글 암호화 (P1, 2026-09-15)

금고(이름·연락처·이메일)와 **같은 열쇠·같은 포맷**(AES-256-GCM, `v1.iv.tag.body`)으로 사람이 쓴 문장도 암호문으로 앉힌다. 상담 내용에는 건강·채무·주거가 섞인다.

| 암호화하는 것 | |
|---|---|
| `sessions` | `memo` · `plan_memo` · `today_goal_text` · `next_goal_text` · `detail`(인테이크 답 통째로) |
| `cards` | `text` · `quote` |
| `card_outcomes` | `reason` · `note` |
| `support_cases` | `overall_goal` |
| `goal_revisions` | `text` |
| `case_closures` | `close_reason` · `unfinished_note` |

**암호화하지 않는 것**: 분류·상태·시각·영역 코드·회차 번호·가명. 이것들로는 사람을 알아볼 수 없고, 이게 암호화되면 목록·계산·통계가 전부 막힌다.

- **경계는 둘뿐이다.** 쓰기는 `service.ts` 의 insert·update, 읽기는 `loadCase()` 한 곳. 도메인 함수와 화면은 평문만 본다.
- 인테이크 답(jsonb)은 `{ "enc": "v1...." }` 한 칸에 통째로 담는다 — 컬럼을 늘리지 않는다.
- **P1 이전 평문 행은 그대로 읽힌다**(`decryptText` 가 암호문이 아니면 되돌려준다). 쓰기는 언제나 암호화하므로 고쳐 쓰는 순간 암호문이 된다.
- 같은 글도 매번 다른 암호문이다(IV). 그래서 **값 비교는 평문끼리** 해야 한다 — 전체 목표가 바뀌었는지 보는 곳이 그 예다.
- 검사: `./scripts/check-encryption.sh` 가 평문 한 줄이라도 남아 있으면 실패한다.

---

## 13. 백업과 복구 (P1, 2026-09-15)

```
./scripts/backup.sh          # 덤프 + SHA-256
./scripts/restore-drill.sh   # 복구 연습(운영 DB 를 건드리지 않는다)
```

- **열쇠는 백업에 들어가지 않는다.** 덤프 안의 자유 글과 금고는 암호문이고 `PII_ENC_KEY` 는 `.env` 에만 있다. 둘이 같은 자리에 있으면 암호화한 뜻이 없다. 열쇠는 별도 금고(1Password 등)에 두고 복구 때 손으로 가져온다. **열쇠를 잃으면 자유 글과 금고는 영영 못 읽는다** — 덤프가 멀쩡해도 그렇다.
- **백업은 "떴다"가 아니라 "되살아났다"로 증명한다.** 복구 연습은 옆에 빈 DB(`relayer_restore_drill`)를 만들어 되살리고, ① 해시 확인 ② 표 7개 행 수 대조 ③ **자유 글 한 건 복호화**까지 하고 연습용 DB 를 지운다. 복호화까지 해야 "열쇠와 덤프가 짝이 맞는다"가 증명된다.
- `backups/` 는 커밋하지 않는다(`.gitignore`).
- 실데이터 전환 때 정할 것(코드 밖): 보관 주기와 보존 기간, 백업 저장 위치(국내 리전·D2), 복구 연습 주기.

**2026-09-15 첫 연습 결과**: 표 7개 행 수 일치, 자유 글 복호화 성공, 통과.

### 13-1. 운영 배선 (2026-09-15)

| | |
|---|---|
| 뜨는 곳 | 맥미니 launchd `or.bss.relayer-backup`, 매일 04:10 |
| 대상 | `.env` 의 `DATABASE_URL`(Supabase `relayer2` 서울) |
| 보관 | `~/services/relayer2/backups/` 최근 14개(`BACKUP_KEEP`) |
| 복구 연습 | 맥북에서 `./scripts/restore-drill.sh` — 로컬 docker Postgres 로 되살린다 |

Supabase Pro 의 자동 백업(일 1회)과 **겹쳐 둔다**. 그쪽은 대시보드에서만 되살릴 수 있어
우리가 "되살아났다"를 증명할 수 없다. 논리 덤프는 우리가 직접 연습에 쓴다.

`pg_dump` 는 Homebrew `libpq` 를 쓴다(`brew install libpq`). PATH 에 없으면
`/opt/homebrew/opt/libpq/bin` 을 본다.

---

## 14. 당사자 열람 (P2, 2026-09-15)

당사자는 **로그인하지 않는다**(`GLOSSARY.md` §3). 실무자가 준 **링크 + 코드**로 연다.

```
participant_access(id, participant_id, token, code_hash, expires_at,
                   attempts_left, revoked_at, last_opened_at, created_by)
```

- **자물쇠가 둘인 이유**: 링크는 주소에 실려 대화방·메일에 남는다. 그것만으로 열리면 안 된다.
- 코드는 여섯 자리 숫자이고 **해시(Argon2id)로만** 둔다. 원문은 발급 직후 화면에 한 번 보이고 저장되지 않는다.
- 기본 14일 만료. 틀리면 남은 횟수가 줄고 **다섯 번에 잠긴다**. 맞으면 횟수가 회복된다.
- 새로 만들면 **이전 링크는 잠근다** — 살아 있는 링크가 여럿이면 누구에게 무엇을 줬는지 알 수 없다.
- 실패 응답은 링크가 틀렸는지 코드가 틀렸는지 구분해 주지 않는다.
- **보이는 것**: 이름·연락처·이메일과 **앞으로의 일정**뿐이다. 상담 내용·과제·목표는 이 경로로 나가지 않는다.
- 당사자 본인의 열람도 `audit_log` 에 남는다(`participant.view`, 실무자 열람과 구분).

### 14-1. 응답 캐시

API 응답 전체에 `cache-control: no-store` 를 건다(정적 파일 제외). 이름·연락처·상담 내용이 브라우저 디스크 캐시나 중간 프록시에 남으면 안 된다.

### 14-2. 개발과 운영의 DB 를 가른다

**테스트는 운영 DB 를 건드리지 않는다.** 맥북 `.env` 는 로컬 docker Postgres 를 보고, 운영(Supabase 서울)은 맥미니만 본다. 2026-09-15 에 잠깐 맥북도 운영을 보게 했더니 E2E 가 무작위로 하나씩 깨졌다(왕복 지연에 따른 경합). 합성 데이터라도 시험이 운영을 흔드는 구조는 두지 않는다.

---

## 15. AI 텍스트 경로 (P3, 2026-09-15)

**순서가 곧 규칙이다.**

```
동의 확인 → 마스킹 → 외부 호출 → 초안 저장 → 사람이 승인해야 기록
```

### 15-1. 동의가 먼저다

`external_llm_cross_border_processing` 이 `granted` 가 아니면 **호출 자체를 하지 않는다**(409). 문안에는 수신자가 묶여 있다.

**수신자를 바꾸면 기존 동의가 `확인 필요`로 떨어진다.** 2026-09-15 실측: 제공자를 OpenAI → Google 로 바꾸자 이미 받아 둔 동의가 자동으로 무효가 되고 호출이 막혔다. 누구에게 보내는지가 곧 동의의 내용이기 때문이다. 승인된 제공자 목록은 `api/src/consent.ts` 의 `AI_PROVIDERS` 뿐이다.

### 15-2. 마스킹 (`api/src/domain/masking.ts`)

화면 문구는 **`마스킹`** 이다(2026-09-16 Q — `가림`은 쓰지 않는다). 동의 문안 `외부 LLM·국외 처리`의
본문에 남은 `가림 처리한`은 문안 해시에 묶여 있어 바꾸면 기존 동의가 전부 `확인 필요`로 떨어진다.
문안을 고치려면 판(`COPY_VERSION`)을 올리고 다시 받는다 — 화면 정리로 바꾸지 않는다.

순수 함수다. 두 축으로 가린다.

| 축 | 가리는 것 |
|---|---|
| 아는 값 | 금고의 이름(→ 가명 자리표)·연락처·이메일 |
| 형태로 아는 값 | 주민등록번호 · 휴대전화 · 지역번호 전화 · 카드번호 · 계좌번호 · 남의 이메일 |

- **지우지 않고 자리표로 바꾼다.** 문장이 무너지면 요약이 엉뚱해진다.
- 금액·날짜·기관 이름은 **남긴다.** 다 지우면 요약할 것이 없다.
- 무엇을 몇 건 가렸는지만 `ai_drafts.mask_hits` 와 감사에 남는다. **가린 값 자체는 남기지 않는다.**

**2026-09-15 실증.** 이름·연락처·주민번호·계좌·카드·남의 이메일을 섞은 회차로 끝까지 돌렸다.

```
가린 것: name 3 · phone 2 · rrn 1 · email 1 · card 1 · account 1
김민희 / 김민희씨 / 김민희 본인  →  [lynx-099]      (조사가 붙어도 잡는다)
010-2345-6789 · 010-9876-5432   →  [연락처]        (딸 번호까지)
850312-2345678 → [주민번호]  ·  110-234-567890 → [계좌번호]
5412-7512-3412-3456 → [카드번호]  ·  kim.official@… → [이메일]
남긴 것: 월세 40만 원 · 두 달 · 20분
```

카드에 적힌 이름도 같은 길을 탄다 — 상담 내용만 가리고 카드를 빠뜨리는 실수가 잦다.
`api/test/ai-outbound.test.ts` 가 **외부로 나가는 글에 원문이 한 글자도 없음**을 고정한다.
- 되돌리기(복원)는 하지 않는다. 초안은 가명으로 읽는다.

### 15-3. 승인 게이트

```
ai_drafts(id, session_id, status, summary, tasks, questions, mask_hits, model, approved_by, created_by)
```

- append-only. 승인·수정은 **새 행**이며 마지막 행이 현재 상태다. 무엇을 보고 승인했는지가 남는다.
- **승인이 곧 기록이다**(2026-09-16 검수 — 승인 행만 남고 어디에도 안 떠서 고쳤다). 승인하면 같은 트랜잭션에서 `수행할 과제`·`다음에 물어볼 것`이 그 회차의 `source_type = ai_approved` 카드가 되어 다음 회차의 확인할 과제·물어볼 것으로 올라간다. 요약·달라진 것은 승인 행에서 읽어 **회차별 요약**과 **15초 다시보기의 직전 회차 요약**에 보인다. 다시 승인하면 결과가 안 찍힌 AI 카드만 갈아 끼우고, 사람이 같은 문장을 이미 적었으면 또 만들지 않는다. 마지막 행이 새 초안이면 이전 승인본으로 되돌아가지 않는다.
- 버튼은 **승인 / 수정** 둘이다. 반려·재생성은 없다(요구 21). 수정본도 별도 승인 전에는 초안이다.
- 후속 제안은 **수행할 과제 / 다음에 물어볼 것** 둘로 나뉜다(요구 22).
- **회차간 사실관계 변화**(`ai_drafts.fact_changes`, 2026-09-16 Q). 초안을 만들 때 그 사례의 지난 기록 회차
  자료(마스킹한 상담 내용·카드)를 함께 보내고, 지난 회차와 이번 회차가 **서로 어긋나는 사실**만 양쪽
  원문 그대로 받는다(`topic · before{seq,quote} · after{seq,quote} · note`). 판정하지 않는다 — 번복했을 수 있다.
  검토 화면의 `내용 불일치`와 회차별 요약의 접힌 카드에 보인다. 마스킹 건수는 이번 회차 것만 센다.
- AI 가 없어도 제품은 돈다. 키가 없거나 응답이 오지 않으면 **503 으로 정확히 실패**하고, 없는 것을 있는 것처럼 답하지 않는다.

### 15-4. 칸을 나눈다 — 한 덩어리 요약은 제멋대로 버린다

초안이 채우는 칸은 **사람이 쓰는 칸과 같다**(GLOSSARY §6-2). 그래야 승인하면 그대로 카드가 된다.

```
요약 (세 문장 이내)  ·  달라진 것  ·  수행할 과제  ·  다음에 물어볼 것
```

`실무자 의견`은 AI 가 쓰지 않는다 — 판정은 사람 몫이다.

**왜 나눴나.** 한 덩어리로 받으면 무엇을 버릴지 모델이 제멋대로 고른다. 804자 자료로 재 봤더니
`gpt-5.5` 는 "주민센터에서 면박당해 못 갔다"는 **진짜 이유**를 버렸다. 서류를 못 뗀 원인이자
다음 행동(동행)의 근거였다. 칸을 나누면 버릴 수 없다.

**우선순위를 준다.** 무엇이 중요한지 말해주지 않으면 잡담이 요약 첫 문장에 온다(실제로 그랬다).

```
1 안전 → 2 돈과 집 → 3 못 한 일과 그 이유 → 4 당사자가 먼저 요청한 것 → 5 건강·돌봄
버린다: 지각·날씨·교통, 반복, 변화 없는 상태
```

### 15-5. 모델은 재서 고른다 (2026-09-15 실측)

`reasoning.effort` 는 최저(`none`, 구모델은 `minimal`)로 고정한다. 적힌 말을 정리할 뿐이다.
`gpt-5-mini` 기준 `minimal` 2.3초 · `low` 8.2초 · `medium` 16.7초(초기값이었다).

**지표를 두 번 고쳤다.** 처음엔 사실 커버리지만 셌더니 **옮겨쓰기가 만점**을 받았다.
`gpt-5.4-nano` 는 804자 입력에 717자(89%, 20문장)를 그대로 옮겨 12/12 를 받았다.
길이 규칙(세 문장)을 함께 보자 곧바로 탈락했다. **요약이 길면 원문을 다시 읽는 것과 같다.**

804자 자료 · 칸 분리 + 우선순위 프롬프트 · 각 3회:

| 모델 | 속도 | 요약 문장(최대) | 핵심 6 | 잡담 | 판정 |
|---|---|---|---|---|---|
| **`gpt-5.5`** | **4.4초** | 3 | **6/6** | 0/3 | **채택** |
| `gpt-5.6-terra` | 4.0초 | 3 | 5/6 | 0/3 | 사실 누락 |
| `gpt-5.4-mini` | 3.1초 | **5** | 6/6 | 0/3 | 길이 어김 |
| `gpt-5.4-nano` | 4.1초 | **23** | 6/6 | **3/3** | 길이·잡담 |

작은 모델이 이긴 것처럼 보였던 것은 **지표가 옮겨쓰기를 보상했기 때문**이다.
제대로 재면 큰 모델이 이긴다. 다만 **구조가 먼저다** — 같은 `gpt-5.5` 도 칸을 나누기 전에는
핵심을 버렸다. 모델을 바꾸는 것보다 칸을 나누고 우선순위를 주는 편이 효과가 컸다.

관문 2 는 **입력 수와 걸린 시간**을 재는 관문이다. 여기서 10초를 흘리면 측정이 무의미해진다.

---

## 16. 음성 경로 (P4, 설계)

### 16-1. 동의가 셋 더 붙는다

정본(CCC `S7-consent-six-domains.md`)의 여섯 영역을 전부 쓴다. P4 가 쓰는 셋은 이것이다.

| 식별자 | 라벨 | 수신자 | 보유기간 |
|---|---|---|---|
| `counseling_recording` | 상담 녹음 | 사회연대은행(KR) — 기관 안에서 녹음한다 | — |
| `external_stt_processing` | 외부 STT 처리 | **정본이 `azure` 로 못박았다** | — |
| `voice_original_retention_period` | 음성 원본 보유기간 | 사회연대은행(KR) | `default_temporary_d85` |

**보유기간이 해시에 묶인다.** 음성은 "얼마나 갖고 있는가"가 동의의 내용이라, 기간이 바뀌면
기존 동의가 `확인 필요`로 떨어진다. 수신자가 바뀔 때와 같은 규칙이다(§15-1).

문안을 한 글자도 바꾸지 않았으므로 **기존 세 영역의 해시는 그대로**다 — 이미 받아 둔 동의는 살아 있다.

### 16-2. 기능이 없으면 동의도 없다

`AZURE_SPEECH_KEY` 가 없으면 음성 세 영역을 **목록에 두지 않는다**. 받을 이유가 없는 동의를
화면에 띄우면 무엇을 승낙하는지 흐려진다. 저장된 옛 사건은 기능이 꺼져도 그대로 남는다.

AI 와 같은 원칙이다 — **없으면 없다고 한다.** 가짜로 켜 두지 않는다.

### 16-3. 순서 (구현 예정)

```
녹음 동의 → 녹음 → 보유기간 고지 → STT 동의 → 마스킹 → 전사 → 사람이 확인해야 기록
```

음성 원본은 **기관 안에만** 둔다. 외부로 나가는 것은 STT 한 번뿐이고 그것도 동의가 있어야 한다.
전사문은 회차 기록의 후보이지 기록이 아니다 — 승인 게이트는 AI 초안과 같다(§15-3).

### 16-4. 불일치 둘 (요구 23, 2026-09-15 구현)

`대조` 는 하나가 아니라 둘이다.

| 이름 | 무엇을 견주나 |
|---|---|
| **음성·수기 기록 불일치** | 전사문과 실무자가 손으로 쓴 기록 |
| **회차간 기록 불일치** | 지난 회차에 적힌 것과 이번 회차에 적힌 것 |

둘은 원인도 대응도 다르다. 앞은 "못 들었거나 잘못 적었다"이고, 뒤는 "말이 달라졌다"이다.
뒤쪽은 당사자가 번복한 것일 수 있으므로 **틀렸다고 판정하지 않는다** — 달라진 사실만 보여 준다.
불일치 판정은 사람이 고칠 수 없는 잠금 항목이다(GLOSSARY §6-5).

**무엇을 견주나.** 문장이 아니라 **숫자**다. 상담 기록에서 어긋나면 곤란한 것은 대개 수다 —
연체 건수·금액·월세·밀린 개월·근무 일수·통원 주기. 문장이 달라도 수가 같으면 어긋난 것이 아니다.

- **한쪽에만 있는 것은 불일치가 아니다.** 안 적었을 뿐이고 그건 흔하다.
  그걸 불일치라고 하면 화면이 경고로 뒤덮여 진짜 불일치가 묻힌다.
- 같은 사실을 두 번 말했으면 **나중 것**을 쓴다. 말을 고쳐 하는 일이 잦고 나중 말이 정정이다.

2026-09-15 실측(2회차 → 3회차):

```
연체 건수: 2회차 3 → 3회차 4
근무 일수: 2회차 3 → 3회차 4
월세 45만 원은 그대로라 잡히지 않는다
```

`GET /sessions/:id/mismatches` 가 둘을 **따로** 낸다. 한 목록에 섞으면 무엇을 해야 할지 알 수 없다.

---

## 17. 보유기간·문서·회차별 다시보기 (2026-09-16 Q)

### 17-1. 보유기간은 1년 하나다

정본(CCC S7)은 `default_temporary_d85`(85일) 하나를 둔다. 릴레이어는 **`institution_retention_1y`(365일)
하나로 통일**한다 — 음성·기록·문서가 서로 다른 기한을 가지면 무엇이 언제 지워지는지 아무도
설명하지 못한다. 하나로 묶어야 당사자에게 한 문장으로 말할 수 있다.

**정본에서 벗어나는 값이다.** 값 이름에 기간을 담아 오해를 없앴다. 보유기간은 동의 문안 해시에
묶여 있으므로, 이 변경으로 기존 `음성 원본 보유기간` 동의는 `확인 필요`로 떨어진다. 설계대로다.

### 17-2. 서면 문서

정본 여섯 영역 밖에 **`document_attachment`(서면 문서 보관)** 를 더한다. 서면 문서에는
채무 내역서·진단서처럼 민감정보가 그대로 들어 있어, `민감정보 처리` 동의로 덮지 않고 따로 받는다.

음성과 같은 규칙이다.

```
동의 확인 → 기관 디스크 저장(0700/0600) → 목록 → 내려받기 → 1년 뒤 삭제
```

- **바이트를 DB 에 넣지 않는다.** 백업·복제본이 사방으로 퍼진다
- **원본 파일명을 쓰지 않는다.** `김민희_진단서.pdf` 는 그 자체로 정보가 샌다. 사람이 붙인 이름만 남긴다
- 받는 형식은 PDF·이미지·문서뿐이고 20MB 까지다. 실행 파일과 압축은 상담에서 주고받을 물건이 아니다
- 지운 뒤에도 행은 남는다 — "있었는데 지웠다"와 "처음부터 없었다"는 다른 사실이다

**다른 점 하나 — 문서는 다시 열어 본다.** 그래서 내려받을 때마다 `document.read` 를 감사에 남긴다.
누가 언제 열었는지가 남지 않으면 열람 통제가 없는 것과 같다.

2026-09-16 실측: 동의 없이 올리기 409 · 올린 뒤 `delete_after` = +365일 ·
내려받기에서 내용 일치 · 감사에 `document.add`·`document.read` 기록 · 디렉터리 0700.

### 17-3. 15초 다시보기는 회차마다 다르다

`GET /cases/:id/briefing?seq=N` 이 **그 회차까지 쌓인 것**만 낸다. 기록은 회차마다 쌓이므로
다시보기도 회차마다 다르다 — 3회차 다시보기는 3회차까지의 과제·질문·요약이고,
4회차에서 닫은 과제는 거기 없다. 그때는 아직 열려 있었다.

`seq` 가 없으면 지금이다. 다음 상담을 준비하는 화면이고 이것이 기본이다.

당사자 정보의 `15초 다시보기` 탭에서 `지금`과 회차들을 고른다.

2026-09-16 실측(사례 1): `seq=1` 과제 0건 · `seq=2` 과제 1건(출처 2회차) · `지금` 과제 1건.

---

## 18. 설정하기와 사이드바 (2026-09-16 Q)

### 18-1. 사이드바

한 줄에 아홉 개가 늘어선 가로 메뉴는 무엇이 무엇의 갈래인지 못 보여 준다("사람들이 헷갈려 한다").
정본(CCC preview)의 `.app-shell` 3부 — 머리줄·사이드바·본문 — 을 그대로 쓴다. CSS 는
이미 이식돼 있었고 켜는 장치만 없었다.

```
일정      상담 일정 등록 · 상담 일정 보기 · 상담 기록하기
당사자    당사자 목록 · 당사자 등록 (+ 열어 둔 사례가 있으면 당사자 정보)
설정      아래 표
```

`상담 기록하기`와 `상담 일정 등록`은 항상 보인다. 사례를 열지 않았으면 당사자 선택 화면으로,
열어 둔 사례가 있으면 해당 사례의 작성 화면으로 간다. `당사자 정보`만 사례가 있을 때 보인다.

### 18-2. 설정하기

`워크스페이스`는 대시보드를 뜻하는 말이라 화면에서는 쓰지 않는다 — 이름은 `설정하기` 하나다.

**묶음마다 사이드바 메뉴 하나**이고, 그 페이지에 항목이 곧바로 펼쳐진다(2026-09-16 Q 3차).

처음에는 항목 열둘을 사이드바에 세웠다 — 위의 일정·당사자가 그 밑에 묻혔다. 다음에는
`설정하기` 한 칸으로 접었다 — 이번에는 두 번 눌러야 내용에 닿아 어디 있는지 알 수 없었다.
묶음 넷이 그 사이다. 사이드바는 네 줄이고, 누르면 그 묶음의 내용이 전부 선다.

| 사이드바 메뉴 | 그 페이지에 서는 것 | 누가 |
|---|---|---|
| 내 정보 | 내 정보 · 화면 테마 · 계정 삭제하기 | 누구나 |
| 실무자 관리 | 실무자 목록 · 담당 배정하기 · 실무자 초대하기 | 관리자 |
| | 담당 배정 요청하기 | 실무자 (관리자는 `담당 배정하기` 안 `내 요청`으로 본다) |
| 기관 정보 관리 | 기관 정보 · 사업 목록 | 관리자 |
| 시스템 | API 연결 관리 · 열람 기록 관리 · 동의서 관리 · 자료 다운로드 | 관리자 |

**묶음 전체가 관리자 몫이면 실무자 사이드바에는 그 메뉴가 서지 않는다.** 실무자에게는
`내 정보`와 `실무자 관리` 둘이고, `실무자 관리`에는 `담당 배정 요청하기` 하나가 선다.

**관리자는 실무자가 하는 일도 전부 본다**(Q 지시). 관리자도 당사자를 맡는 사람이지,
남의 일을 구경만 하는 자리가 아니다 — `담당 배정 요청하기`가 관리자에게도 선다.

**역할은 `실무자`, 관계는 `담당`**이다(2026-09-16 Q 3차 검토 · 정본 D40·ADR-0017).
기관에 있는 것(`실무자 초대하기`·`실무자 목록`)과 이 사람을 맡은 것(`담당 배정하기`)은
다르다. 근거는 `GLOSSARY.md` §15-1.

**관리자 몫은 화면에서 감추는 것으로 끝내지 않는다.** 라우트마다 역할을 다시 본다.
감추기는 안내이지 잠금이 아니다. 2026-09-16 실측: 실무자가 `#/settings/org` 로 바로 들어가면
`관리자만 볼 수 있어요`, API 는 403.

### 18-3. 규율 넷

**로그인 아이디는 안 바뀐다.** 감사 기록이 사람을 아이디로 가리키는데 그것이 움직이면
지난 기록이 누구를 말하는지 흐려진다. 이름·연락처·연락 이메일만 고친다.

**탈퇴는 행을 지우지 않는다.** `deactivated_at` 을 찍어 로그인만 막는다. 그 사람이 남긴
상담 기록은 사례의 것이지 계정의 것이 아니다. 마지막 관리자와 맡은 당사자가 있는 실무자는
나갈 수 없다 — 나가면 워크스페이스를 고칠 사람이 없거나 당사자가 미아가 된다.

**초대장 없이 가입하는 길은 하나뿐이다 — 기관을 여는 첫 가입**(2026-09-17 Q, §24-1). 활성 관리자가
한 명도 없는 새 배포에서만 `#/signup` 이 열리고, 첫 관리자가 들어오면 닫힌다. 그 뒤 합류는 초대 링크뿐이다.
주소만 알면 누구나 들어오는 문은 상담 기록을 다루는 제품에 있어서는 안 된다. 토큰은 해시로만 저장하고
만든 순간 한 번만 보여 준다. 7일 뒤 만료된다. 메일 발송은 없다(링크 수동 복사).

**열쇠는 화면에 보이지 않는다.** `API 연결 관리`는 붙었는지 여부·제공자·출처(`db|env`)까지만 보여 준다.
OpenAI 키는 화면에서 넣을 수 있되(§24-3) 서버가 검증한 뒤 암호문으로 저장하고, 어떤 응답·감사·로그에도
값을 싣지 않는다. STT·DB 열쇠는 여전히 기관 서버의 환경 변수다.

### 18-4. 배정

당사자 목록의 `내가 맡기` 버튼과 요청 동작은 제거했다. 배정은 관리자의 기존 공동 배정 화면에서 관리한다.
이번 변경은 프런트만 다루며, 기존 배정 요청 이력·승인 API와 접근 권한은 그대로 유지한다.

미배정 사례는 서버가 허용한 가명·사업·담당자·상태만 표시하며 상세 링크를 만들지 않는다.
이름·회차·일정을 비워 받은 것은 자료가 없다는 뜻이 아니므로 `기록 없음`으로 표시하지 않는다.

### 18-5. 사업은 고르기만 — 그리고 사업은 실체다

`support_cases.program_name` 이 자유 텍스트라 `함께온기금 울타리대출`과 `함께온기금울타리대출`이
다른 사업으로 갈렸다. `programs` 표를 두고 **당사자 등록에서는 고르기만** 한다. 목록은
관리자가 `기관 정보 관리`에서 만든다. 이미 쌓인 사례의 사업 이름은 그대로 목록으로 옮겼다 —
지금 있는 것이 곧 정본이다.

2026-09-17(§24-4)부터 사례는 이름이 아니라 **`support_cases.program_id` FK** 로 사업에 묶인다.
이름을 바꿔도 사례가 따라오고, 종료는 잠금이지 삭제가 아니며, 다시 열 수 있다.
종료한 사업은 지우지 않는다. 이름이 사라지면 그 사업으로 열린 사례를 설명할 말이 없어진다.

---

## 19. 열람 기록은 무엇을 남기나 (2026-09-16 Q)

### 19-1. 목록 조회는 남기지 않는다

실측으로 700줄 가운데 **676줄이 `일정 목록`·`당사자 목록`** 이었고, 그 사이에 묻힌 실제
사건은 24줄이었다. 화면을 여는 것마다 한 줄이면 기록이 아니라 소음이다.

목록에는 가명과 회차만 실려 PII 값이 나가지 않는다. **남길 값이 없는 조회를 남기는 것은
감사가 아니라 접속 기록이다.** 누구의 무엇을 봤는지는 사례를 열 때 남는다.

### 19-2. 같은 열람은 접는다

15초 다시보기는 상담 중에 몇 번이고 다시 보는 화면이다. 그대로 두면 한 사람이 백 줄을 만든다.
**같은 사람이 같은 대상을 10분 안에 다시 열면 새 줄을 만들지 않는다** — 몇 번 봤는지가 아니라
봤다는 사실이 감사의 내용이다.

2026-09-16 실측: 다시보기를 다섯 번 연 뒤 늘어난 줄 0건.

### 19-3. 한 줄이 한 문장이다

```
[열람] 15초 다시보기 조회 · 김민희
       9. 16. 오전 11:35 · 시험 관리자 · 함께온기금 울타리대출 · 이름
```

**누가 · 누구의 것을 · 무엇을 했나.** 코드 이름(`case.briefing`)을 그대로 두면 관리자가
읽어 낼 수 없고, 읽을 수 없는 기록은 통제가 아니다. 사건 이름은 서버가 정한다
(`AUDIT_KINDS`) — 화면마다 다르게 부르지 않게 한 곳에 둔다.

`kind` 셋으로 거른다.

| 묶음 | 무엇 |
|---|---|
| **열람** | 누가 누구의 무엇을 봤나. 개인정보 통제의 본체다 |
| **기록** | 상담 자료가 바뀐 일. 동의·AI·음성·문서 |
| **운영** | 사람과 기관을 건드린 일. 배정·초대·설정 |

### 19-4. 이름은 표에 없다

감사가 또 하나의 개인정보 창고가 되면 안 되므로 `participant_id` 만 두고, **볼 때 금고에서
꺼낸다.** 가명만 보여 주면 관리자가 당사자 목록과 맞춰 볼 수 없어 "누구 것을 봤나"에
답하지 못한다 — 그것이 이 화면의 존재 이유다.

그래서 **이 화면을 연 것 자체도 남는다**(`열람 기록 조회`).

`AUDIT_KINDS` 에 없는 사건은 목록에 세우지 않는다. 옛 `schedule.list` 같은 줄이 섞이면
소음을 걷어 낸 뜻이 없어진다 — 표에는 그대로 남는다. **감사는 지우지 않는다.**

---

## 20. 열람 기록 화면 (2026-09-16 Q · `docs/audit-view.md`)

인터뷰로 정하고 그대로 구현했다. 근거와 레퍼런스 조사는 `docs/audit-view.md` 에 있다.

### 20-1. 시스템은 한 계층 더 들어간다

```
설정 › 시스템 ┬ API 연결 관리
              ├ 열람 기록 관리
              ├ 동의서 관리
              └ 자료 다운로드
```

다른 묶음(내 정보·실무자 관리·기관 정보 관리)은 항목이 그대로 펼쳐지지만, 시스템은
안의 화면들이 각자 크고 서로 상관이 없어 목록으로 세우고 눌러 들어간다(2026-09-16 Q 4차).
한 페이지에 쌓으면 무엇을 보러 왔는지 잃는다.

### 20-2. 찾기 전에는 아무것도 보여 주지 않는다

수백 건을 늘어놓으면 훑을 수 없고, **그 자체가 개인정보를 화면에 펼쳐 두는 일이다.**
글자를 치거나 눈여겨볼 것을 골라야 줄이 선다 — 그 전에는 서버를 부르지도 않는다.

요약을 위에 따로 세우지 않는다. **거르는 조건과 세는 조건이 같으므로 필터가 곧 숫자다.**
`맡지 않은 당사자를 연 것 322건`은 버튼 이름이면서 그 자체로 점검 결과다.

```
열람 기록 기간      지난 7일 · 지난 30일 · 지난 90일 · 지난 1년
눈여겨볼 것    맡지 않은 당사자를 연 것 322건 · 문서를 내려받은 것 1건 · 모두 483건
열람 기록 검색      김민희 · otter-001 · 내려받기 · 함께온기금
```

찾기 칸은 **무엇을 칠 수 있는지 미리 적어 둔다** — 빈 칸은 무엇을 넣으라는 말이 없으면
아무것도 못 넣는 칸과 같다.

### 20-3. 표로 본다

한 건이 세 줄로 흐르면 수백 건에서 못 읽는다. 엑셀 시트처럼 한 줄에 한 건을 놓는다.

```
언제 | 묶음 | 한 일 | 한 사람 | 대상 | 사업 | 자세히
```

정본에 표 부품이 없어 `app.css` 에 `.data-table` 을 만들었다(`DESIGN.md` §12) —
새 어휘 없이 기존 토큰만 쓰고, 머리줄은 sticky, 넘치면 표만 스크롤한다.

### 20-4. 판정하지 않는다

`맡지 않은 당사자를 연 것 304건`은 사실이고 `의심스러운 접근 304건`은 판정이다.
앞엣것만 쓴다. 붉은색도 경고 표시도 없고, 다른 숫자와 같은 모양으로 놓는다.
불일치 기능과 같은 규율이다 — 판정하지 않고 사실만 낸다.

세는 것 둘뿐이다. **맡지 않은 당사자를 연 것**(상담 기록은 맡은 사람만 보는 것이 원칙)과
**문서를 내려받은 것**(화면으로 본 것과 파일로 가져간 것은 다르다).
야간 열람과 열람 횟수는 세지 않는다 — 기관마다 근무 형태가 다르고, 횟수는 10분 접기가 걷었다.

`api/test/audit.test.ts` 가 사건 이름에 `의심·위험·경고·이상·불법·무단`이 섞이지 않는지 지킨다.

### 20-5. 찾기와 받기를 갈랐다

화면에서 찾는 것은 한 건을 짚는 일이고, 파일로 받는 것은 기간 전체를 통째로 옮기는 일이라
고를 것이 다르다. `자료 다운로드`가 따로 선다 — `기간` · `실무자` · `기록 종류` · `당사자 표기`.

**글자 검색은 화면이 한다.** 이름이 금고 암호문이라 서버가 이름으로 못 찾는다 —
불러온 것 안에서 거른다. 당사자 목록이 이미 같은 제약을 안고 있다.

### 20-6. 내보내기는 고르고, 그 선택이 남는다

`가명만`(외부 제출용)과 `이름 포함`(기관 안에서만) 중에 고른다.
**어느 쪽으로 내렸는지가 열람 기록에 남는다** — 자유는 두되 책임이 따른다.

화면 그대로 내리면 금고에 넣어 둔 이름이 평문 파일로 기관 밖에 나가고, 그 파일에는
우리 보유기간도 삭제 장치도 닿지 않는다.

2026-09-16 실측: `열람 기록 내려받기 · 이름 포함 · 줄 457 · 기간 30일`이 감사에 남음.

### 20-7. 3년 두고, 트리거가 지킨다

지금까지 `audit_log` 는 update·delete 를 통째로 막아 기한이 지나도 못 지웠고, 정리하려면
잠금을 손으로 풀어야 했다. **잠금을 푸는 절차가 상시로 필요한 순간 그 잠금은 이미 잠금이 아니다.**

그래서 규칙을 좁혔다(`0017_audit_retention.sql`) — **고치는 것은 여전히 못 하고,
지우는 것은 3년이 지난 줄만 된다.** 기록(음성·문서 1년)보다 길게 둔다. 사고는 늦게 발견되므로,
기록은 지워도 누가 봤는지는 남긴다.

2026-09-16 실측: 오늘 줄을 지우려 하면 `audit_log: 3년이 지나야 지울 수 있어요`.

### 20-8. 가명은 세지 않고 뽑는다 (2026-09-16 버그)

`nextPseudonym` 이 `count(*) + 1` 로 번호를 만들었다. **당사자를 지우면 그 수가 줄어
이미 쓴 가명이 다시 나오고**, 유일 제약에 걸려 당사자 등록이 500 으로 죽는다.
시험 데이터 142건을 지운 날 정확히 그 일이 났고, E2E 가 잡았다.

세는 것과 번호 매기는 것은 다른 일이다. `participant_seq` 시퀀스로 바꿨다 — 뒤로 가지 않는다.

---

## 21. 동의서 문안은 표준 양식이다 (2026-09-16 Q)

### 21-1. 한 줄 설명으로는 동의가 아니다

v1 문안은 영역마다 한 문장이었다. 개인정보보호법이 고지하라는 것 — **무엇을 받고, 왜 받고,
얼마나 두고, 거부하면 어떻게 되는지** — 가 없었다. 거부 권리가 빠진 문서는 동의가 아니라 통보다.

v2(`consent-standard-form-v2`)가 일곱 영역 모두에 그 넷을 채운다. 밖으로 나가는 영역은
**어디로 가나**(수신자와 국가)가 하나 더 붙는다.

```
무엇을 받나   이름 · 연락처 · 이메일 · 생년월일 · 주소 · 상담 신청 내용
왜 받나       상담과 사례관리를 제공하고 그 기록을 관리하기 위해서예요.
얼마나 두나   상담 종결 뒤 1년까지 보관하고 그 뒤에 지워요.
거부할 수 있나 동의하지 않을 수 있어요. 다만 이 정보가 없으면 상담 신청을 받을 수 없어요.
문안 판       consent-standard-form-v2 · 지문 70a314f1a841
```

### 21-2. 표준 양식 항목도 해시에 들어간다

`canonicalPreimage` 가 `items`·`purposeText`·`retentionText`·`refusalText` 를 함께 굽는다.
**무엇을 받고 얼마나 두고 거부하면 어떻게 되는지가 바뀌면 그것은 다른 동의다.**
`api/test/consent.test.ts` 가 그 계약을 지킨다 — 항목 하나를 바꾸면 해시가 달라져야 한다.

v1 → v2 로 판이 올라, **이미 받은 동의는 전부 `확인 필요`로 떨어진다.** 설계대로다.

### 21-3. 고치는 버튼은 아직 없다

화면은 읽기만 한다. 아코디언으로 영역을 펴 문안 전체를 본다.

문안을 고치면 그 영역에 동의한 **모든 사람에게 다시 알리고 받아야 한다.** 받기 전에는
그 기능이 멈춘다. 고치는 장치는 **알리는 절차가 정해진 뒤에** 붙인다(2026-09-16 Q) —
버튼이 먼저 생기면 절차 없이 눌린다.

`동의서 관리` 아래 `문안을 고칠 때` 카드가 그 사실을 네 줄로 적어 둔다.

### 21-4. 실무자 관리 배치

```
실무자 목록        (표 · 한 사람 한 줄 · 맨 오른쪽 당사자 보기)
담당 배정 요청     ├ 승인할 배정 요청  (남이 올린 것)
                   └ 내 요청          (내가 올린 것)
담당 실무자 배정
초대 링크 만들기 / 보낸 초대
```

목록이 맨 위다 — 누가 있는지 보고 나서 배정을 다룬다. 승인할 것과 내가 올린 것을 한 카드에
두는 이유는 **관리자도 당사자를 맡는 사람**이라, 둘이 갈리면 흐름이 끊기기 때문이다.
실무자에게는 `담당 배정 요청하기` 항목이 따로 선다.

---

## 22. 공동 배정과 음성 업로드 (2026-09-16 Q)

### 공동 실무자와 접근 권한

- `case_assignments`가 배정의 정본이다. `0019`는 기존 단일 배정만 이관하고 미배정 사례를 임의 배정하지 않는다.
- 신규 등록자는 생성 트랜잭션에서 최초 담당으로 배정된다. 이후 관리자가 복수 선택으로 추가·제외한다.
- 배정 요청 승인은 기존 담당을 유지하며 요청자를 추가한다. 이미 결정된 요청을 다시 승인해 제외된 사람을 되살릴 수 없다.
- 관리자도 비담당 사례의 상담 기록·문서·음성·전사·동의에 접근하지 못한다. 목록·감사·CSV에서도 비담당 상담 내용과 실명을 가린다.
- 다른 사례의 카드·회차 식별자를 제출해 자신의 사례 경로로 우회하는 요청도 거절한다.
- 당사자 열람 링크의 발급·조회·회수는 `/cases/:id/access`를 사용한다. 선택한 사례의 일정만 제공하며, 발급자가 배정에서 제외되면 해당 링크도 회수한다.
- 동의 사건은 당사자 식별자뿐 아니라 사례 식별자로도 범위를 제한한다. 한 사업의 동의 변경이 다른 사업의 처리 권한을 바꾸지 않는다.

### 음성 업로드와 비교

상담 기록 화면의 저장된 회차 또는 예정 회차에서 `음성·수기 기록 불일치`를 연다.
업로드 → 전사하기 → 전사 확인 → 불일치 확인 순서이며, 수기 기록을 자동으로 덮어쓰지 않는다.

- WAV·MP3·M4A·FLAC·OGG·WebM 컨테이너를 최대 **200MiB**까지 받는다(2026-09-16 Q, 브라우저 녹음 한두 시간 분량. Azure 한도 300MB·2시간 안). 파일 형식과 바이트 상한을 서버에서도 검사한다.
- 음성 원본은 `VOICE_ROOT`에 저장한다. 파일명은 서버가 생성하고 디렉터리/파일 권한은 0700/0600이다.
- 외부 전사는 Azure Fast Transcription의 multipart API를 사용한다. 실제 전사에는 유효한 Speech 키와 해당 리소스의 지역 설정이 필요하다.
- 전사문과 시간 구간은 암호화해 저장한다. 승인할 초안 ID를 확인하며, 문구를 수정한 승인본에는 이전 시간 구간을 그대로 붙이지 않는다.
- 현재 불일치 비교는 **숫자 항목 비교**다. 일반적인 의미·누락·진술 모순을 모두 검출하는 분석으로 표시하지 않는다.
- `네 건`·`열두 건`·`삼십오만 원` 같은 한글 수사는 숫자로 정규화해 비교하되 출처 문장은 원문을 유지한다. `내 건` 같은 동음어를 숫자로 추측하지 않는다.
- 전사 없음·확인 전·수기 기록 없음은 비교 불가 상태다. 기록이 수정됐지만 저장되지 않았으면 이전 비교 결과를 숨긴다.
- 음성·전사문을 정답으로 판정하지 않고 양쪽 값과 출처 문장을 보여준다.

`VOICE_ENABLED=1`은 업로드 기능을 켠다. `AZURE_SPEECH_KEY`와 지역 또는 엔드포인트가 있어야 전사가 준비된다.
설정은 `docs/secrets.md`를 따른다. 2026-09-16 Q 승인으로 기존 Speech 리소스의 KEY 2를 재사용한다. 실제 사용자의 녹음을 시험 데이터로 보내지 않는다.

외부 계약 출처: [Azure Fast Transcription](https://learn.microsoft.com/en-us/azure/ai-services/speech-service/fast-transcription-create),
[지원 지역](https://learn.microsoft.com/en-us/azure/ai-services/speech-service/regions?tabs=stt).

## 23. 시작이 곧 회차, 자동 전사 (2026-09-16 Q)

### 시작 경로

상담은 수기든 녹음이든 **시작하는 순간 회차**다. 부족한 정보는 나중에 채운다. 임시저장·예정 같은 중간 상태를 두지 않는다.

- `POST /cases/:id/sessions/start` — 본문 `{ session_id?, method?, is_closing? }`. 응답 `{ session_id, seq }`, 201.
  회차를 `status='done'`·`held_at=now`·`memo=null` 로 만든다. `session_id` 로 예정(planned) 회차를 주면 그 회차가 기록됨이 된다.
  지정한 회차가 이 사례 것이 아니면 404, 이미 기록됨이면 409, 사례가 종결됐거나 민감정보 처리 동의가 없으면 409.
- 화면은 회차 id 가 없는 상태에서 녹음 시작 또는 수기 첫 입력 때 이 경로를 **한 번** 부르고, 이후 업로드·저장에 그 id 를 쓴다.
- `PATCH /sessions/:id` 의 `memo` 는 **선택**이다. 안 보내면 있던 것을 지키고, 빈 글을 보내면 없음으로 둔다.
  수기가 없어도 회차 번호·회차 수·15초 다시보기·회차 한 줄에 포함되는 정식 회차다.
- `GET /cases/:id/detail` 의 `sessions[]` 에 `written`(수기 있음)과 `voice { recordings, transcript }` 가 붙는다.
  `transcript` 는 `none|pending|draft|approved|failed|skipped` — 전사문이 있으면 그 상태, 없으면 가장 최근 녹음의 진행 상태다.
  화면은 `written=false` 를 `수기 미작성` 으로 그린다.

### 자동 전사

업로드 응답은 전사를 기다리지 않는다. 서버가 응답 뒤 같은 프로세스에서 전사를 이어 돌린다 — 잡 큐·워커를 두지 않는다.

- 진행 상태는 `recordings.transcribe_state` 가 갖는다(`0023`). `pending` 전사 중 · `done` 초안 있음 · `failed` 오류 · `skipped` 외부 STT 동의 없음 또는 제공자 설정 없음. 이유는 `transcribe_note` 한 줄.
  `transcripts` 는 그대로 append-only 다 — 상태를 전사문 행에 적지 않는다.
- 외부 STT 동의가 없으면 보내지 않고 `skipped` 로 남긴다. 상담 녹음·보유기간 동의가 없으면 업로드 자체가 409 다.
- 실패하면 `failed` 와 이유를 남기고, `POST /recordings/:id/transcript`(전사하기)로 다시 돌린다. 자동·수동은 같은 길을 지난다.
- 서버가 다시 뜨면 `pending` 이던 녹음을 `failed` 로 바꾼다 — 끊긴 전사를 영원히 '전사 중'으로 두지 않는다.
- 전사 초안은 승인 전에도 `GET /sessions/:id/transcript` 로 읽힌다. 승인 게이트는 불일치 비교·기록 반영에만 남는다(§16-3).
- 감사: 자동 전사도 `voice.transcribe`. 보낸 음성·전사 원문은 감사에 남기지 않는다.

### 동의 철회와 종결

- 어느 사례에서 `counseling_recording` 또는 `voice_original_retention_period` 가 `withdraw` 로 기록되면 **그 사례**의 음성 원본을
  그 자리에서 지우고 `deleted_at` 을 찍는다(문안: "글로 옮긴 뒤 음성 원본을 바로 지워요"). 다른 사례는 건드리지 않는다(§22 사례 단위 동의 범위).
  전사문(글)은 남는다. 감사 라벨은 `voice.withdraw`.
- 종결(`support_cases.status='closed'`)된 사례에는 새 녹음 업로드·전사 요청·시작 경로를 409 로 거절한다. 이미 붙은 녹음의 재생과 전사문 열람은 그대로다.

화면 계약은 `docs/handoff-voice-ui-2026-09-16.md`(DESIGN 레인).

## 24. 기관 준비 — 첫 가입·마법사·사업 실체·역할 (2026-09-17 Q)

테넌시는 그대로 기관 하나다(PLAN A3). `organizations` 표·`org_id` 를 만들지 않는다. `organization` 단일 행에
`slug`(`^[a-z0-9-]+$`)·`onboarded_at`·`enc_openai_key` 컬럼만 더한다(`0024`). 기관별 서브도메인(기관.relayer.kr)은
DNS·리버스 프록시 절차(`docs/deploy.md`)이고 **앱은 Host 를 읽지 않는다.**

### 24-1. 기관을 여는 첫 가입

- `GET /auth/signup` → `{ open: boolean }`. 활성 관리자(`role='admin' and deactivated_at is null`)가 0명일 때만 `true`. 로그인 앞.
- `POST /auth/signup { org_name, slug, email, password, name }`. 로그인 앞. 한 트랜잭션에서 `organization` 행을 `for update` 로 잠근 뒤
  활성 관리자가 0명일 때만 관리자 계정을 만들고 기관 이름·슬러그를 적는다. Argon2 해싱은 트랜잭션 밖이다(초대 수락과 같은 모양).
  성공은 초대 수락과 같은 모양 — 로그인 쿠키(`relayer_session`) + `{ ok: true }`. 활성 관리자가 있으면 **403** 이고 계정을 만들지 않는다.
  같은 아이디가 이미 있으면 409. 동시에 둘이 두드려도 관리자는 한 명만 생긴다. 감사 `org.bootstrap`(fields `name`·`slug`·`user=<id>`).
- **관리자 수를 바꾸는 트랜잭션은 모두 같은 `organization` 행 잠금을 먼저 잡는다** — 가입·초대 수락·역할 변경·탈퇴.
  마지막 활성 관리자는 실무자로 내리거나 탈퇴시킬 수 없다(409).
- 초대 규율은 그대로다. 기관을 만드는 첫 가입만 예외고 그 뒤 합류는 초대 링크뿐이다. 메일 발송은 없다.
- 화면: 로그인 화면은 문이 열려 있을 때만 `기관 만들고 시작하기` 를 보이고 `#/signup` 으로 보낸다. 닫혀 있으면 `#/signup` 은 초대 안내만 낸다.

### 24-2. 마법사 `#/onboarding`

관리자 전용 단계형 화면. **기관 정보(reg_no·address·phone — 이름은 가입에서 이미) → 사업 1개 이상(이름·기간·한 줄 설명) →
실무자 초대(건너뛰기 가능) → API 연결 → 완료.** 설정의 기관 정보·사업 목록·초대·API 연결 폼을 그대로 쓴다 — 새 스타일·토큰이 없다.

- 완료는 `POST /settings/onboarding/complete`(관리자 전용) 로 `onboarded_at` 을 찍고(두 번 눌러도 처음 시각) `#/participants/new` 로 보낸다.
- `GET /me` 에 `onboarded: boolean` 이 실린다. `onboarded_at` 이 비어 있는 동안 관리자는 **클라이언트 리다이렉트**로 마법사에 머문다 —
  서버 잠금은 없다. 실무자는 영향이 없다. 마친 뒤 `#/onboarding` 으로 오면 당사자 등록으로 보낸다.
- `0024` 는 `update organization set onboarded_at = now() where name <> ''` 로 **기존 배포는 마법사를 건너뛴다.** `PUT /settings/org` 의 `name` 은 `min(1)`.

### 24-3. API 연결 — 키만 넣으면 되는 것과 설치가 필요한 것

`API 연결 관리` 는 둘을 갈라 보여 준다. **키만 넣으면 되는 것**: OpenAI API 키(→ gpt-5.5). **설치가 필요한 것**: STT(Azure)·DB — 상태·안내만.

- `PUT /settings/ai-key { key: string | null }` 관리자 전용. 저장 전 OpenAI `GET /v1/models` 로 검증해 실패하면 **400** 이고 저장하지 않는다.
  유효하면 `pii.ts` 의 `encryptPii` 로 `organization.enc_openai_key` 에 넣고, `null` 이면 지운다. 감사 `ai.key.set`(fields `provider=openai` 또는 `removed=1`).
- `ai.ts` 의 `callOpenAi` 는 **호출마다** DB 키 → `OPENAI_API_KEY` 순으로 고른다. provider 가 `openai` 일 때만 DB 키를 쓴다.
- `GET /settings/connections` 의 `ai` 에 `source: 'db' | 'env' | null` 이 붙는다. **키 값은 어떤 응답·감사·로그에도 싣지 않는다.**
- STT·Supabase 키 입력 방식은 이번 범위 밖이다.

### 24-4. 사업은 실체다

`programs` 에 `starts_on date`·`ends_on date`·`description text` 를 더한다(`starts_on ≤ ends_on` 검사, `ends_on` 은 정보용 — 지나도 잠기지 않는다).
`support_cases.program_id bigint not null references programs` 를 더하고 `program_name` 컬럼을 없앤다. 백필 규칙(`0024`):
목록에 없는 `program_name` 은 사업으로 만들고, `''` 은 `(미지정)` 사업(내린 채로 만들거나 재사용)에 붙인다.

- 사업 이름은 어디서나 `programs` 를 join 해서 읽는다. 응답의 `program_name` 은 파생값이고 `program_id` 가 열쇠다. `POST /cases` 와 당사자 등록 화면은 `program_id` 를 보낸다.
- `POST /settings/programs { name, starts_on?, ends_on?, description? }` → 201 사업 한 건. 이름이 겹치면 **409** — 되살리지 않는다(종료된 사업이면 다시 열기가 길이다).
- `PATCH /settings/programs/:id { name?, starts_on?, ends_on?, description? }` 관리자 전용, id 고정. 종료된 사업은 409(다시 열기 뒤에만).
  과거 열람 기록·당사자 화면에도 새 이름이 보인다. 감사 `program.update`.
- `DELETE /settings/programs/:id` — 열린 사례 수·열린 사례의 예정 회차 수가 0 이 아니면 **409 `{ open_cases, planned_sessions }`** 를 돌려주고 멈춘다.
  `?confirm=1` 이면 `retired_at` 을 찍는다. 화면은 그 사이에 `scripts/backup.sh` 를 권한다. 종료된 사업은 당사자 등록 선택지·기본 목록에서 빠진다(`?all=1` 에는 남는다). 감사 `program.retire`.
- `POST /settings/programs/:id/reopen` 관리자 전용, `retired_at` 만 `null` 로 되돌린다. 종결된 사례는 되살리지 않는다. 감사 `program.reopen`.
- **종료 잠금** — `access.ts` 의 `assertProgramActive(caseId)`. 사례 생성·인테이크 저장·새 일정·새 회차 시작·기록된 회차 고쳐 쓰기·문서 저장·동의 기록·열람 링크 발급·배정 요청은 **409** 이고 메시지가 사업 종료를 말한다.
  **예외**: 사례 종결, 열람 링크 회수, 관리자 배정 변경(`/settings/assign`), 이미 잡힌 예정 회차의 기록(`recordSession`·`startSession` 의 `session_id` 지정).
- 닫힌 사례의 예정 회차는 일정 목록(`GET /schedules`)에서 숨긴다 — 일정 취소 API 는 없다.
- **사업별 보기는 새 페이지가 아니다.** 사업 목록에서 사업을 고르면 사업명·기간·설명·담당 실무자(그 사업의 열린 사례에 배정된 실무자 — 파생)·참여 당사자 수가 보이고
  `#/participants?program=<id>`·`#/settings/staff?program=<id>` 로 걸개가 걸린 채 건너간다. `GET /participants` 행에 `program_id` 가 실리고 화면이 사업·이름·상태로 거른다.
  `GET /settings/workers?program=<id>` 는 그 사업의 열린 사례를 맡은 실무자만 돌려준다. `program_workers` 같은 명시 배정 표를 만들지 않는다.

### 24-5. 역할 바꾸기

`PUT /settings/workers/:id/role { role: 'worker' | 'admin' }` 관리자 전용. 자기 자신을 내리는 것도 되지만 **마지막 활성 관리자는 409**.
세션 캐시가 없어 다음 요청부터 새 역할이다. 실무자 관리 › 실무자 목록의 줄마다 `관리자로`/`실무자로` 가 선다. 감사 `user.role.update`(fields `user=<id>`, `role=<new>`).

### 24-6. 감사·시드·검증

- `AUDIT_KINDS` 운영 종류에 `org.bootstrap`·`program.update`·`program.reopen`·`user.role.update`·`ai.key.set` 이 붙는다. fields 에 키 값·이름 외 개인정보를 넣지 않는다.
- `seed.ts` 는 `test4`(둘째 관리자)를 더하고 기관 이름·슬러그·`onboarded_at` 과 사업 하나를 심어 `createCase` 에 `program_id` 를 넘긴다. 가입 문은 seed 없는 새 DB 에서 검증한다.
- 통합 테스트: `signup`·`role-change`·`migration-0024`·`program-lifecycle`·`program-filter`·`ai-key`. 새 DB 가 필요한 둘(가입·역할, 0024 백필)은 `api/test/scratch-db.ts` 로
  `relayer_shared_check_scratch_*` DB 를 만들어 쓰고 끝나면 지운다. E2E `web/e2e/onboarding.spec.ts` 도 같은 헬퍼로 새 DB·서버를 띄운다.
